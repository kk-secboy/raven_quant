# 个人量化投资与模拟盘系统设计稿

> 面向个人或 1—5 人小团队的中低频量化研究、回测、模拟投资、推荐与提醒系统

| 项目 | 内容 |
| --- | --- |
| 版本 | 1.1 策略扩展定稿 |
| 日期 | 2026-07-17 |
| 使用者 | 个人投资者或 1—5 人小团队 |
| 市场 | A 股及境内场内 ETF；只有已完成交易规则与风险建模的 ETF 子类型白名单可以启用 |
| 数据主源 | Tushare |
| 研究与回测技术 | RD-Agent + Qlib |
| 核心能力 | 数据更新、自动研究、训练、正式回测、模拟盘、投资推荐、月/周/日/分时提醒 |
| 明确排除 | 券商写入、自动下单、自动撤单、资金划转、真实账户控制（默认关闭的 QMT 沙箱插件除外，见 8.6/13 节）；券商只读不是当前依赖 |
| 文档状态 | 已冻结的目标设计；不代表当前代码已经实现，也不代表任何策略已经具有可投资价值 |

## 1. 产品目标与边界

### 1.1 产品目标

本系统使用真实市场数据，完成从数据更新到投资建议的完整闭环：

1. 定时下载并校验 Tushare 数据。
2. 使用 RD-Agent 改进已有策略，或研究新因子、新模型和新策略。
3. 使用 Qlib 构建数据集、训练模型、生成预测并执行回测。
4. 用正确的数学统计方法判断结果是否可能只是过拟合。
5. 用符合 A 股和场内 ETF 规则的方式计算成交、费用、容量和风险。
6. 将合格策略放入模拟账户持续运行，记录订单、成交、持仓、现金和净值。
7. 综合策略信号、账户状态和个人风险约束，生成唯一的账户级建议。
8. 提供月度、周度、日度和分时执行提醒，由用户自行决定是否操作。

系统可以得出“没有可靠机会”“继续持有”“等待执行”或“停止推荐”。为了保持有信号而降低验证标准，属于错误实现。

### 1.2 四条正确性主线

本设计只保留四条会直接影响结果可信度的业务主线：

| 方向 | 必须回答的问题 |
| --- | --- |
| 数学与统计 | 结果是否来自未来数据、重复试验、错误样本数或偶然波动？ |
| 金融市场与会计 | 价格、复权、费用、成交限制、公司行动、现金和 NAV 是否算对？ |
| 量化研究 | RD-Agent、Qlib、策略、模型、回测和版本之间是否边界清楚、结果可复现？ |
| 模拟盘投资 | 多策略如何形成一份账户建议，如何模拟成交并给出可执行但不误导的提醒？ |

数据下载、调度、日志、备份和通知属于最小工程支撑，不扩展成机构级通用平台。

### 1.3 明确不做

- 不连接券商写接口，不提交、修改或撤销真实委托。唯一例外是默认关闭的 QMT 沙箱插件（见 8.6、13 节）：它仅连接沙箱账户、默认禁用、不属于唯一主线；页面、调度和任何任务不得经它发单。
- 不读取或控制真实资金账户作为首版依赖；真实持仓只允许用户手工或 CSV 导入作为可选影子账户。
- 不做 Tick、Level-2、逐笔、毫秒级抢单或高频交易。
- 不建设第二套 C++ 回测引擎。
- 不把独立分钟 Alpha 作为低频主系统可用的前置条件；它可以进入独立研究模板，但在自己的数据、标签、交易规则、成本、容量和前向证据全部合格前，分时能力只服务已经批准的中低频目标。
- 不宣称支持所有场内 ETF。跨境、债券、黄金、商品和货币 ETF 只有在对应的交易日历、回转交易、估值、费用和特有风险已经实现并验收后，才加入可用白名单。
- 不建设 Kafka、全域事件总线、复杂微服务、mTLS、SBOM、复杂 IAM、审计哈希链等机构级设施。
- 不承诺盈利、最大损失、成交概率或所谓“精准买卖点”。

### 1.4 个人投资参数

以下参数必须由用户配置，不写成系统永久常数：

- 模拟账户初始资金和容量测试资金档位。
- 近期计划用款、应急现金和最低现金比例。
- 可承受的人民币损失、回撤处置线和恢复期。
- 单证券、单行业、单资产类别和单策略上限。
- 是否允许股票、ETF、配对、融资融券假设或其他特殊品种。
- 账户对科创板、创业板、北交所、风险警示证券和各 ETF 子类型的实际权限及确认日期。
- 每天可处理的建议数量、允许的操作时间和最长响应延迟。

回撤线是风险处置触发器，不是损失保证。跳空、连续跌停、停牌和流动性枯竭都可能使实际损失超过处置线。

## 2. 唯一主线与责任边界

### 2.1 端到端主线

```mermaid
flowchart LR
    A["Tushare 数据"] --> B["原始快照与质量检查"]
    B --> C["Qlib 数据集"]
    C --> D["RD-Agent 改进模板/研究新策略 + Qlib 探索"]
    D --> E["冻结候选版本"]
    E --> F["独立复算"]
    F --> G["未参与选择的 Qlib 正式样本外回测"]
    G --> H["隔离前向模拟"]
    H --> I["证据门 + 人工批准"]
    I --> J["账户级综合推荐"]
    J --> K["主模拟订单、成交与账本"]
    K --> L["月/周/日/分时提醒与报告"]
    L --> D
```

RD-Agent 不需要在每次日常推荐前运行。研究链按周、月或人工触发运行；日常生产链只读取已经冻结并允许进入模拟盘的策略和模型版本。

### 2.2 组件责任

| 组件 | 负责 | 不负责 |
| --- | --- | --- |
| Tushare | 行情、财务、成分、公司行动等研究数据 | 策略判断、账户决策和成交保证 |
| Qlib | Dataset、Expression、Model、训练、预测、Workflow、Recorder、正式历史回测 | 最终个人账户建议、持久模拟账本和真实交易 |
| RD-Agent | 自动提出和迭代因子、模型、参数及新策略候选，并调用 Qlib 做探索实验 | 自动批准策略、修改当前运行版本、生成正式账户建议 |
| 项目平台 | 数据认证、研究台账、验证、A 股规则、组合合并、推荐、模拟账本、调度和提醒 | 券商写操作和真实资金控制（默认关闭的 QMT 沙箱插件除外，见 8.6/13 节） |
| 用户 | 配置个人约束、确认策略进入模拟、决定是否参考建议并自行操作 | 不得用人工选择性执行结果反向冒充策略全量表现 |

### 2.3 三个唯一权威

系统只保留三个业务权威，避免同一事实由多套实现解释：

1. **正式历史回测权威**：候选冻结后，使用固定版本的 Qlib Workflow 和 Recorder 编排正式样本外回测；其中自定义 Executor/Exchange 适配器调用项目唯一的轻量执行核心。RD-Agent 展示的探索成绩和 Qlib 默认执行假设都不能直接作为正式成绩。
2. **账户建议权威**：项目组合与推荐模块将所有有效策略净额合并后，只发布一份账户目标；单个策略信号不能直接改模拟持仓。
3. **前向模拟权威**：项目模拟账本保存现金、订单、成交、持仓、公司行动和 NAV；Qlib 内部账户状态不能代替持久模拟账户。

项目只实现一个轻量、确定性的 `ExecutionCore`，不是事件溯源平台。它必须是无数据库、无系统时钟、无外部副作用的纯规则函数：

`ExecutionCore(account_snapshot, market_event, order_intent, rule_config) → fills + state_delta + reason`

该核心统一计算订单资格、下一 Bar、T+0/T+1、申报单位、价格限制、成交量、部分成交、费用、现金冻结/释放、可卖数量和待交收状态，但只返回结果，不自行修改 Qlib 对象或 PostgreSQL。正式历史回测中，Qlib Account/Position 是本次运行的临时状态投影；前向模拟中，PostgreSQL 是持久状态投影。两种适配器都只能把同一 `state_delta` 应用一次，不得再次计算或重复应用成交、费用、交收和现金变化。策略只读取上一事件已经应用完成的账户快照，不能在 Qlib 状态和另一套账本之间择优取值。

每个结果使用账户、订单、Bar 时间和规则版本形成稳定事件键。公司行动和交收由独立账本处理器驱动时，也必须调用同一组纯规则原语并只应用一次。两条适配链在相同历史事件和订单输入上必须通过黄金案例和差分测试；Qlib 未改造的默认执行器只作探索对照。

## 3. Tushare 数据与时间语义

### 3.1 下载方式

数据链分为初始化和日常增量：

Tushare 是行情、财务、成分和研究数值的生产主源；交易所、监管机构、中国结算和基金管理人的公开资料允许用于交易规则、税费、公司行动和产品属性核验，但不能未经新数据版本静默混入 Alpha 特征或收益。所有时间使用 `Asia/Shanghai`，交易任务按对应交易所日历运行。

- 初始化按接口允许的完整时间窗口批量下载，优先按交易日或时间区间取数，避免逐证券、逐日双重循环。
- 日常任务按交易日增量下载，接口迟到时等待实际数据水位，而不是到固定时间就假设数据完整。
- 历史分钟数据按 Tushare 权限和接口限制分批下载；盘中只拉取主动账户持仓/待执行低频目标、独立 `forward_paper` 分钟研究账户的冻结白名单，以及已进入 `recommendation_enabled` 的分钟策略冻结白名单，不扫描未批准的全市场候选。
- 原生研究频率至少区分日线、1min 和 5min；15min、30min、60min 等只能由已经闭合且通过质量门的更细 Bar 按冻结规则重采样，并保存来源频率和聚合版本，不能把未闭合 Bar 当成完整输入。
- 每次调用记录接口、参数、时间范围、获取时间、行数、权限状态、错误和重试次数。
- Token 只保存在密钥配置中，不进入日志、实验提示词或版本库。

### 3.2 数据分层

系统保留三层数据：

1. **原始快照层**：保存 Tushare 原始响应和校验和，不原位覆盖。
2. **标准化层**：统一代码、时间、单位、交易日历、可用时间和字段语义。
3. **Qlib 数据集层**：只从通过质量检查的标准化快照构建，记录来源快照、构建配置、频率和版本。

上游数据修订时创建新快照并重建受影响数据集、模型、回测和推荐；历史上已经发出的建议和模拟成交不得被静默改写，只能追加更正说明和反事实重算。

### 3.3 PIT 与可用时间

每项可能改变历史研究结果的数据至少区分：

- `effective_at`：经济事实属于哪个日期或区间。
- `available_at`：市场参与者最早何时能够知道。
- `ingested_at`：平台何时实际取得并通过检查。

研究和回测只能使用决策时点之前已经 `available_at` 的版本。财务数据按实际公告时间使用；指数成分、行业分类、股本、停复牌和退市状态必须按当时版本展开，禁止用当前状态回填历史。

每个 PIT 字段还必须标记历史可恢复等级：`native_history`（上游原生历史版本）、`reconstructed`（依据当时公告重建）、`current_only`（只有当前修订）或 `unavailable`。只有前两类可以作为正式历史证据；后两类只能用于当前观察或研究提示。若来源只有发布日期而没有可用时刻，保守地从下一可用交易时点开始使用。

股票池必须包含历史退市证券，不能只用今天仍上市的证券，否则会产生幸存者偏差。

### 3.4 Tushare 单位与价格

| 数据 | Tushare 原始单位 | 平台标准单位 |
| --- | --- | --- |
| A 股日线 `vol` | 手 | 股，乘以 100 |
| A 股日线 `amount` | 千元 | 人民币元，乘以 1,000 |
| 已核实为股/元的分钟接口 `vol/amount` | 股/元 | 保持股/元，不重复换算 |
| OHLC | 未复权价格 | 原始价永久保存 |
| `adj_factor` | 复权因子 | 原始因子永久保存，研究计算必须固定锚点和快照 |

不同 Tushare 接口可能使用不同单位，不能只按字段名推断；每个接口必须有字段映射和数量级测试。

执行、涨跌停判断、成交和账本使用原始价格。研究收益或连续价格使用带快照和锚点的复权计算，例如：

`adjusted_price(t, b) = raw_price(t) × adj_factor(t) / adj_factor(b)`

`total_return(t) = raw_price(t) × adj_factor(t) / [raw_price(t-1) × adj_factor(t-1)] - 1`

公式方向必须用分红、送转和拆股黄金案例验证。不得把随查询截止日变化的前复权序列作为无版本永久事实。

### 3.5 数据质量门

进入 Qlib 前至少检查：

- 主键重复、时间顺序和交易日缺口。
- `low ≤ open/close ≤ high`、负价格和异常跳变。
- 成交量、成交额单位及 `price × volume` 数量级。
- 日线与分钟聚合的一致性。
- 复权连续性和公司行动价值守恒。
- 财务公告时间、成分生效时间和未来字段泄漏。
- 停牌、零成交量、上市、退市和证券代码变更。

关键数据缺失、权限不足、单位不明或数据过期时，禁止形成新的风险仓位，不能用旧数据生成伪新建议。已有目标和未完成模拟订单必须按预先配置的有效期进入 `WAIT`、`CANCELLED`、`EXPIRED` 或人工风险复核；不能只输出 `NO_ACTION` 后让旧计划无限继续。硬风险退出只有在账户和市场数据仍足够可信时才能继续释放，否则保持阻断并升级提醒。

## 4. 数学统计与反过拟合

### 4.1 时间切分

所有研究必须遵守时间顺序：

1. 训练区间用于拟合模型和预处理器。
2. 验证区间用于选因子、参数和模型。
3. Walk-forward 外层区间用于评价整个选择流程；每个外层 Fold 都只能使用该 Fold 之前的数据，重新执行内层训练、验证和候选选择。若只是把同一冻结策略放入多个历史区间滚动运行，必须称为“冻结策略的滚动样本外评价”，不能声称评价了 RD-Agent 的完整选择流程。
4. 最终保留一个未参与选择的样本外区间，只在候选冻结后使用。

标准化、缺失填充、中性化、特征选择、超参数选择和模型集成都只能在相应训练窗口拟合。标签区间或持仓期重叠时，训练、验证、外层 Fold 和最终样本外的所有边界都按标签跨度与持有期使用 purge；需要避免相邻信息传播时使用 embargo。

不得把所有策略硬套相同的训练天数、样本外天数或固定阈值。窗口长度必须在研究开始前根据决策频率、持有期、标签重叠、市场状态覆盖和可用历史确定。

每次研究在打开最终样本外区间前，必须冻结一份简化准入规范：主要指标及方向、简单基线、最小经济改善、证据充分性、成本、容量、风险门、压力场景和失败条件。人工确认可以决定是否继续观察，但不能覆盖已经失败的硬门。

最终样本外是一次性资源，而不是每个候选都能重复领取的区间。每次研究至少保存 `research_campaign_id`、`hypothesis_family_id`、`oos_vintage_id`、`sealed_at`、`first_opened_at` 和 `consumed_at`。若联合确认多个候选，打开前还必须保存 `sealed_candidate_set_id/hash`、有序候选 ID 与 `StrategySpec` 哈希、主要指标、通过规则和多重检验规则。任何人、脚本或 Agent 第一次看过结果后，该 `oos_vintage_id` 即标记为 `consumed`；所有 `candidate_formation_ts > first_opened_at` 的候选都不再具有该区间的最终未见样本资格，不论后来如何新建或改名研究活动、假设谱系和策略版本。即使候选在打开前已经存在，只要未列入密封候选集合，也不能在看过结果后补入该集合或使用该 OOS 晋升。只有首次打开前共同冻结且列入密封集合的候选，才可按预注册规则一起确认，不能看完后再挑赢家。

RD-Agent 研究环境在物理挂载和数据权限上都不能读取最终样本外区间；只有候选冻结后的独立正式任务可以读取。打开结果后发生任何特征、标签、模型、参数、组合或选择规则修改，都必须形成新候选，并使用之后新产生的数据或隔离前向模拟继续验证。

### 4.2 试验台账

人工、脚本和 RD-Agent 的所有候选使用同一份简化试验台账，记录：

- 研究问题、经济假设和简单基线。
- 数据集、特征、标签、模型、参数、成本和代码版本。
- 训练、验证、外层 walk-forward 和最终样本外边界。
- 成功、失败、中止和被放弃的全部试验。
- 候选之间的继承关系，以及研究人员何时看过哪些结果。
- `candidate_formation_ts`、所属研究活动/假设谱系及最终样本外的密封、首次打开和消耗状态。
- RD-Agent/LLM 提供商、模型标识、可获得的知识截止信息、是否联网、检索资料清单和提示词版本。

不能只登记赢家。查看最终样本外结果后再修改候选，必须创建新候选，并使用之后未参与修改的新数据继续验证；同谱系候选不能继续复用已经消耗的最终样本外。

LLM 可能已经从训练语料或联网检索中知道历史测试期之后的论文、事件和策略结果，因此旧历史区间不一定是真正未暴露证据。存在这类风险时，旧历史回测只能支持技术和经济合理性，候选必须继续使用形成后的新数据做隔离前向模拟，不能仅凭旧历史结果进入个人推荐。

### 4.3 独立样本与不确定性

- 股票数量不是时间独立样本数。
- 分钟 Bar 数不是独立投资决策数。
- 重复交叉验证折、随机种子和 Monte Carlo 路径不产生新的市场证据。
- 月频策略主要按独立月度决策和完整持有周期判断证据；周频、日频和分钟策略分别按自己的有效决策、成交和持有周期判断。
- 截面因子先计算每日 IC/Rank IC，再对日期序列做推断，不能把同一天数千只股票当作数千个独立日期。

时间序列相关性可使用 HAC 标准误或时间区块 Bootstrap；候选相对基线的差异应在相同时间区间做成对比较。区块长度、方法和主要指标必须预先确定，并进行合理敏感性检查。

候选很多时，根据假设数量和依赖结构选择一种适当的标准校正方法，例如探索阶段的 FDR，或最终少量候选的 Holm 校正。DSR、PBO 等只在前提成立时作为诊断，不把所有方法同时堆成通用认证平台。

### 4.4 指标口径

正式结果至少报告：

- 扣除完整成本后的累计收益和年化收益。
- 年化波动率、Sharpe、Sortino、最大回撤和恢复期。
- 相对策略基准的超额收益、Information Ratio 和 Tracking Error；仅在适用时使用。
- 换手、交易次数、持仓周期、容量和未成交比例。
- 主要结果的置信区间或明确的“证据不足”。
- 对简单可投资基线的增量价值，而不只看收益是否大于零。

账户人民币 NAV 用于账本对账；策略收益、回撤和风险指标统一基于扣除完整成本后的日频单位化 TWR 表现曲线。只有实际转入或转出被评价账户的资产才是外部现金流；尚未转出的计划用款只改变最低现金和投资约束，不进入收益公式。

首版把实际外部现金流分成期初和期末两类。开盘前已确认且可用于当日决策的净流量记为 `F_t_open`；开盘后确认的净流量记为 `F_t_close`，当日不得用于新交易，下一交易日才进入可投资现金。入金为正、出金为负。设 `V_{t-1}` 为上一交易日收盘 NAV，`V_t` 为包含实际现金流后的当日收盘 NAV：

`r_t = (V_t - F_t_close) / (V_{t-1} + F_t_open) - 1`

`investment_wealth_0 = 1`

`investment_wealth_t = investment_wealth_{t-1} × (1 + r_t)`

`TWR = investment_wealth_T / investment_wealth_0 - 1`

分红、利息、卖出回款、费用和税费都是账户内部现金流，不进入 `F_t_open/F_t_close`。若分母小于等于零、外部现金流时点不明确或账本未对平，从该日开始的连续表现曲线标记为不可用，必须修复账本并从最后一个已验证状态重建，不能只跳过该日后继续连乘。

最大回撤和恢复期基于单位化表现曲线，不基于人民币余额：

`max_drawdown = max_t [1 - investment_wealth_t / max_{s≤t}(investment_wealth_s)]`

恢复是 `investment_wealth` 重新达到此前高水位。`CAGR = (investment_wealth_T / investment_wealth_0)^(365.2425 / elapsed_calendar_days) - 1`。正式 Sharpe、Sortino、Information Ratio、年化波动率和 Tracking Error 必须冻结同一套日频收益、交易日年化因子、可获得现金/无风险收益、最低可接受收益、基准总收益和零方差处理。分钟结果先汇总到账户日收益再年化；样本不足、时间未对齐或分母为零时输出“未定义/证据不足”，不得输出无穷大或伪精确数值。

目标权重换手可统一采用：

`one_way_turnover = 0.5 × [Σ_证券 |target_weight_i - current_weight_i| + |target_cash_weight - current_cash_weight|]`

XIRR 只作为个人资金体验的资金加权补充，不作为策略 Alpha 的主要证据；账户内部股息、卖出回款、费用和税费不能再次当作外部现金流。

Qlib 分数、预期收益和上涨概率是三个不同对象。未经样本外校准的排名分数不得展示成“上涨概率 80%”或“预期收益 10%”。

### 4.5 策略验证与晋升原则

冻结候选进入隔离前向模拟 `paper` 前必须同时满足：

- PIT、下一 Bar、单位和防泄漏测试通过。
- 最终样本外在打开前没有参与候选形成、调参或挑选；若预注册多个候选，只能按冻结的联合通过规则和多重检验办法作确认，不能看完后再事后挑赢家。
- 扣费后相对简单基线仍有经济意义，或在相近收益下显著降低风险、提高流动性。
- 参数邻域、不同市场阶段、成本、容量和延迟压力不过度脆弱。
- 结果可从冻结输入和代码重新生成。
- 样本不足时标记“证据不足”，不能通过降低门槛晋升。

从 `paper` 进入 `recommendation_enabled` 前，还必须达到该策略预注册的最少自然时间、独立决策/成交事件、完整持有或再平衡周期、数据完整率、账本对账率和成本偏差要求。只有事前声明为策略专属硬门的关键市场状态才必须覆盖；其他尚未经历的状态保留风险标签或限制资本预算，不能假装已经验证。证据不足时继续处于 `paper`。历史验证门和前向证据门不能相互替代。

不同策略使用不同主要目标：指数增强重点检查主动收益、IR、Tracking Error 和暴露；ETF 配置重点检查政策基准、回撤、流动性和资金用途；趋势或 Swing 检查成本后收益、尾部风险和换手；执行算法检查完成率与 Implementation Shortfall。账户回撤处置线不能机械复制为所有策略的准入线。

## 5. 金融市场、成本与账户语义

### 5.1 证券规则按品种和日期配置

每只证券至少维护以下有效期规则：

- 交易所、板块和证券子类型。
- 当日买入何时可卖，即 T+0、T+1 或其他回转规则。
- 买入和卖出申报单位、零股规则、价格步长。
- 涨跌幅比例、参考价、舍入方式和上市初期特殊规则。
- 停牌、临时停牌、风险警示和退市阶段。
- 印花税、过户费、基金或其他品种费用适用性。
- 当日买入何时可卖、卖出款何时可继续交易、法定交收何时完成、现金何时可取；四者不得混成一个 T+N 字段。

不能假设所有 ETF 都是 T+0，也不能假设所有 A 股和 ETF 具有同一税费、涨跌幅和交收规则。只有已完成规则、估值和特有风险测试的 ETF 子类型才能进入白名单；其他 ETF 只能展示或研究。规则变化时增加新版本，并重跑覆盖相关日期的回测和模拟验证。

### 5.2 信号与成交时间

- 信号只能使用决策时点已经完成并可获得的数据。
- 任何信号最早在下一可执行 Bar 成交。
- 日线收盘后形成的目标最早在下一交易日执行。
- 分钟 Bar 完成后形成的判断最早使用下一根分钟 Bar。
- 股票当日买入数量按证券规则进入不可卖状态；允许回转交易的 ETF 按自身规则处理。

### 5.3 成交模型

仅有 OHLCV Bar 时，系统不能知道真实盘口队列，因此只能做保守模拟：

- 停牌、无有效 Bar 或成交量为零时不成交。
- 一字涨停买入、一字跌停卖出等缺乏排队证据的方向，按保守未成交处理；这不是“交易所禁止申报”。
- 订单量不能超过预先配置的单 Bar 和单日参与率，超过部分部分成交、延期或过期。
- 买入不能超过可用现金，卖出不能超过可卖数量。
- 跨日订单明确规定继续、取消或重新定价，不能默认按收盘价强制成交。
- 历史 Bar 回放只能代表模型化成交，不得展示成真实排队或保证成交概率。

### 5.4 成本只计算一次

公共成本计算器按证券、方向、日期和订单逐笔计算：

- 券商佣金和单笔最低佣金。
- 适用品种的印花税、过户费和其他明确费用。
- 买卖价差、滑点、市场冲击和人工延迟。
- 借券费用只用于具备明确借券假设的研究模拟。
- 未成交和延迟单独用于实现差额分析，不能既改成交价又再次从现金扣除。

每项经济成本只能在“成交价格影响”或“现金费用”中出现一次。若基金净值、市场价格或收益序列已经内含管理费用，不得再次从账户扣除同一费用。

成本和税费是带生效日期的运行配置，不在设计稿中永久写死。没有真实成交校准时使用保守参数；模拟成交不能自我证明成本模型准确。

### 5.5 容量与离散交易

容量验证必须同时考虑：

- 账户规模、目标订单金额和整手误差。
- 最低佣金对小额订单的拖累。
- PIT 成交额、分钟成交量、停牌和涨跌停。
- 单 Bar/单日参与率、允许执行天数和未成交比例。
- 建仓、减仓和退出在正常及压力状态下需要的时间。

固定“日均成交额大于某值”只能用于初筛，不能代替容量验证。策略可以在某个账户规模有效、在另一个规模无效，报告必须明确适用资金范围。

组合优化器不是首版必选组件。现金、等权或固定预算策略可以不用优化器；只有策略确实使用风险预算、基准相对约束或多资产权重求解时，才必须满足以下合同：

- 冻结信号语义、预测期限、协方差估计期限与方法、基准权重、当前持仓、有效未完成订单、完整成本、现金和硬约束；排名分数不得冒充预期收益。
- 协方差期限与收益、持有和再平衡期限相容，并检查缺失、非有限值、收缩、半正定性和病态程度。
- 检查求解状态、残差和全部约束。超时、不可行、非有限解或约束违例时 fail-closed，回到上一个仍有效目标、预注册简单基线或现金，不能采用“看起来合理”的权重。
- 普通现金账户只能向现金缩放风险；未经策略和个人政策明确批准，不得为达到目标波动率自动加杠杆。
- 连续权重按申报单位、最低订单、最低佣金、可卖数量、冻结资源和有效未完成订单离散化；离散后必须重新检查现金、单票、行业、资产类别、Tracking Error、换手、容量和风险，不能静默放宽硬约束。
- 保存优化合同与求解器版本、输入哈希、连续/离散权重、绑定约束、失败原因和确定性种子。

### 5.6 公司行动、退市和估值

- 执行和账本始终使用原始价，公司行动分别调整现金、应收、持仓数量和证券身份。
- 现金分红、送转、拆并股、配股、换股、代码变更、ETF 份额折算和基金清盘按事件处理，不能只依赖复权价。
- 每个公司行动至少区分公告、权益登记、除权除息/经济生效、到账或新增证券上市可交易阶段，并分别保存 `effective_at` 和唯一事件键。公告只产生信息和提醒，不改变现金、持仓或 NAV；登记日只确定资格，不提前确认未来资产。
- 除权除息/经济生效时确认现金分红应收，或调整拆分、送转后的经济数量与单位成本，以避免价格机械下跳形成虚假亏损；尚未到账或上市的权利保持应收或不可卖状态。到账/上市阶段只把应收重分类为现金或把新增证券改为可卖，除税费差额外不得再次增加 NAV。
- 需要持有人选择的复杂事件由系统提醒并进入人工处理；不得替用户提交选择。
- 未完成订单在公司行动前后按规则取消或调整，不能沿用旧价格和数量。
- 股票股息税按带日期版本的税务规则和固定持仓批次匹配规则计算；金额尚未最终确定时记录保守税费负债或估值不确定性，后续只确认差额。
- 退市不统一假设为现金结算；持仓可能经历整理期、转板、换股、清算或零回收。在确认回收事件前保留持仓或权利，并使用保守估值。
- 停牌、长期无成交或估值来源不可靠时，NAV 必须标记估值不确定，不能把最后价格冒充可变现价格。

### 5.7 账户守恒

系统唯一采用**交易日会计**。模拟成交一经确认，立即确认经济持仓或卖出回款以及本笔交易费用；交收只改变可卖、可交易、可取和科目分类，不再次改变经济资产与 NAV。

模拟账本至少维护：互斥的现金余额桶、经济持仓数量、可卖数量、冻结数量、公司行动应收、应付款、费用、税费和外部现金流。`tradable_at`、`withdrawable_at` 是现金批次的可用时间属性，`sellable_at` 是证券批次的状态属性，不另记一份现金或证券资产。买入成交按成交金额和已确认费用减少现金、增加经济持仓；卖出成交减少经济持仓并增加一笔带可交易/可取时间的回款现金批次。卖出回款不得同时出现在现金、在途现金和应收款中，待交收证券也不得在经济持仓之外再计一份资产。

模拟订单创建时只把自由现金/证券重分类为冻结状态，不改变 NAV；买入成交从对应冻结现金批次消费，取消或部分未成交只释放剩余冻结额。任何冻结、消费和释放都使用同一订单事件键，不能在可用余额和冻结余额各扣一次。

每个持仓批次保存取得日期、剩余数量和成本基础，用于已实现/未实现损益及持有期相关股息税。闲置现金只按账户实际可获得的配置收益计息；没有对应现金工具时使用零收益，不能把研究无风险利率当作账户实际收益。

交收事件只做状态重分类，除新增确定的费用或税费差额外不得改变 NAV。模拟交易费用在成交时按冻结规则确认一次；日终校准或用户导入最终费用时只记录“最终费用减已确认费用”的差额，不得再次全额计提。公司行动应收与普通卖出回款分开保存，每个成交、费用、交收和公司行动事件都必须有唯一键。

日终必须验证：

`NAV = Σ互斥现金余额 + Σ(经济持仓数量 × 估值价格) + 公司行动应收 - 应付款及已确认税费负债`

`可交易现金` 和 `可取现金` 是根据现金批次及其时间属性计算的权限视图，不能再加进 NAV。买入、卖出及随后交收在市场价格不变且不考虑费用时只能改变资产分类，不能令 NAV 重复跳变；重复处理同一事件不得再次改变现金、证券或费用。

实际入金和出金是外部现金流，不得误报为投资收益或亏损；尚未实际划出的计划用款只是账户现金约束。公司行动前后在没有真实经济损失的情况下应满足价值守恒。

## 6. RD-Agent、Qlib 与完整策略研究体系

### 6.1 先分清组件、模板、策略版本和模型制品

本系统不再只保留“一个示例策略”。两份 Word 和扩展 Markdown 中适合个人股票/ETF系统的策略思想都进入本章，但必须先按职责拆开，不能把指标、策略、组合方法和执行算法混成一个概念。

| 对象 | 含义 | 例子 |
| --- | --- | --- |
| `strategy_family` | 经济逻辑家族 | `asset_allocation`、`cross_sectional_alpha`、`trend`、`mean_reversion`、`relative_value`、`event` |
| `engine_type` | 策略自身证券目标的形态，不包含账户分配或执行 | `long_only_basket`、`long_only_relative_allocation`、`offline_pair_research` |
| `template_id` / `recipe_id` | 可运行的研究合同 | `index_enhancement`、`trend_breakout`、`minute_mean_reversion_etf` |
| 研究组件 | 模板插槽内的可复用计算单元 | 动量、ADX、协整、行业中性化 |
| `StrategySpec` | 某个策略版本的完整不可变配方 | 数据、标签、模型、组合、执行和风险规则的冻结版本 |
| `ModelArtifact` | 按某个 `StrategySpec` 在一次实际训练窗口上拟合出的模型制品 | 模型文件、预测哈希、训练起止日期和数据截止时间 |
| `AccountAllocationPolicy` | 在账户层对已启用策略只分配一次资本预算 | 固定预算、等资金、核心—卫星、跨策略逆波动/风险均衡 |
| `AllocationArtifact` | 按冻结账户政策在某次预注册决策日产生的预算制品 | 输入截止时间、估计窗口、成员资格、预算、有效期和回退状态 |
| `ExecutionPolicy` | 把唯一最终账户目标转成模拟订单或人工执行计划 | 下一 Bar、TWAP、VWAP、参与率限制、跨日切片 |

三类对象的写权限固定：

- `StrategySpec` 只能在分配给自己的资本预算内产生本策略的证券目标和解释，不能修改其他策略预算，不能直接创建账户订单。
- `AccountAllocationPolicy` 只能按冻结日历读取合格成员策略及账户约束并生成 `AllocationArtifact`；账户合并时该预算只能应用一次。政策不能持有证券、不能作为成员策略再次获得预算，也不进入 `recommendation_enabled` 生命周期。
- `ExecutionPolicy` 只能在所有策略目标净额合并并通过账户硬约束后生成订单计划，不能反向产生 Alpha、修改策略预算或改变最终账户目标。

每个 `StrategySpec` 至少冻结：

- `economic_hypothesis_id` 与 `economic_hypothesis_group`、`strategy_family`、`engine_type`、`template_id` 和版本。
- 经济假设、预期收益来源、可能失效机制和允许的资产/账户权限。
- 数据源、字段、频率、PIT/最早可用时间、证券池、去重规则和数据质量门。
- 特征/组件版本、标签、输出语义、决策/调仓/持有/执行频率和信号有效期。
- 模型类型、参数或参数搜索规则、训练窗口构造、例行 refit 日历、随机性处理和预测后处理。
- 模板插槽、预算内组合构建、退出、现金、容量、成本、风险、兼容执行要求和不可交易时的降级规则。
- 简单可投资基线、验证切分、准入指标、失败条件、压力场景和个人交付资格。

策略输出必须声明是 `rank_score`、`expected_return`、`event_probability`、`target_weight` 还是离散规则状态。`rank_score` 没有收益单位，不能直接与人民币成本比较或冒充预期收益；`expected_return` 必须说明期限、收益口径并经样本外校准；`event_probability` 必须经样本外概率校准。信号有效期和允许的最大人工响应延迟由衰减实验决定，不能凭经验写死。

### 6.2 完整研究组件库

| 组件组 | 纳入的组件 | 正确使用方式 |
| --- | --- | --- |
| 趋势与突破 | 双均线、长短均线结构、N 日/箱体突破、ADX/+DI/-DI、MACD、量价确认 | 区分方向、强度与入场；ADX 不是方向，量能使用时必须满足当时已完整可知 |
| 均值回归与择时 | 布林带位置/带宽、RSI、乖离率、价格或收益 Z-score、VWAP 偏离 | 可作独立均值回归逻辑或趋势策略的入场时机；必须测试单边趋势尾部损失 |
| 股票横截面因子 | 中长期动量、短期反转、价值（PE/PB、EP/BP、股息率）、质量（ROE/ROA、盈利与现金流）、成长、低波动、流动性、杠杆、应计/盈利质量 | 财务字段按公告时间 PIT；负分母和异常会计值单独处理；收益型因子与资格过滤分开；因子方向和期限明确 |
| 资格与基础过滤 | 历史 ST/退市风险、停复牌、上市天数、成交额/换手、价格、涨跌停、质押、商誉、财务可用性 | 只能使用当时可知状态；过滤器属于硬门，不能被高分覆盖 |
| 因子预处理与合成 | 缺失处理、去极值、方向统一、截面标准化、行业/规模/Beta 中性化、等权/经验权重/模型合成 | 所有拟合参数只在训练窗估计；计划获得的价值、成长或低波动暴露不能又被全部中性化为零 |
| ETF 选择与配置 | 资产类别、跟踪指数、规模、成交额、跟踪误差、折溢价、流动性、动量、趋势、波动率、相关性、底层重合 | 先做产品与交易规则白名单及同类去重，再进行评分或配置 |
| 配对与相对价值 | 相关性初筛、Engle-Granger/其他协整检验、滚动回归、Kalman 动态对冲比率、价格/对数价差、价差 Z-score | 配对选择和参数估计只能在形成期完成；相关性不等于协整，关系需在未见区间验证 |
| 机器学习 | 线性/正则化线性、随机森林、GBDT/XGBoost/LightGBM、时序模型/LSTM、模型集成 | 作为已有模板的排序、收益或事件概率增强；必须有简单模型/原规则基线、消融和校准 |
| 轮动与事件研究 | 行业动量、行业资金变化、财务公告、业绩预告、回购、减持等公司事件 | 只有事件发生时间、公开时间、预期差和历史行业分类均可 PIT 重建时才能从研究态升级 |
| 策略内组合与仓位 | 现金、等权、分数权重、Top-N、ETF 策略内波动率目标/逆波动/风险平价、基准相对优化 | 只在该 `StrategySpec` 的资产与已分配预算内工作；跨策略核心—卫星和风险均衡属于 `AccountAllocationPolicy` |
| 风险与退出 | 单票/行业/资产/策略上限、流动性、容量、换手、跟踪误差、ATR/波动止损、跟踪止盈、趋势破坏、回撤处置 | 固定比例只作候选参数；止损、止盈和波动率缩放不能承诺最大损失 |
| 执行政策库 | 下一 Bar、TWAP、VWAP、参与率限制、限价/等待/取消、跨日拆分 | 它不是研究组件或 `StrategySpec`；只在 6.5 节以 `ExecutionPolicy` 实现最终账户目标 |

每个组件保存 `component_id`、`component_version`、输入/输出单位、频率、窗口、最早可用时间、参数域、缺失行为、状态依赖、测试和数据血缘。同名“日线 RSI”“1min RSI”“5min RSI”是三个不同组件版本，不能共享参数、样本外证据或默认成本。原文中的固定均线、ADX 阈值、止损比例、因子权重、股票/ETF 对和收益目标都只能成为候选配方，不能成为系统常数。

### 6.3 模板插槽与固定控制顺序

模板统一使用以下插槽：

1. `eligibility_gate`：账户权限、证券可交易性、流动性和数据质量硬门。
2. `universe_dedup`：ETF 同类产品或高度重合暴露去重；不需要时显式为空。
3. `direction_regime_gate`：方向或市场环境门；可选。
4. `alpha_rank`：规则评分、横截面排序、预期收益或事件概率。
5. `entry_timing`：立即进入、等待更合适窗口或放弃。
6. `exit_state`：持有期结束、逻辑失效、风险退出或继续持有。
7. `portfolio_risk`：现金、权重、集中度、容量、换手和风险约束。
8. `execution_requirement`：正式回测采用的执行假设，以及生产可兼容的下一 Bar、TWAP/VWAP、参与率、订单有效期和跨日要求。

固定控制顺序为：

`资格 → 去重 → 方向/环境 → 排名/预测 → 入场时机 → 退出状态 → 策略内组合风险 → 执行要求`

每个模板必须声明必填槽、可选槽和空槽行为。RD-Agent 可以在授权范围内绑定组件、研究参数或提出插槽扩展草案，但不能重排硬门，不能用高 Alpha 分数覆盖权限、不可交易、现金、容量或风险门。模板的执行要求用于历史证据和兼容性检查；生产中只有账户净额合并后的 `ExecutionPolicy` 可以创建订单计划。

### 6.4 完整策略模板与配方目录

以下目录完整保留两份 Word 和扩展 Markdown 中适合个人股票/ETF系统的策略思想，不再采用“只做一个示例策略”的最小目录。为了避免“素材全保留”变成“所有策略同时阻塞个人主线”，每项同时声明：

- `catalog_role`：`baseline`（基线）、`alpha_template`（独立经济假设）、`recipe_variant`（父模板配方）、`research_only`（只能离线研究）、`model_challenger`（依附父模板）或 `research_admission`（新假设入口，不是可运行兜底模板）。
- `implementation_tier`：`foundation`（公共合同与可持续基线）、`standard`（当前股票/ETF系统的完整实现目录，可分阶段完成）或 `conditional`（先实现结构化研究/阻断，只有数据和权限满足后才实现或启用）。

`standard` 和 `conditional` 不要求同时进入 `recommendation_enabled`，也不要求取得正收益；但它们不能从设计中消失。`AccountAllocationPolicy` 和 `ExecutionPolicy` 分别在 6.10 和 6.5 节管理，不作为成员策略。

#### 6.4.1 基线、资产配置与股票横截面

| `template_id / recipe_id` | 角色/层级 | 目标与典型频率 | 简单基线 | 关键合同与状态 |
| --- | --- | --- | --- | --- |
| `simple_policy_baseline` | `baseline/foundation` | 现金/可获得现金收益、货币工具、宽基 ETF 持有或冻结 ETF/现金政策；月度或静态 | 自身就是最低复杂度可投资基线 | 必须长期可运行；主动策略失败时允许回到该策略，不承诺收益 |
| `etf_asset_allocation` | `alpha_template/standard` | 白名单 ETF 与现金的政策或主动配置；通常月度决策、日度风险检查 | 冻结的简单 ETF/现金政策、同资产等权 | 先做资格与同类去重，再用趋势、动量、跟踪质量、折溢价、波动率和相关性；防底层重合、清盘、底层休市和子类型规则误用 |
| `etf_rotation` | `alpha_template/standard` | 在不同资产类别或指数 ETF 间作周/月度低换手轮动 | 同资产等权或静态政策配置 | 轮动排名、风险门和现金规则分开；必须报告危机期相关性上升、换手和追涨风险 |
| `etf_risk_balanced_recipe` | `recipe_variant/standard` | 仅在某个 ETF 配置策略自身资产池与预算内使用逆波动、目标波动、风险平价/ERC 或受限权重；周/月度 | 同资产等资金、静态政策和逆波动 | 不分配其他策略预算；期限与协方差一致、解可行、离散后复检，失败回退；普通现金账户不得为追逐目标波动自动加杠杆 |
| `personal_stock_core` | `alpha_template/standard` | PIT 可交易 A 股的低复杂度个人股票核心；月度或低换手周度 | 宽基总收益与同约束等权股票组合 | 持仓数由 NAV、整手、最低佣金、集中度和人工能力共同决定；不能把机构化指数增强简单截成固定前 N 只 |
| `single_factor_stock/*` | `alpha_template/standard`，同时作为基线 | `momentum`、`short_reversal`、`value`、`quality`、`growth`、`low_vol`、`liquidity` 单因子多头；月/季度 | 同 PIT 股票池等权、对应指数 | 可运行时走完整策略生命周期，同时作为多因子消融基线；负盈利、财务公告时点、行业/规模暴露和换手分别处理 |
| `simple_multifactor_stock` | `alpha_template/standard` | 少量可解释因子的低换手股票组合；月/季度 | 股票池等权、各单因子、简单等权因子组合 | 原子指标先独立预处理再合成；固定六因子权重只作候选，必须报告增量、冗余、暴露和成本 |
| `index_enhancement` | `alpha_template/standard` | 对某个 PIT 指数成分和权重做基准相对增强；周/月度 | 总收益指数、基准复制组合和同约束简单增强 | 标签为持有期基准相对收益；关注主动收益、IR、Tracking Error、暴露、换手和容量；原始 z-score 不得直接冒充预期超额收益 |
| `full_market_multifactor` | `alpha_template/standard` | PIT 全 A 可交易池的多因子多头；周/月度 | 宽基总收益、同约束等权和简单低换手因子组合 | 控制非预期行业、规模、Beta、流动性暴露；防小盘偏置、拥挤、数据挖掘和不可交易收益 |
| `industry_neutral_multifactor` | `recipe_variant/standard` | 在全市场多因子上增加明确行业相对目标；周/月度 | 无行业中性版本及同约束行业内等权 | 是 `full_market_multifactor` 的严格配方变体，共享组件但独立 `StrategySpec`；中性化不能抹掉计划 Alpha |

#### 6.4.2 趋势、突破、均值回归、周频与事件

| `template_id` | 角色/层级 | 目标与典型频率 | 简单基线 | 关键合同与状态 |
| --- | --- | --- | --- | --- |
| `trend_ma` | `alpha_template/standard` | 双均线或多均线结构捕捉中期趋势；日线决策、持有数日至数周 | 买入持有、单长均线规则 | 防震荡反复交易、参数敏感和长回撤；分钟数据只能辅助执行，不能偷换日线证据 |
| `trend_breakout` | `alpha_template/standard` | N 日高点/箱体突破后顺势，按冻结规则退出；日线或小时形成、下一 Bar 执行 | 最简单 N 日高点入/N 日低点出或买入持有 | 防假突破、跳空、涨停买不到和搜索过拟合；N 与退出规则都是候选 |
| `swing_trend` | `alpha_template/standard` | 趋势方向 + ADX/波动强度 + 量能 + 可选质量门的复合波段；日线形成、数日至数周 | `trend_ma` 或简单冻结趋势规则 | 方向、强度、入场、退出必须分槽；RSI/布林带/乖离率只作时机，不得把多条件堆叠当成天然更优 |
| `daily_mean_reversion` | `alpha_template/standard` | 布林带、RSI、乖离率或收益 Z-score 的日线回归；持有期由标签决定 | 简单均线偏离/Z-score 规则或不交易 | 必须测试单边趋势、跳空、连续跌停和越跌越买；A 股买入后退出遵守 T+1 |
| `weekly_tactical_overlay` | `alpha_template/standard` | 具有明确经济假设的周频策略只输出自己预算内的证券目标或增量，不调整其他策略预算 | 无周频 Alpha、沿用核心目标 | 每个配方仍需独立假设、标签、基线和 `StrategySpec`；周报不等于周频策略，不能把日线信号或账户预算政策改名 |
| `industry_rotation` | `alpha_template/conditional` | 用 PIT 行业分类、行业动量/基本面/资金数据做周/月度轮动 | 行业等权、宽基或静态行业权重 | 历史分类、资金口径和发布时间不能重建时停在 `research`，禁止正式评估 |
| `event_driven_swing` | `alpha_template/conditional` | 财报、业绩预告、回购、减持等事件后的有限持有期波段 | 无事件策略或简单市场/行业基线 | 冻结事件公开时间、预期差、重复事件合并、可交易时点和事件概率语义；没有 PIT 事件库时停在 `research` |

`trend_ma`、`trend_breakout` 和 `swing_trend` 共用趋势组件与执行链，但保留不同配方；`daily_mean_reversion` 不得仅作为趋势策略的一个参数开关而隐藏其相反的尾部风险。

#### 6.4.3 Long-only 相对配置、分钟模板与配对离线研究

| `template_id / admission_id` | 角色/层级 | 目标与典型频率 | 简单基线 | 关键合同与状态 |
| --- | --- | --- | --- | --- |
| `etf_long_long_relative_allocation` | `alpha_template/standard` | 在高流动性、经济关系明确的 ETF 间做 long-only 相对权重/轮动；日线形成、下一 Bar/分时执行 | 两资产等权、静态持有或不交易 | 形成期内选关系和估参；使用普通 long-only 证券目标、账户净额和订单链，不要求原子双腿；不能宣称市场中性或完整价差套利 |
| `stock_pair_stat_arb` | `research_only/conditional` | 研究股票对相关、协整、滚动/Kalman 对冲比率和价差回归 | 不交易、滚动 OLS/对数价差 Z-score | 当前设计永久只做离线统计研究和理想情景上界，不创建正式 `candidate`、持久 `forward_paper` 或账户推荐；若未来要模拟/推荐，必须另立含卖空头寸、保证金、受限回款、借券负债/费用、召回和强平的专项设计 |
| `minute_mean_reversion_etf` | `alpha_template/conditional` | 对明确允许 T+0 且规则完整的 ETF 做 1min/5min 完整 Bar 后回归 | 不交易或只执行批准的低频目标 | 最早下一 Bar 执行；独立冻结分钟数据、标签、延迟、成本、容量、资本和 `forward_paper` |
| `stock_intraday_entry_overnight_exit` | `alpha_template/conditional` | 用股票分钟信息择时买入，最早在下一可卖交易日退出 | 日线入场或不交易 | 对应原材料中的“股票分钟均值回归”，但标签必须跨到真实可卖时点；不得假设 A 股当日买入数分钟后卖出，也不得复用 ETF 分钟证据 |
| `independent_minute_alpha` | `research_admission/conditional` | 目录外分钟经济假设的受控入口，不是可长期运行的万能模板 | 不交易与低频目标执行策略 | 每一种新分钟逻辑仍须走 `NewStrategyProposal`，取得明确的新 `template_id` 和独立证据后才能成为 `candidate`；不能充当分钟版 `controlled_custom` |

机器学习条目是父模板的模型挑战者，不是独立策略模板：

| `challenger_id` | 角色/层级 | 依附对象 | 简单基线与关键合同 |
| --- | --- | --- | --- |
| `ml_cross_sectional_enhancement` | `model_challenger/standard` | `personal_stock_core`、`index_enhancement` 或多因子模板 | 与父规则和简单线性模型比较；检查泄漏、消融、分组单调性、校准、复杂度和漂移 |
| `ml_trend_enhancement` | `model_challenger/standard` | 均线、突破或 Swing 父模板 | 与父趋势规则及简单线性/逻辑模型比较；证明同路径、同成本下的样本外增量 |
| `ml_ensemble_challenger` | `model_challenger/standard` | 已冻结的同一父模板模型集合 | 权重只在训练/验证窗学习；不得看最终样本外后挑模型，失败时回退简单模型 |
| `ml_event_probability` | `model_challenger/conditional` | 已先通过 PIT 数据门的事件父模板 | 与事件频率基线和简单逻辑模型比较；概率必须样本外校准，不能用 LLM 记忆补历史标签 |

模型挑战者继承父 `StrategySpec` 的资产、标签、频率、成本、资本预算和生命周期，不创建独立策略账户或另取一份资本。若模型改变资产、标签、持有期、交易逻辑或收益来源，则不再是挑战者，必须走 `NewStrategyProposal` 创建新模板；同一配方按日历重训只生成新的 `ModelArtifact`。

#### 6.4.4 目录覆盖与启用规则

- `foundation` 必须先提供可机器读取的公共合同、字段校验和可持续简单基线。`standard` 是完整策略实现目录，但可按 12.6 节分阶段完成，不阻塞已经正确运行的个人主线。
- 任何可能获得账户资本并产生证券目标的 `alpha_template`、已部署 `recipe_variant` 或可投资 `baseline` 都属于 `capital_eligible_strategy`，必须有完整 `StrategySpec`、适合自身目标的简单基线/准入门、Qlib 适配、`candidate → paper` 生命周期和“不支持时失败关闭”状态。`recipe_variant` 不能借父模板名义绕过新 `StrategySpec`；可投资基线不要求正 Alpha，但仍要通过交易规则、成本、风险、账本和前向运行门。
- `conditional`、`research_only`、`model_challenger` 和 `research_admission` 至少必须能保存 `ResearchBrief`、实验、父模板或阻断原因；它们不自动获得独立账户、资本或生命周期。缺少 PIT 事件、分钟权限或 ETF 规则时显示 `blocked_by_data_or_permission`，不能用当前信息或理想成交补齐。
- `stock_pair_stat_arb` 是明确例外：当前只承诺离线统计研究，不因补到借券数据就自动取得双腿模拟或推荐资格。
- 目录之外的新想法不需要重写整个平台：先走 6.6 节的 `new_strategy_proposal`，通过模板设计评审后创建新的 `template_id` 主版本。
- `controlled_custom` 只允许承载尚未评审的新策略草案，不是绕过字段、成本、验证和晋升门的万能类型；草案进入 `candidate` 前必须取得有明确名称和合同的新 `template_id`。
- 期货/CTA、期限结构、期权波动率套利、裸空、跨市场/加密套利、Tick/Level-2 高频、做市、低延迟和自动实盘交易不属于当前股票/ETF个人系统；RD-Agent 必须在可研究资产和权限白名单处阻止它们进入正式链。

### 6.5 独立 `ExecutionPolicy` 目录

`ExecutionPolicy` 不是 Alpha 策略，不获得策略资本或独立账户，不能用 Sharpe、IC 或 Alpha 证明自己有效。

| `execution_policy_id` | 用途 | 主要评价 |
| --- | --- | --- |
| `next_bar_baseline` | 完整信号后在下一可交易 Bar 按统一规则成交，是所有复杂执行的基线 | 实施差额、成交率、不可成交原因 |
| `twap_execution` | 在允许窗口内按时间切片释放低频目标 | 相对下一 Bar 的实施差额、完成率、延迟、未成交率 |
| `vwap_execution` | 按仅由当时可得历史信息估计的量能曲线切片 | 实施差额、预测量能偏差、参与率和容量 |
| `participation_capped_slicing` | 按成交量参与率、最小订单和流动性限制切片 | 成交率、冲击、参与率、剩余目标 |
| `wait_cancel_replace` | 1min/5min 检查低频目标，决定等待、继续、取消或替换 | 响应延迟、过期率、重复订单为零 |
| `multi_day_transition` | 大额或流动性受限目标跨日完成 | 跨日风险、机会成本、完成率和现金占用 |

执行策略只能在上层最终账户目标和硬风险边界内工作。它不得自行增加目标仓位、把卖出改成买入、用盘中模型绕过 `recommendation_enabled`，也不得把未成交目标当成已成交持仓。

每个最终订单或明确的执行组必须恰好绑定一个 `execution_policy_id`；一份账户执行计划可以按冻结的证券/流动性路由包含多个兼容政策，例如部分订单下一 Bar、部分订单 VWAP。路由只能在证券目标净额合并和硬约束之后发生，不能按策略来源拆回相反订单；无兼容政策时回到 `next_bar_baseline` 或阻断。`ExecutionPolicy` 或路由规则的实质变更会改变历史/前向证据配置，必须版本化并按 9.5 节新建相应模拟阶段。

### 6.6 RD-Agent 自主研究新策略

#### 6.6.1 研究模式与触发

| `research_mode` | RD-Agent 可以做什么 | 产物 |
| --- | --- | --- |
| `template_extension` | 在现有模板插槽内研究新组件、组合、参数或规则 | 新 `StrategySpec` 候选 |
| `component_discovery` | 生成、实现和筛选新因子/技术/统计组件 | 版本化组件、单组件增量与失败记录 |
| `model_challenger` | 为父模板研究线性、树、时序或集成模型 | 模型候选、消融和校准报告 |
| `portfolio_execution_challenger` | 研究账户分配或执行政策 | `AccountAllocationPolicy` / `ExecutionPolicy` 候选及同目标简单政策比较；不生成成员策略或独立资本 |
| `new_strategy_proposal` | 自主提出目录外的新经济假设、策略家族或模板草案 | `NewStrategyProposal`，不能直接成为正式策略 |
| `falsification_or_retirement` | 主动寻找既有策略失效、泄漏、成本不足或市场结构变化的证据 | 反证报告、暂停/退役建议或新研究任务 |

调度器可按周、月、人工研究简报、策略漂移、成本偏差或组合暴露缺口触发研究。日常推荐不得依赖 RD-Agent 当次成功：RD-Agent 离线、失败或预算耗尽时，冻结的 Qlib 推理、模拟账本和提醒继续运行。

#### 6.6.2 每轮先冻结 `ResearchBrief`

`ResearchBrief` 至少包含：

- 研究问题、经济缺口、允许的策略家族/资产/频率/数据和明确禁止项。
- 用户权限、个人可执行数量、资本上限和计划交付状态：`research_only`、`simulation_only` 或“可申请正式评审”。
- 数据快照、PIT 规则、标签与输出语义、训练/验证/外层 walk-forward/最终样本外边界。
- 简单可投资基线、主要指标、失败条件、完整成本、容量、风险和压力场景。
- 最大候选/公式/参数/模型/随机种子数、运行时间、并发、CPU/内存、LLM 费用和停止条件。
- LLM 提供商、模型、可获得的知识截止信息、是否联网、允许的资料和最终样本外不可见规则。

预算达到后停止并保留失败结果，不能自动扩大预算。修改研究问题、标签、基线、主要指标或试验预算属于新研究活动，不能覆盖原台账。

#### 6.6.3 `NewStrategyProposal` 的必备内容

RD-Agent 可以真正“自己研究新策略”，而不只是在现有模板上调参数。每个新策略草案至少输出：

1. 经济假设、行为或风险补偿解释、与现有模板的差异及预期失效机制。
2. `strategy_family`、`engine_type`、建议的 `template_id`、资产池和所需账户/市场权限。
3. 数据字段、PIT 可获得性、样本覆盖、标签、输出语义、决策/调仓/持有/执行频率。
4. 八个模板插槽的组件绑定、组合、退出、现金、风险和执行规则。
5. 一个容易理解且走同一证券池、时间、成本和执行路径的简单基线。
6. 成本、容量、流动性、停牌/涨跌停/T+0/T+1、公司行动和主要尾部风险。
7. 负对照、泄漏哨兵、消融、参数邻域、市场状态和分项成本压力测试。
8. Qlib Dataset/Expression/Model/Strategy/Workflow/Recorder 与项目 `ExecutionCore` 的适配计划。
9. 代码、测试、依赖、来源、LLM 历史知识污染风险、试验预算和明确失败条件。
10. 预期交付级别：仅研究、仅隔离模拟，或完成正式评审后才可能参加推荐。

新策略草案不能因为探索回测漂亮而自动创建正式模板。用户先确认其经济逻辑、合法交易路径、标签、基线、成本和风险，系统才创建新的模板主版本并进入正常候选链。

#### 6.6.4 自动研究闭环

已有模板扩展走：

`ResearchBrief → 假设/组件/模型生成 → 数据与可交易性审计 → 沙箱原型 → Qlib 探索训练和回测 → 全部原子试验入账 → 负对照/泄漏/消融/稳健性反证 → 冻结 StrategySpec → candidate → 独立复算 → 正式 Qlib 回测与硬门 → 自动进入隔离 paper → 前向证据门 → 人工批准 recommendation_enabled`

目录外新策略先走：

`ResearchBrief → NewStrategyProposal → 人工模板设计确认 → 命名 template_id 与研究态 StrategySpec → 上述探索、冻结 candidate、独立复算、正式回测和 paper 链`

账户分配/执行挑战者冻结为各自的政策候选并与简单政策成对比较，不进入策略 `candidate/paper/recommendation_enabled` 生命周期；只有被明确启用的账户政策版本才能在生产链工作一次。

具体规则：

1. RD-Agent 生成的代码只在隔离研究环境运行，不得读取 Tushare Token、个人账户、生产数据库写权限或宿主机敏感目录。
2. 每个被评分的公式、参数、组件组合、模型和随机种子都进入统一 `ExperimentLedger`；失败、超时和人工放弃不能删除。
3. 探索阶段执行 PIT/最早可用时间、单位、频率、标签、下一 Bar、信号平移、负对照和未来字段泄漏测试。
4. 外层 walk-forward 的每个 Fold 重新做内层选择；最终样本外对 RD-Agent 物理不可见并遵守 4.1 节全局一次性消费规则。
   首轮 A 股/ETF 日频主线固定以 `2008-01-01` 为历史目标起点，默认训练 2008—2017、验证 2018—2020、最终锁箱 2021 至最近完整交易日，正式证据覆盖不得少于十个自然年。晚于该日期才出现的数据源、证券和制度按真实起点标记缺失，禁止回填；分钟数据只承担真实可用年份内的成交、成本和容量验证。
5. 候选形成后冻结代码、数据、组件、参数、`candidate_formation_ts` 和 `StrategySpec`，随后才进入 `candidate`；新家族必须先完成人工模板设计确认，不能让 `NewStrategyProposal` 直接进入独立复算。
6. 独立任务从锁定快照重新计算组件、训练模型和生成正式预测，不能读取 RD-Agent 导出的因子值、预测文件或文字结论；同时生成单组件增量、消融和失败对照。
7. 正式 Qlib 结果通过硬门后自动创建独立 `paper` 账户，不需要人为挑选探索赢家；`paper` 达到预注册前向证据门后，用户才决定是否进入 `recommendation_enabled`。
8. RD-Agent 永远不能修改当前运行版本、最终样本外、成本、风险硬门、个人约束或模拟账本，也不能在日常/盘中任务中临时生成代码后直接给账户建议。
9. 漂移、成本偏差或反证产生下一轮研究任务；新版本合格前继续使用仍有效的旧版本，旧版本失效则回到简单基线或现金。

### 6.7 Qlib 探索、独立复算与正式回测

每个候选至少保存：

- Qlib Dataset 与数据快照版本、证券池和 PIT 语义。
- 特征/组件、标签、模型、策略、参数、随机种子和 `StrategySpec`。
- 训练、验证、外层 walk-forward 和最终样本外边界。
- 预测文件、Recorder 标识、回测配置、完整结果和失败日志。
- 代码/依赖、成本、执行、组合、风险和数据规则版本。

研究阶段可以使用 Qlib 快速迭代。候选冻结后，独立任务必须从冻结输入重新生成特征和预测，再使用正式 Qlib Workflow/Recorder 复算；正式结果不能采用 RD-Agent 探索阶段挑出的预测或文字总结。

正式 Qlib 工作流只能使用调用 `ExecutionCore` 的自定义 Executor/Exchange 适配器。Qlib 默认 Executor/Exchange 可以作快速探索和兼容对照，但不能形成正式准入或模拟晋升证据。

`StrategySpec` 和 `ModelArtifact` 分别版本化。按冻结 `StrategySpec` 自然滚动训练起止日期是例行 refit；改变数据/PIT、证券池、特征、标签、模型类型、参数搜索规则、训练窗口构造或长度、样本筛选、refit 日历、预测后处理、组合或执行规则，则创建新的 `StrategySpec`。

### 6.8 反过拟合、增量证据与信号有效期

- 每个正式候选与同路径简单基线比较，并提供单组件增量和“移除该组件”的消融；复杂 ML 还要与简单线性模型和父规则模板比较。
- 负对照至少覆盖随机/打乱标签、无经济含义特征或等价的策略专属对照；泄漏哨兵覆盖未来字段、标签平移、同 Bar 成交和当前状态回填。
- 报告参数邻域、时间子区间、市场状态、成本分项、容量和延迟压力；不能只展示最佳参数点。
- 多个近似版本、频率或模型共享同一 `economic_hypothesis_group`，统一进入试验计数和资本上限；改名不能获得新最终样本外或多份独立资金。
- IC/RankIC、命中率、回归系数和漂亮曲线只是诊断；投资结论必须来自相同账户路径、成本和独立时间证据。
- 信号在不同延迟下重新执行，得到衰减曲线；提醒中的有效期和最大人工响应延迟取自预注册的可接受衰减边界。证据不足时只显示“观察/已过期”，不输出伪精确分时点。

### 6.9 分策略准入目标

不同策略不能共用一张万能 Sharpe 阈值表。具体阈值在研究前按数据量、风险和个人目标冻结，但至少评价：

| 策略类型 | 主要目标与诊断 |
| --- | --- |
| 简单政策/ETF 配置/风险均衡 | 与同资产政策基线比较的净收益和下行风险、回撤、换手、相关性漂移、离散可执行性；风险平价只验收风险配置和净结果，不冒充 Alpha |
| 单因子/多因子/股票核心 | 同 PIT 证券池的净组合表现、IC/RankIC 稳定性、分组单调性、行业/规模/Beta 暴露、换手、容量和小账户整手失真 |
| 指数增强 | 主动收益、Information Ratio、Tracking Error、行业/风格暴露、换手、容量及相对基准的尾部偏离 |
| 趋势/突破/Swing | 成本后收益、回撤和尾部损失、震荡期损耗、交易分布、参数邻域与跨状态稳定性 |
| 均值回归 | 趋势状态尾部损失、持有期和回归速度稳定性、换手、跳空/涨跌停、完整成本与容量 |
| ETF long-only 相对配置 | 与静态/等权配置比较的成本后表现、权重集中、底层重合、换手、容量和关系漂移；不得使用市场中性指标包装 |
| 股票配对离线研究 | 形成期外关系稳定性、价差性质、对冲比率漂移和结构断裂压力；结果只作研究诊断，不设投资晋升门 |
| 分钟 Alpha | 下一 Bar 和真实可卖时点、延迟衰减、实施差额、成交/未成交率、参与率、容量、日内与隔夜风险 |
| ML 增强 | 对父模板和简单模型的独立增量、消融、校准、稳定性、复杂度、漂移与可复算性 |
| 执行策略 | Implementation Shortfall、完成率、未成交率、参与率、延迟和容量；不得用策略 Sharpe、Alpha 或 IC 验收 |

### 6.10 `AccountAllocationPolicy`、多策略资本与净额

`AccountAllocationPolicy` 是独立的账户级不可变政策版本。它读取已启用策略的证据状态、风险估计、当前账户和用户约束，只输出每个成员策略的资本预算；它不能输出证券目标、进入策略生命周期或再作为成员获得一份预算。

每个政策必须冻结成员资格、输入截止语义、风险/相关性估计方法、估计窗口构造与长度、`decision_frequency`、`rebalance_frequency`、生效日历、`valid_until`、上限、现金/缺失回退、优化器和失败规则。只有到达冻结决策日才计算新的 `AllocationArtifact`；其他推荐或分钟任务复用仍有效制品并只检查成员状态和硬上限，不能临时重估风险。

每个 `AllocationArtifact` 保存政策版本、输入 `as_of`、实际估计窗口、合格成员、原始/约束后预算、求解状态、生效时间、`valid_until`、回退原因和哈希。按照同一政策和预注册日历自然产生的新制品属于例行账户再分配，不是新政策版本。

| `allocation_policy_id` | 用途 | 关键约束 |
| --- | --- | --- |
| `fixed_budget_policy` | 用户给每个策略固定上限或等资金分配 | 总预算不超过可投资资本，剩余为现金 |
| `core_satellite_account_policy` | 核心承载主要预算，卫星只使用受限主动预算 | 核心/卫星定义、上限和再平衡规则版本化；不是平台唯一组合形式 |
| `account_inverse_volatility_policy` | 按成员策略风险做跨策略逆波动分配 | 风险期限一致、上限生效、估计失效时回退固定预算 |
| `account_risk_balanced_policy` | 可选的跨策略目标波动或 ERC/风险预算 | 只有实际启用时才求解；失败关闭且普通现金账户不得自动加杠杆 |
| `cash_fallback_policy` | 成员不足、暂停或证据过期时把未分配预算转为现金/简单基线 | 不能由 RD-Agent 临时填入未经批准候选 |

`etf_risk_balanced_recipe` 只在单个 ETF `StrategySpec` 的资产池和已分配预算内部配置；`account_risk_balanced_policy` 只在成员策略之间分配预算。两者即使都启用也分别只执行一次，并在制品中保存 `allocation_scope=within_strategy_assets|across_strategy_members`，不得重复缩放同一层资本。

- 策略资本预算、最大资本上限、目标波动和风险预算是不同概念，分别保存；不启用风险预算求解器时不得伪造风险贡献结果。
- 同一时刻只有一个已由用户选择的 `active_account_allocation_policy_id`。新政策先作离线/隔离挑战，不能由 RD-Agent 自动启用；政策规则/频率/窗口/成员资格/人工预算配置实质变化属于 9.5 节前向配置变化，必须新建模拟阶段。按冻结日历产生的例行 `AllocationArtifact` 不重置阶段。
- 同一 `economic_hypothesis_group` 的均线、突破、不同频率或不同 ML 包装共享一个资本上限；相关性和暴露报告用于人工审查，不建设机构级自动克隆检测平台。
- 固定顺序为：`按冻结日历生成或读取 AllocationArtifact → 将其中预算应用一次 → 各 StrategySpec 在预算内输出证券目标 → 证券级净额合并 → 账户硬约束与 no-trade band → ExecutionPolicy`。
- 相反需求先净额抵消；不得让两个策略分别产生相反真实订单来制造换手，也不得把净额后的同一成交和费用在多个策略重复计入。
- 某个策略被暂停、证据过期或数据不可用时，其预算按冻结政策转给现金/简单基线或重新归一，不得由 RD-Agent 临时填补。

### 6.11 简化生命周期

主晋升路径为 `research → candidate → paper → recommendation_enabled`；其中 `candidate → paper` 仅在独立复算和正式硬门通过后自动发生，`paper → recommendation_enabled` 必须同时满足前向证据门和人工批准。任一受控阶段可以按规则进入 `rejected`、`paused` 或 `retired`。

- `research`：允许修改，仅作探索。
- `candidate`：版本冻结，等待独立复算、正式回测和验证。
- `paper`：正式硬门通过后自动创建独立隔离模拟账户，允许展示该实验账户的模拟数量，但不参与用户综合推荐，也不输出个人主账户数量建议。
- `recommendation_enabled`：允许参与唯一账户综合建议和主模拟账户；仍不具备实盘交易权限。
- `rejected`：证据不足或不合格，保留全部记录。
- `paused`：数据、风险、漂移或运行异常，停止新增风险。
- `retired`：不再恢复，只能以新 `StrategySpec` 重新研究。

个人系统只保留两个不可自动越过的人工作业点：目录外新策略从 `NewStrategyProposal` 变成正式命名模板，以及合格 `paper` 进入 `recommendation_enabled`。新 `StrategySpec` 和每次 `ModelArtifact` 都不覆盖旧版本；例行 refit 的模型制品切换按下一节的预批准安全门处理，不冒充新策略晋升。

`paper → recommendation_enabled` 不能只靠人工点击。每个策略在进入 `paper` 前必须按自身频率预注册最少自然时间、独立决策/成交事件、完整持有或再平衡周期、数据完整率、账本对账率、允许的成本偏差，以及确有必要的策略专属市场状态硬门；任何硬门不足都继续留在隔离模拟。固定 60 日只能是某个策略配置，不是跨策略通用数学门。

### 6.12 例行 refit、漂移与回滚

- 按已批准 `StrategySpec` 的冻结日历例行 refit，只生成新的 `ModelArtifact`，不视为新策略候选，也不重新累计长期前向晋升证据。切换前通过预先固定的轻量安全门：输入数据、代码和时间边界正确，训练成功，预测可复算或在容差内一致，输出有限且分布无异常，账户和风险约束可执行。
- 每个 `ModelArtifact` 保存所属 `StrategySpec`、实际训练起止日期、数据截止时间、数据快照、代码/依赖版本、随机种子、模型/预测哈希和切换时间。
- 监控输入缺失率、特征/预测分布、模型误差、成本偏差、回撤和相对基线表现。数据或实现错误立即暂停；统计或经济表现恶化时停止新增风险并进入复核。
- refit 失败时继续使用仍有效的旧制品；旧制品也失效时回到简单基线或现金。新 `StrategySpec` 未通过正式回测、隔离前向模拟和人工批准前，不得自动替换旧配方。
- 瞬时下载或运行故障在数据和账本复核通过后可以恢复同一不可变版本；修复数据、代码或规则时必须生成相应新制品/配方。历史实现或数据错误使受影响的 Recorder、回测证据和前向阶段全部失效并重建，不能只解除暂停。
- 回滚只切换到同一有效 `StrategySpec` 下仍满足数据、规则和风险条件的历史 `ModelArtifact`，不能恢复已经失效或退役的配方。

## 7. 正式回测与投资证据

### 7.1 正式回测输入

正式 Qlib 回测必须冻结：

- 数据快照、证券池和 PIT 规则。
- 特征、标签、模型配方，以及正式复算任务生成的模型与预测制品。
- 信号频率、决策频率、持有期和最早成交时点。
- 初始资金、基准、现金收益、成本和容量参数。
- 执行规则配置版本和 Qlib Strategy/Executor/Exchange 配置。
- 评价指标、压力场景和样本边界。

### 7.2 简单基线与基准

每个策略至少有一个容易理解、现实可投资的基线：现金、对应 ETF、买入并持有、简单等权、简单资产配置或冻结的规则策略。

用于证明增量价值的**同类型简单基线**必须使用同一 PIT 证券池、时间区间、初始资金、实际可获得的现金收益、决策/再平衡日历、`ExecutionCore`、费用税费、整手、容量、成交率、未成交与估值规则。基线可以按自身规则选择不交易，买入并持有也只在初始时点下单，但不能通过省略候选必须承担的可交易限制或成本获得不公平结果。若另列不同资产的指数或 ETF 作为经济/政策基准，必须标明其资产范围差异，并同时保留一个同证券池简单基线，不能用异资产基准替代公平的增量比较。

策略研究基准、账户政策基准和简单可投资基线分别保存：

- 指数增强使用对应指数并重点看主动风险。
- ETF 配置使用个人政策基准和简单 ETF/现金组合。
- 股票核心使用宽基和相同约束下的简单股票基线。
- 账户整体使用符合个人资金用途的政策基准。

比较超额收益时优先使用包含分红的总收益口径；若只有项目重建的合成代理，必须标明“合成”，不能冒充官方全收益指数。

### 7.3 压力与稳健性

至少检查：

- 佣金、价差、滑点、冲击和成交率的分别恶化。
- 参与率收紧、部分成交和跨日执行。
- 参数邻域与特征轻微变化。
- 牛市、熊市、震荡和高波动时期，但市场阶段定义不得事后挑选。
- 小、中、大不同账户规模下的整手、最低佣金和容量。
- 数据延迟、缺失、停牌、涨跌停和退市案例。

“所有成本简单乘 2”可以作为辅助情景，但不能替代逐项压力，也不能作为最大损失上限。

### 7.4 回测输出边界

回测页面必须同时显示：样本内、验证、walk-forward、最终样本外、成本前、成本后和简单基线结果。

以下结论必须明确区分：

- 计算成功：程序跑完。
- 结果可复现：同一输入重新运行一致。
- 统计证据支持：结果不太可能只是候选选择造成。
- 经济上可用：扣费、容量和风险后仍有增量价值。
- 允许进入隔离模拟：全部正式硬门通过后由系统自动建立 `paper` 前向实验；这不等于允许参与个人推荐。

回测盈利不等于模拟盈利，更不等于未来盈利。

## 8. 账户组合与最终投资推荐

### 8.1 从策略信号到一个账户目标

每个策略只输出自己的目标权重和解释。系统同一时刻只能有一个 `active_recommendation_account_id`，其类型只能是 `main_paper`，或状态足够新鲜且经用户明确选中的 `manual_shadow`。默认使用 `main_paper`；启用影子账户后，主模拟账户可以继续作为模型组合对照，但不能同时冒充用户账户发送“最终建议”。`forward_paper` 永远不能成为主动推荐账户。

切换主动账户必须人工确认并重新运行推荐；两个账户的现金、持仓、订单、成交和通知状态不得迁移或混合。若主动影子账户变陈旧，停止精确数量和新开仓分时提醒，直到刷新或用户明确切回主模拟账户，不能静默换源。对主动账户只生成一份综合目标，并按以下顺序处理：

1. 读取所选目标账户已经确认成交的经济持仓、现金、待执行订单和仍有效目标，但暂不修改订单或冻结资源。
2. 读取唯一有效的 `AccountAllocationPolicy` 和当前 `AllocationArtifact`。只有到达政策冻结决策日才按截止时点生成一个新制品；其他推荐运行复用仍有效预算。账户合并时预算只应用一次，政策本身不产生证券目标；预算之和不得超过可投资资本，剩余部分为现金，过期/失败时按冻结回退或阻断。
3. 收齐当前已启用 `StrategySpec` 的最新有效目标，每个策略只能在获配预算内转为账户证券权重，再做证券级净额合并；不同频率没有新信号时沿用其上一有效目标，而不是每天重新调仓。禁止不同策略在同一账户自买自卖，也禁止策略再次修改账户预算。
4. 应用市场权限、最低现金、近期用款、单票/行业/资产上限、流动性、可卖数量和风险退出等硬约束，得到候选目标。
5. 分别构建“继续当前有效目标”和“切换到候选目标”的可执行路径，计算完整换仓成本、整手误差、容量、执行时间和风险。
6. 对普通 Alpha 调整应用 no-trade band，先决定保留旧目标还是接受候选目标；增量不足以覆盖成本和不确定性时保留旧目标及其订单计划。现金缺口、证券失效、权限收紧和风险减仓/退出等硬约束绕过 Alpha no-trade band，但仍受可卖数量、停牌和市场可交易性限制。
7. 只有最终目标确定后，才计算预计持仓：`projected_position = filled_position + 有效未完成买单 - 有效未完成卖单`，并逐笔生成 `keep/cancel/replace/new` 计划。新订单只补最终目标与预计持仓之间的差额。
8. 已成交部分直接进入 `filled_position`；取消或替换只释放尚未使用的冻结现金/数量一次。目标与订单计划在同一事务提交，每次部分成交后按同一顺序重新计算，任务重试必须得到相同结果。
9. 账户动作由最终目标与 `filled_position` 比较，订单数量由最终目标与 `projected_position` 比较；最后生成唯一最终目标和一份综合建议，再交给模拟盘。

硬约束优先于 Alpha。普通模型分数不能覆盖现金需求、不可交易、可卖数量、风险退出和人工执行能力。

### 8.2 No-trade band

账户设置简单的不交易区间，用于避免微小目标变化、数值误差或模型噪声导致频繁交易。

佣金、税费、滑点和冲击已经进入换仓成本，不能又重复塞入 no-trade band。具有经过样本外校准的预期收益时，可以比较保守增量收益与完整成本；只有排名分数或规则目标时，使用预先冻结的目标漂移带、最小权重变化和最小合法订单，不得把 rank score 与人民币成本直接比较。满足相应门槛后才发布新交易建议。

### 8.3 账户风险状态

账户至少监控现金缺口、集中度、波动/尾部风险、单位化 `investment_wealth` 回撤、流动性、未完成订单和数据新鲜度。风险状态保持简单：`normal`、`caution`、`risk_off`。

所有风险指标必须标记 `risk_scope=selected_account_only`。未导入的其他券商账户、基金、现金、房产、负债和未来收入不在计算范围内，所选模拟/影子账户的回撤和集中度不能描述成个人或家庭总资产风险。

- `caution` 可以缩小新订单、提高现金或要求人工复核。
- `risk_off` 停止新增风险，并根据可交易性生成减仓或退出目标。
- 风险覆盖只能减少或延后普通 Alpha 风险，不能凭空增加仓位。
- 风险减仓或 `EXIT` 是账户硬门，不是分钟 Alpha 反转，也不保证立即成交。

### 8.4 推荐动作与执行状态

账户动作与执行状态是两个维度。一个 `SELL` 建议可以同时处于 `WAIT`，不能把等待误写成没有卖出目标。

| 账户动作 | 含义 |
| --- | --- |
| `BUY` | 最终目标高于已经确认成交的 `filled_position`；即使有效未完成买单已经覆盖差额，动作仍是 `BUY` |
| `SELL` | 最终目标低于 `filled_position` 但仍大于零；即使有效未完成卖单已经覆盖差额，动作仍是 `SELL` |
| `EXIT` | 风险、证券失效或策略退出要求目标降至零或最低风险状态 |
| `HOLD` | `filled_position` 已达到最终目标；任何会使持仓偏离目标的有效订单必须在同一计划中取消 |
| `NO_ACTION` | 没有新的有效账户动作或目标；不得用于覆盖仍有效的 `BUY/SELL/EXIT` |

| 执行状态 | 含义 |
| --- | --- |
| `READY` | 可以按规则释放或继续模拟订单 |
| `WAIT` | 目标仍有效，但价格、流动性或交易状态暂不适合执行 |
| `PARTIAL` | 已部分成交，仍有有效剩余量 |
| `CANCELLED` | 模拟订单或系统计划被取消；账户目标是否继续有效单独判断 |
| `EXPIRED` | 订单或建议超过有效期 |
| `BLOCKED` | 数据、账户或市场硬门阻止执行 |

`NO_ACTION` 不能删除上一有效目标；已有 `BUY/SELL/EXIT` 被数据、账户或市场硬门阻断时，必须保留原动作并标记 `WAIT` 或 `BLOCKED`。`WAIT` 不能被展示成没有信号；`EXIT` 也不能暗示一定能立即成交。`CANCELLED` 只取消模拟订单或系统计划；本系统永远不会替用户撤销券商客户端中的真实委托，只能提醒用户自行处理。

新增订单差额单独按 `final_target_quantity - projected_position` 计算并受整手、现金和可卖数量约束。典型语义必须成立：持仓 0、目标 100、未成交买单 100 时仍显示 `BUY`，执行状态按当前可执行性为 `READY` 或 `WAIT`（已有部分成交时为 `PARTIAL`），新增买单为 0；持仓 100、目标 100、仍有买单 20 时显示 `HOLD` 并取消多余买单；持仓 100、目标 0、未成交卖单 60 时保持 `EXIT`，尚需卖出 40。

### 8.5 推荐内容

每条账户建议至少包含：

- 数据截止时间、策略版本和决策频率。
- 主动账户的 `account_id`、`account_type` 与状态时间；`main_paper` 或 `manual_shadow` 只有一个可以产生最终提醒。实验账户使用 `forward_paper` 单独展示，不参与综合推荐。没有用户确认或新导入时不得暗示真实账户已经成交。
- 当前权重、目标权重、权重差额。
- 可信账户状态下的建议数量；状态不新鲜时只显示目标权重。
- 动作、执行日期/时段、订单类型、价格保护和最长等待时间。
- 建议原因、主要因子/模型贡献和多策略冲突处理结果。
- 估算费用、容量、流动性、停牌/涨跌停和可卖数量提示。
- 失效时间、失效条件、阻断原因和主要风险。
- 回测证据、模拟证据和数据血缘链接。

建议只提供决策支持，不使用“必涨”“必赚”“精准买点”或“保证成交”等表述。

### 8.6 手工/CSV 个人影子账户

首版若要输出基于用户实际账户的个人数量建议，必须支持用户手工或 CSV 导入实际持仓、现金、可卖数量、未完成订单和实际成交。没有足够新鲜的影子状态时，只能输出模拟账户建议或目标权重，不能给出伪精确的个人数量。

影子账户、模型组合和模拟账户必须分别展示。用户可以确认全部执行、部分执行、未执行或自行成交；系统不能假设用户按建议执行，也不能只统计被用户选择执行的盈利建议来证明策略有效。

券商只读同步不属于首版，未来如增加也必须默认关闭，并在技术上拒绝所有写接口。默认关闭的 QMT 沙箱插件是唯一例外：它只连接沙箱账户、启动时强制沙箱环境断言、HMAC 鉴权，并与唯一主线在技术上隔离——任何任务、调度、页面和模拟链都不得调用它。

### 8.7 个人市场权限与建议范围

每个模拟账户或 `manual_shadow` 都绑定版本化个人投资政策。按交易所、板块、风险警示类别和 ETF 子类型记录 `buy_sell`、`sell_only`、`disabled` 或 `unknown`，并保存确认来源、`as_of` 和有效期：

- `buy_sell` 可以形成买入、卖出和退出建议。
- `sell_only` 只允许减仓和退出，不能新增风险。
- `disabled` 不得新增；已有持仓只按预注册处置规则展示或退出。
- `unknown` 或权限过期只能用于研究、通用模拟或显示目标权重，不能输出伪可执行的买入数量。

个人政策只能收紧策略、市场和风险规则，不能放宽数据、可交易性或账户硬门。账户状态陈旧、存在未确认成交，或建议数量/切片/响应时间超过用户配置的人工执行能力时，精确数量和即时新开仓建议降为 `simulation_only`；不能临时删掉部分证券后继续引用原策略的正式成绩。

## 9. 模拟盘、订单与账本

### 9.1 账户类型

- **主模拟账户**：运行允许参与推荐的策略和最终综合目标。
- **实验模拟账户**：每个冻结候选及其证据配置使用独立账户，隔离观察，不影响主建议。
- **压力场景运行**：在不同资金规模和成本假设下临时测试容量，不要求持久化成独立账户。
- **个人影子账户**：系统提供手工/CSV维护能力，用户可以不启用；不产生自动实盘操作。

初始资金是账户配置，不固定为 500 万元。本项目个人 paper 账户首轮默认使用 10 万元；50 万、100 万、500 万等只可作为容量测试档位，不能替代用户实际规模。

`main_paper` 和 `manual_shadow` 可以并行维护，但只有 `active_recommendation_account_id` 指向的一个账户可以发送最终建议；`forward_paper` 永远只作实验展示。

### 9.2 订单生命周期

模拟订单至少支持：

`planned → open → partially_filled → filled | cancelled | expired | rejected`

订单保存证券、方向、数量、已成交量、剩余量、创建时间、最早成交时间、有效期、价格保护、执行窗口、最终综合目标版本和不可变的策略贡献明细。

多策略净额合并后只能创建账户级综合模拟订单。稳定键至少由 `account_id + final_target_version + security_id + side + execution_window + tranche_index` 组成，`strategy_id` 不得作为创建多份订单的幂等键。任务重试不得重复创建订单、成交、费用或公司行动。

`strategy_contributions` 保存各策略版本、资本预算、净额前目标变化和净额后贡献，只用于解释和冻结归因。相反方向需求先在账户内部净额抵消；容量、费用、冻结现金和可卖数量只对净额后的实际综合订单计算。如保留策略级执行归因，成交量和费用按预先冻结的同向净需求比例分配，整手余数使用固定规则；否则不得声称存在精确的策略执行归因。主模拟账户的归因不能代替各策略独立 `forward_paper` 的晋升证据。

只有账户按 8.1 节先选定最终目标后，才把已成交数量计入 `filled_position`、根据仍有效未完成订单计算 `projected_position`，并逐笔执行 `keep/cancel/replace/new`。取消或替换只能释放尚未使用的冻结现金/数量；不得重复下单、重复冻结或把已成交部分重新下单。

### 9.3 模拟成交

- 只消费账户级最终综合建议，不直接消费 RD-Agent 输出或单策略信号。
- 使用与正式 Qlib 回测相同的轻量 `ExecutionCore`。
- 按下一 Bar、T+0/T+1、可卖数量、价格限制、停牌、参与率和成本判断。
- 支持部分成交、未成交、跨日、取消和过期。
- 每笔成交由 `ExecutionCore` 返回唯一 `state_delta`，历史 Qlib 适配器或前向 PostgreSQL 事务只应用一次；按交易日会计同时确认经济持仓/回款和本笔费用，交收仅改变可卖、可交易、可取等状态。
- 模拟成交只代表规则假设，不冒充真实盘口成交。

### 9.4 日终处理

每个交易日的事件顺序固定为：盘前只处理 `effective_at` 已到的交收状态变更和公司行动当前阶段，盘中按完成 Bar 处理模拟订单，盘后再估值、确认非交易费用或交易费用差额并对账。公告但未到经济生效阶段的公司行动不得提前入账。每个交易日结束后：

1. 汇总并核对盘前已经生效的交收/公司行动和盘中已经产生的模拟成交；每个事件使用唯一键，只能生效并入账一次。
2. 重新计算现金批次、持仓批次、已实现/未实现损益、公司行动应收、费用、估值、人民币 NAV、单位化投资表现和回撤。
3. 与上一日 NAV、外部现金流和当日损益做守恒对账。
4. 检查重复订单、重复成交、负现金、负持仓和可卖数量异常。
5. 保存策略、行业、资产、成本和执行偏差归因。
6. 任何不平衡或关键估值缺失都将账户标记为待复核，并阻止新的正式推荐。

### 9.5 历史回放与前向模拟

必须分别标记：

- `historical_backtest`：Qlib 历史正式回测。
- `historical_replay`：用模拟盘规则重放历史订单，验证实现一致性。
- `main_paper`：只运行已获准参与推荐的策略，形成主模拟账户综合目标；当它不是主动推荐账户时只作模型组合对照。
- `forward_paper`：只使用运行当时真实取得的数据向前运行；提醒和成交必须标记实验模拟账户来源，永远不发送最终个人建议。
- `manual_shadow`：用户实际持仓和成交的手工记录。

每个实验前向阶段由“`StrategySpec` 版本 + 数据/PIT 语义合同版本 + 证券池规则版本 + 成本规则 + `AccountAllocationPolicy` 版本/人工预算配置 + `ExecutionPolicy` 与路由规则版本集合 + 主要风险配置”唯一确定，并拥有独立的 `forward_paper_account_id`、现金、订单、成交、持仓、NAV 和证据计数。多个候选可以共用进程和只读数据，但不得共用账本或资本。阶段内保存每天实际使用的数据快照、`ModelArtifact` 和 `AllocationArtifact` 序列；按冻结数据合同追加新交易日、按 `StrategySpec` 日历例行 refit，以及按冻结账户政策日历产生例行预算制品，都不重置阶段。

历史分钟数据盘后补齐不能回写并美化当天的前向模拟成交或提醒。`StrategySpec`、数据/PIT 语义或实质修订、证券池规则、成本、`AccountAllocationPolicy` 规则/频率/窗口/成员资格、人工预算配置、`ExecutionPolicy`/路由规则或主要风险规则发生实质变化时必须新建模拟阶段，证据从零重新累计；旧阶段保持只读，旧、新自然时间、成交事件和表现不得拼接用于晋升，也不得修改旧阶段 NAV。按冻结政策日历正常变化的实际预算不重置；例行 `ModelArtifact` 切换若超出预注册日历或改变模型配方，必须先形成新 `StrategySpec`，不能借“refit”绕过重置。

## 10. 月度、周度、日度与分时提醒

### 10.1 四个频率的职责

| 层级 | 主要职责 | 是否必须产生新交易 |
| --- | --- | --- |
| 月度 | 资产配置、核心策略调仓、模型/参数例行复核 | 否；无增量价值时保持原目标 |
| 周度 | 策略健康、风险、漂移、当前账户目标有效性和可选周频策略复核 | 否；即使没有周频 Alpha，也报告当前账户动作或 `HOLD/NO_ACTION` |
| 日度 | 收盘数据更新、风险检查、当前目标有效性、已启用日频策略和次日建议 | 否；每天给出状态结论，但状态不变时不重复调仓或推送 |
| 分时 | 检查已批准目标当前是否可执行、应等待、取消或失效 | 默认不能重新选股或反转低频目标 |

月、周、日是不同的决策频率，不等于数据每天更新就每天调仓。每个策略的目标持续到下一正式决策、明确失效、暂停或风险覆盖。

策略目录与提醒频率的默认映射为：

- 月度：ETF 配置/轮动、风险均衡、核心—卫星、个人股票核心、单因子/多因子、指数增强和全市场配方。
- 周度：`weekly_tactical_overlay`，以及经 `StrategySpec` 明确设为周频的低换手横截面或趋势配方；没有获批周频策略时只报告账户与策略状态。
- 日度：均线/突破/Swing、日线均值回归、配对关系检查、风险与目标有效性。
- 分时：默认执行获批月/周/日目标；只有单独获批的 ETF 分钟、股票盘中入场隔夜退出或独立分钟模板才能产生新的分钟 Alpha。

具体策略可以使用不同冻结频率，但改变决策、持有或执行频率属于新的 `StrategySpec`，不能只改报表标签。

### 10.2 分时买卖点的准确含义

主推荐账户在没有已通过完整证据门的独立分钟策略时，不提供分钟 Alpha。“分时买卖点”默认含义仍是对已有中低频目标进行 1min/5min 执行检查：

- 现在可以释放买入或卖出计划。
- 当前价格超出保护范围，应继续等待。
- 停牌、涨跌停、流动性或成交量不足，暂时无法执行。
- 已部分成交，提醒剩余数量和剩余有效期。
- 目标或模型已经失效，应取消未执行部分。
- 风险状态变化，需要减仓或退出。

分时检查只能在具备盘中实时分钟权限、完整 Bar、可接受延迟和数据质量时启用。盘后更新的历史分钟数据只能做回放，不能冒充实时提醒数据。

“部分成交”和“剩余数量”只来自模拟账本，或来自用户刚确认/导入的 `manual_shadow` 状态；提醒必须显示账户来源，不能因为模拟成交就暗示真实账户已经成交。

6.4.3 节的独立分钟研究模板只有在自己的数据、标签、T+0/T+1、成本、容量、回测、统计证据、资本预算和隔离模拟全部合格，并单独进入 `recommendation_enabled` 后，才可产生分钟 Alpha；它不能借用低频策略成绩。提醒必须明确标识“低频目标执行点”还是“已批准分钟策略信号”。

### 10.3 通知规则

每个完整 Bar 可以静默检查，但只在以下状态发生实质变化时通知：

- 动作改变。
- 目标权重或建议数量形成可执行变化。
- 风险等级升级。
- `WAIT` 变为可执行，或可执行变为阻断。
- 订单部分成交、失败、取消、过期或信号失效。

连续相同的 `HOLD`、`NO_ACTION` 或 `WAIT` 不重复推送。硬风险提醒不能被普通免打扰设置屏蔽。

通知至少包含动作、证券、当前/目标、建议数量或权重、执行窗口、有效期、原因、主要风险和查看详情链接。通知记录创建、发送、失败、重试、送达和用户确认，但“发送成功”不等于“用户已经执行”。

首版至少实现 Web 站内通知，并在 Email、企业微信、钉钉或等价 Webhook 中实现一个可配置外部推送通道；外部发送失败时控制台必须保留待处理事件并重试。

### 10.4 参考运行日历

具体时间可配置，任务先后关系固定：

- 每个交易日盘后：下载日线、复权、财务/成分和历史分钟增量，完成质量检查后构建 Qlib 数据集。
- 每个交易日数据就绪后：先完成当日模拟成交、交收、公司行动、估值和账本对账；只有对账通过后，才使用最终现金、持仓、可卖数量和未完成订单运行日度风险、账户合并并生成下一交易日建议。
- 每周末：生成周报，检查数据、模型、成本、回撤和策略状态；按预算运行 RD-Agent 研究。
- 每月决策日：运行月频策略、资产配置、重训计划和组合目标。
- 盘前：检查交易日、策略有效期、账户状态、未完成订单、停牌和风险状态。
- 盘中：分别处理待执行低频目标、独立 `forward_paper` 分钟研究账户，以及已进入 `recommendation_enabled` 的分钟策略冻结白名单。每个完整 Bar 后，获批分钟策略先生成自身预算内目标，再读取当前有效 `AllocationArtifact` 做预算检查、证券级净额、账户硬约束和 `ExecutionPolicy` 路由；只有账户政策自身冻结决策日到达时才生成新预算制品，不得每分钟重估风险，也不得扫描或临时研究未批准的全市场候选。

数据未就绪时延后或阻断，不允许用固定时钟绕过质量门。

## 11. 最小工程与运行保障

### 11.1 部署形态

个人/小团队首版采用单服务器或单台工作站：

- 一个 PostgreSQL 保存任务、策略版本、推荐、订单、成交、持仓和 NAV。
- 文件系统或简单制品目录保存原始快照、Qlib 数据集、模型、预测和 Recorder 制品。
- 一个调度器和一个或少量 worker 运行下载、研究、回测、模拟和通知任务。
- 一个 Web 控制台查看状态、研究、回测、模拟账户和建议。
- Docker Compose 可用于部署，但不是金融正确性的前提。

不引入消息总线和多服务一致性平台。数据库事务、唯一任务键和短租约足以防止同一任务重复运行。

### 11.2 任务可靠性

每个任务必须具备：

- 唯一任务键和幂等重跑。
- 超时、有限重试、取消和失败原因。
- 输入版本、输出制品、开始/结束时间和日志。
- 依赖数据过期或失败时的阻断，不静默使用旧结果。
- 崩溃后从数据库状态恢复，不重复下载入库、记账或提醒。

### 11.3 最小安全

- Tushare Token、LLM 密钥和通知密钥不进入 Git、日志和 RD-Agent 提示词。
- RD-Agent 候选代码在隔离环境运行，只读挂载研究数据，不访问生产数据库写权限。
- Web 控制台至少具备本地登录和最小权限；个人账户明细不进入研究制品。
- 除默认关闭的 QMT 沙箱插件外，系统代码中不提供券商写入入口；自动化测试扫描网络调用和配置，证明主线任务不会发出真实委托，且沙箱插件仅在沙箱环境断言下可连接。
- 严重数据、账本或版本异常触发 `safe_mode`，停止新建议和新模拟订单，但保留查看、对账和恢复能力。

### 11.4 备份与恢复

定期备份：

- PostgreSQL。
- 原始快照索引和必要原始文件。
- Qlib 数据集构建配置、模型、预测和 Recorder 制品。
- 策略、`AccountAllocationPolicy/AllocationArtifact`、`ExecutionPolicy`、成本、市场规则和个人配置版本。

至少定期做一次恢复演练。恢复后用校验和、订单/成交数量和 NAV 对账确认数据完整；只备份但从不验证恢复不算通过。

## 12. 测试、验收与实施顺序

### 12.1 数学测试

- 修改决策时点之后的数据不能改变过去的特征、信号和订单。
- 训练窗口外数据不能影响标准化、特征选择和模型参数。
- 重叠标签在训练、验证、外层 Fold 和最终样本外边界的 purge/embargo 正确；声称评价完整选择流程时，每个外层 Fold 都会重新执行内层选择。
- 试验台账包含失败和放弃候选。
- 最终样本外一经打开，所有形成于 `first_opened_at` 之后的候选都不能重复冒充最终未见样本，即使改名研究活动或假设谱系。
- 联合确认时，`sealed_candidate_set_id/hash`、有序候选与 `StrategySpec` 哈希、指标、通过规则和多重检验规则在 OOS 打开前冻结；形成较早但未列入集合的候选也不能事后加入或使用该 OOS 晋升。
- 实际外部入金/出金不会制造收益或回撤，未实际划出的计划用款不进入外部流；单位化 TWR、最大回撤、恢复期、换手和 XIRR 使用固定公式与现金流时点。
- 股票数、Bar 数、折数和随机种子数不会被当作独立市场样本。
- 候选相对基线的统计比较使用相同时间和成对样本。

### 12.2 金融测试

- Tushare 日线手/千元与分钟股/元的单位转换黄金案例。
- 原始价、复权收益、执行价和估值价分离。
- 股票 T+1 与不同 ETF 回转规则按品种和日期生效。
- 下一 Bar、整手、最低佣金、印花税、过户费和成本不重复计算。
- 停牌、涨跌停、部分成交、跨日订单和容量收紧符合单调性。
- 分红、送转、拆股、配股、换股、退市和外部现金流满足账本守恒。
- 已公告但未生效的公司行动不提前入账；除权确认应收、到账重分类和新增证券可卖阶段不会重复增加 NAV。
- 买入/卖出成交与随后交收只改变一次经济资产；交收不重复改变 NAV，日终只确认交易费用差额。
- 长期停牌或缺少可靠估值时不会伪造可变现 NAV。

### 12.3 量化测试

- RD-Agent 候选不能直接修改允许推荐的策略版本。
- RD-Agent 必须分别跑通“已有模板扩展”和“自主提出目录外新策略”两条路径；`NewStrategyProposal` 在没有人工模板设计确认时不能变成 `candidate`，两条路径都不能直接写推荐或模拟账本。
- 冻结候选可以用 Qlib 从零重跑并得到一致预测和回测结果。
- 6.4 节每一项都带 `catalog_role`、`implementation_tier` 和实现状态；任何 `capital_eligible_strategy`（`alpha_template`、部署型 `recipe_variant` 或可投资 `baseline`）都具有完整 `StrategySpec`、适用基线/准入目标、Qlib 入口和确定性测试，`conditional/research_only/model_challenger/research_admission` 不会误获独立账户或资本。
- 日线、1min、5min 及重采样组件版本和证据相互隔离；模板八个插槽按固定顺序执行，硬资格、现金、容量和风险门不能被 Alpha 分数覆盖。
- 正式 Qlib 适配器和模拟盘对同一历史事件、订单使用同一 `ExecutionCore`，黄金案例结果一致且 `state_delta` 只应用一次；Qlib 默认执行器不能生成正式证据。
- 独立复算不读取 RD-Agent 导出的因子值、预测或总结；正式候选同时生成单组件增量、消融、负对照、泄漏哨兵、参数邻域和分项成本压力结果。
- 未改变 `StrategySpec` 的例行 refit 只走模型制品安全门；改变特征、标签、选择规则、重训日历、组合或执行规则必须创建新候选并重走晋升门。
- 不同策略类型使用自己的基准和准入目标，而不是一张统一阈值表。
- 候选与同类型简单基线使用同一 PIT 证券池、时段、现金收益、决策日历、执行核心、成本、整手、容量和成交规则；异资产政策/指数基准不能替代该比较。
- `rank_score`、`expected_return`、`event_probability` 和 `target_weight` 的类型、期限与单位不会混用；收益/概率完成样本外校准，信号有效期与最大响应延迟来自衰减测试。
- 相同经济假设改名、换频率或换模型仍归入同一 `economic_hypothesis_group`，共享试验计数和资本上限。
- 成本增加不能提高同一成交路径的净收益；容量收紧不能增加成交量。
- 启用优化器时，病态协方差、不可行约束、非有限解和离散后约束违例必须失败关闭；未启用优化器的简单策略不被强制增加优化模块。
- 下一 Bar、TWAP、VWAP 和切片执行使用 Implementation Shortfall、完成率、未成交率、参与率、延迟和容量验收，不使用 Alpha、IC 或策略 Sharpe 代替执行证据。
- `etf_long_long_relative_allocation` 只产生普通 long-only 权重并走账户净额；`stock_pair_stat_arb` 只保存离线统计研究，不能创建 `candidate`、持久 `forward_paper`、卖空订单或推荐。
- 没有正 Alpha 时系统可以正确拒绝候选并保持简单基线。

### 12.4 模拟盘与推荐测试

- 多策略对同一证券的冲突先净额合并，只生成一份账户目标。
- 每次推荐只把当前有效 `AllocationArtifact` 在账户层应用一次；只有政策冻结决策日才生成新制品，普通日度/分钟任务不会重估动态预算。`AccountAllocationPolicy` 不能作为成员策略再次获配资本，`StrategySpec` 不能修改其他策略预算，`ExecutionPolicy` 不能取得策略资本或改变最终目标。
- 当前持仓、待执行订单、现金、可卖数量和整手都进入建议计算。
- `BUY/SELL/EXIT/HOLD/NO_ACTION` 账户动作与 `READY/WAIT/PARTIAL/CANCELLED/EXPIRED/BLOCKED` 执行状态分别保存，语义互不混淆。
- 持仓 0、目标 100、未成交买单 100 时保持 `BUY`、新增订单量为 0；持仓已达目标但存在多余订单时取消多余订单。
- 普通 Alpha 候选未通过 no-trade band 时旧订单完全不变；风险退出绕过 Alpha band，但仍受市场可交易性限制。
- 同一时刻只有主动推荐账户产生最终通知；`main_paper` 对照、`manual_shadow` 和 `forward_paper` 不会同时冒充最终账户。
- `sell_only`、`unknown`、权限过期和陈旧影子账户分别正确阻止新增风险或降级为权重/模拟展示；风险报告始终标记 `risk_scope=selected_account_only`。
- 多策略净额后的综合订单只计一次容量和费用，任务重试不会因策略来源重复建单。
- 任何 `StrategySpec`、数据/PIT 语义、证券池规则、成本、`AccountAllocationPolicy` 规则/频率/窗口/成员资格、人工预算配置、`ExecutionPolicy`/路由规则或主要风险配置实质变化都会新建独立 `forward_paper` 阶段并从零累计证据；按冻结合同追加数据、例行 refit 和按政策日历产生 `AllocationArtifact`/预算变化都不会重置阶段。
- 模拟订单支持部分成交、未成交、取消、过期和重试幂等。
- 日终现金、持仓、应收、费用和 NAV 对账守恒。
- 相同状态不会每 5 分钟重复提醒；状态变化会产生一次可追踪通知。
- 实时分钟数据缺失或延迟超限时分时提醒关闭或降级。
- 盘中调度同时覆盖待执行低频目标、独立分钟 `forward_paper` 和已获批分钟策略白名单；获批分钟目标在完整 Bar 后仍经过账户分配检查、净额和硬约束，未批准候选不会被盘中扫描。
- 任意任务都不能发起券商写请求。

### 12.5 端到端验收

系统至少通过以下完整链路：

1. 从 Tushare 下载一段真实数据并生成不可变快照。
2. 通过质量门构建 Qlib 日线数据集；需要分钟数据的模板还要验证 1min/5min 与重采样血缘。
3. 加载 6.4 节完整策略目录及 `catalog_role/implementation_tier/implementation_status`：`foundation` 和任何已经实现的 `capital_eligible_strategy` 运行适用基线、候选或明确硬失败；计划项和条件项保存研究任务、父模板、实施状态或数据/权限阻断原因。
4. 分别选取资产配置、股票横截面、趋势/均值回归、相对价值/分钟或 ML 增强的代表配方，证明它们复用同一数据、验证、执行和模拟主线而不是各建一套引擎。
5. RD-Agent 完成一次已有模板扩展和一次目录外新策略提案；前者形成可复现候选，后者在人工模板确认前停在 `NewStrategyProposal`。
6. 独立任务从锁定输入重新计算候选，Qlib 完成训练、预测、正式回测、消融/负对照和 Recorder 保存。
7. 通过正式历史验证门和基础运行安全门的冻结版本，自动创建独立隔离模拟账户。
8. 隔离前向模拟达到预注册证据门后，经人工确认进入 `recommendation_enabled`；证据不足时链路正确停在 `paper`。
9. 根据主模拟账户当前状态和多策略净额形成唯一账户建议。
10. 分别验证下一 Bar 基线执行和一个适用的 TWAP/VWAP/切片方案；模拟部分/完整成交并记入现金、费用、持仓和 NAV。
11. 完成日终对账、日报、周报或月报。
12. 生成低频建议、分时执行点和一次状态变化提醒；若存在已批准分钟策略，分钟 Alpha 与低频执行提醒可清楚区分。
13. 证明任何研究、回测、模拟或提醒任务都不会调用券商写接口。

端到端场景还必须覆盖：外部入金不制造投资收益/回撤；未成交买单覆盖目标时保持 `BUY` 且不重复下单；成交、交收、费用和分红应收/到账全链路 NAV 守恒；Qlib 历史状态与 PostgreSQL 前向状态各自只应用一次执行增量；三个账户并行时只有主动账户产生最终提醒。

系统能力验收不要求策略盈利。正确地输出“候选不合格”“证据不足”或 `NO_ACTION` 也属于通过。只有某个策略单独完成正式历史验证、前向模拟和人工确认后，才能将其状态设为 `recommendation_enabled`；这仍不构成收益保证。

### 12.6 固定实施顺序

| 阶段 | 必须完成 | 阶段结果 |
| --- | --- | --- |
| 1. 数据基础 | Tushare 权限、批量下载、原始快照、单位、PIT、质量门、Qlib 转换 | 可重复构建日线数据集 |
| 2. 策略与自主研究框架 | 完整目录及角色/层级/实现状态、组件注册、八插槽、`StrategySpec/ModelArtifact`、简单基线、试验台账、`ResearchBrief`、`NewStrategyProposal`、RD-Agent 沙箱和预算 | 可扩展已有模板，也可自主研究新策略而不越权 |
| 3. 正式验证 | 预注册准入规范、walk-forward、最终样本外、完整成本、容量、压力、冻结 Recorder | 可区分探索成绩和正式证据 |
| 4. 模拟基础 | 公共执行规则、订单生命周期、现金/持仓/可卖数量、公司行动、NAV、对账 | 可运行可靠前向模拟盘 |
| 5. 基线、ETF 与账户政策 | 简单政策、ETF 配置/轮动/策略内风险均衡，以及独立的固定预算、核心—卫星、跨策略逆波动/风险均衡 `AccountAllocationPolicy` | 可形成月度配置、一次性策略预算和现金退路 |
| 6. 股票横截面模板 | 个人股票核心、各单因子、多因子、指数增强、全市场/行业中性配方 | 可形成周/月度股票研究、正式回测和隔离模拟 |
| 7. 趋势与战术模板 | 均线、突破、Swing、日线均值回归、周频战术；轮动/事件在 PIT 数据不足时保持研究阻断 | 可形成日/周度研究和建议候选 |
| 8. 条件模板与挑战者 | ETF long-only 相对配置、ETF 分钟、股票盘中入场隔夜退出、新分钟策略入口、ML/事件增强；股票配对仅离线统计研究 | 合格项可独立验证；条件不足时明确阻断，股票多空不进入正式模拟或推荐 |
| 9. 推荐与执行提醒 | 多策略预算/净额、个人硬约束、影子账户、no-trade band、下一 Bar/TWAP/VWAP/切片、月/周/日/分时通知 | 可输出唯一低频账户建议，并在获批后区分分钟 Alpha 与执行提醒 |

阶段顺序表示依赖关系，不再把策略素材缩减为一个示例。`foundation` 先保证个人主线可用，`standard` 是承诺保留的分阶段实现目录，`conditional` 不因外部数据/权限未满足而阻塞正确的主线；三者的名称、角色和状态都必须可见。不能在数据、时间、成本和模拟账本未正确前，用 RD-Agent 自动研究、漂亮页面或更多策略掩盖基础缺陷。

## 13. 条件启用与范围外能力

以下策略已经纳入 6.4 节，不需要以后重新改架构；它们只是在满足条件前保持 `research` 或 `blocked_by_data_or_permission`：

| 能力 | 启用前置条件 |
| --- | --- |
| 独立分钟 Alpha | 实时 1min/5min 权限、完整历史、真实标签/可卖时点、延迟、成本、容量、独立最终样本外和隔离模拟证据 |
| ETF long-only 相对配置 | 产品关系、普通多证券流动性、换手、容量和共享底层暴露均已建模；仍走单一 long-only 账户净额链 |
| 行业轮动与事件策略 | 历史行业分类、事件公开时间、修订、预期差、数据口径和可交易时点能够 PIT 重建 |
| 跨境、债券、黄金、商品、货币等 ETF | 对应交易日历、汇率/久期/信用/估值、回转交易、费用和特有风险进入子类型白名单并通过测试 |

`stock_pair_stat_arb` 不在上述“条件满足即可启用”集合内。当前设计只保留离线关系研究；即使取得借券数据也不能进入正式双腿回测、持久模拟或推荐。未来若确实要做，必须另立完整卖空账户与双腿执行专项设计，而不是改一个权限开关。

以下事项仍在当前产品范围之外；若未来确有需求，应另立专项设计：

- 券商只读同步可以作为可选数据接入；手工/CSV 影子账户已经在当前设计内。券商写入、自动下单、自动撤单和资金控制不在本设计授权内；唯一例外是默认关闭的 QMT 沙箱插件——它只面向沙箱账户、默认禁用、不构成任何主线能力，也不改变本系统不发出真实委托的承诺。
- 期货/CTA、期权、期限结构、波动率套利、裸空和跨市场/加密资产套利。
- Tick/Level-2、逐笔、做市和低延迟高频交易。
- 高级个人效用函数、目标成功概率和家庭人力资本模型。
- 通用统计认证平台、复杂联合重采样和序贯检验平台。
- 第二套 C++ 正式回测引擎。
- 多机高可用、消息总线、机构级审批、安全和审计体系。

## 14. 设计来源与外部基准

### 14.1 本地设计来源

- [历史原始需求与策略素材](./如何搭建自己的量化交易系统-GitHub原版副本.docx)
- [3.0 结构化设计快照](./如何搭建自己的量化交易系统.docx)
- [现有扩展 Markdown](./如何搭建自己的量化交易系统.md)

三份材料发生冲突时，以本稿明确的当前需求为准：个人/小团队、中低频、Tushare + RD-Agent + Qlib、完整模拟盘与推荐提醒、永不自动实盘交易。

策略与研究内容的覆盖关系如下：

| 来源内容 | 本稿落点 |
| --- | --- |
| 原版 Word 的双均线、N 日/箱体突破、复合 Swing、布林带/RSI/乖离率均值回归 | 6.2、6.4.2 |
| 原版 Word 的动量、反转、价值、质量、成长、低波动、流动性、单因子/多因子、指数增强 | 6.2、6.4.1 |
| 两份 Word 的配对/协整/Kalman、分钟均值回归、RF/XGBoost/LSTM、行业轮动和事件驱动 | 6.2、6.4.3、6.4.2 |
| 结构化 Word 的指数增强、全市场行业中性、核心—卫星、逆波动/风险平价/目标波动 | 6.4.1、6.9、6.10 |
| 两份 Word 的下一 Bar、1min/5min、TWAP、VWAP、参与率和跨日拆单 | 5.2—5.5、6.5 |
| 扩展 Markdown 的 ETF 配置、个人股票核心、周频战术、策略分类、八插槽、预测语义和重复经济暴露 | 6.1—6.4、6.8、6.10 |
| 扩展 Markdown 的 RD-Agent 模板扩展、新策略草案、独立复算、消融/负对照和研究生产隔离 | 4.1—4.2、6.6—6.8 |

没有迁入的内容不是遗漏：固定资金规模、股票数、均线/止损/因子权重、固定收益和回撤目标、统一 60/120/504 日门槛、固定三日 VWAP/时段、券商写入（默认关闭的 QMT 沙箱插件除外）、第二套 C++ 引擎以及机构级审批/认证设施，均因缺乏普适金融依据或超出个人系统边界而被明确排除。

### 14.2 官方技术与规则来源

- [Tushare A 股日线接口与字段单位](https://tushare.pro/document/1?doc_id=27)
- [Tushare 分钟数据说明](https://tushare.pro/document/1?doc_id=234)
- [Tushare ETF 实时分钟字段单位示例](https://tushare.pro/document/2?doc_id=470)
- [Qlib Workflow 官方文档](https://qlib.readthedocs.io/en/stable/component/workflow.html)
- [Qlib Recorder 官方文档](https://qlib.readthedocs.io/en/stable/component/recorder.html)
- [Qlib Account/Position API](https://qlib.readthedocs.io/en/latest/reference/api.html)
- [RD-Agent 官方仓库](https://github.com/microsoft/RD-Agent)
- [GIPS 外部现金流与时间加权收益口径](https://www.gipsstandards.org/standards/gips-standards-for-firms/gips-standards-handbook-for-firms/)
- [深交所关于部分证券当日回转交易的规则说明](https://www.szse.cn/disclosure/notice/general/t20190111_564052.html)
- [上交所债券 ETF 业务指南](https://etf.sse.com.cn/ruleguide/guide/c/5287915.shtml)
- [上交所黄金 ETF 业务指引](https://www.sse.com.cn/lawandrules/sselawsrules2025/fund/trading/c/c_20250519_10779388.shtml)

外部规则可能变化。实现时必须保存实际采用的接口版本、规则生效日期和配置版本；本节链接用于核验设计方向，不代替运行时规则更新。

## 15. v1.1 策略扩展定稿与变更边界

本文冻结为个人投资者或 1—5 人小团队量化平台 v1.1 目标设计。唯一主线是：Tushare 数据 → RD-Agent 改进模板或自主研究新策略 → Qlib 探索 → 冻结候选 → 独立复算与 Qlib 正式回测 → 隔离前向模拟 → 人工批准 → 唯一账户综合推荐 → 主模拟账本 → 月/周/日/分时提醒。系统永久不具备券商写入、自动下单、自动撤单或真实资金控制权限；默认关闭的 QMT 沙箱插件不属于主线，不改变这一承诺。

v1.1 已将两份 Word 和扩展 Markdown 中适合当前股票/ETF个人系统的策略、因子、账户分配政策、执行政策和 RD-Agent 新策略研究能力纳入完整目录，并用 `catalog_role` 与 `implementation_tier` 防止它们混成一批独立资金账户。`foundation` 必须可运行；任何已实现且可获配资本的 `alpha_template`、部署型 `recipe_variant` 或可投资 `baseline` 都必须具备完整 `StrategySpec`、适用基线/准入门、研究/正式回测/拒绝/隔离模拟路径。条件项、模型挑战者和研究入口必须保留实验、父模板或明确阻断。设计完整不等于所有策略已经实现、通过证据门、适合个人或具有正收益，也不要求它们同时参与推荐；股票多空配对在当前版本明确只作离线研究。

后续实施可以按 12.6 节分阶段完成，但不能再把目标缩回“一个示例策略”。RD-Agent 日后提出新的经济假设时，只要继续使用 `NewStrategyProposal → 人工模板设计确认 → 研究并冻结 StrategySpec → candidate → 独立复算 → 正式回测硬门 → 自动隔离 paper → 前向证据门 → 人工推荐批准` 的合同，就属于本设计已经覆盖的正常扩展，不需要为了每个新策略重写设计稿。

固定收益率、回撤、股票数、参数、权重、资金规模和观察天数都只能是版本化配置或研究假设，不是架构常数。

从本版起：

- 实现类细节、页面布局、任务拆分、策略参数和目录内新配方进入实施文档、`StrategySpec` 或 backlog，不再反复改写本设计。
- 市场费税、交易规则、Tushare 接口和个人权限变化优先创建带生效日期的运行配置版本；只有它们改变了本文的原则性语义时才升级设计稿。
- 只有产品范围变化，或发现会改变数据、回测、模拟账本、投资证据、账户动作或非实盘边界的原则性错误时，才升级为后续设计版本。
- 当前项目实现是否符合本文，应由代码、配置和验收测试逐项证明；本文定稿不等于当前代码已经完成。

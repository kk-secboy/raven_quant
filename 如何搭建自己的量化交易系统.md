# 如何搭建自己的量化交易系统

> 个人中低频量化研究、推荐与模拟交易最终架构与产品基准

| 项目 | 定稿内容 |
| --- | --- |
| 方案版本 | 4.4（最终架构与产品基准） |
| 批准日期 | 2026-07-16 |
| 文档载体 | Markdown 权威稿 |
| 适用边界 | A 股及境内场内 ETF；生产行情、财务、成分和研究特征仅 Tushare，官方控制/参考数据版本化隔离；日线与分钟线；研究、回测、推荐、审批、个人影子账户与完整模拟交易 |
| 技术基准 | Qlib + RD-Agent（固定并经兼容验证的版本） |
| 唯一排除 | 券商连接、真实账户、真实资金与真实委托必须按权限拆分治理：不得提交真实委托、撤单、资金划转、券商账户控制或真实资金托管；QMT 默认关闭；只允许手工/CSV，及用户主动启用、权限可证明为只读的账户状态适配器用于建议对账 |

> **权威声明**
>
> 本 Markdown 是 4.4 最终架构与产品基准，冻结系统范围、唯一主线、责任边界、投资治理、数学统计原则、金融语义、风险优先级、证据等级和验收机制；Qlib 与 RD-Agent 决定技术实现基准。市场规则、税费、数据接口、软件版本、账户政策、模型参数和通知渠道属于版本化运行配置，其变化不构成重新设计系统。只有系统边界、唯一主线、证据等级、风险优先级或实盘授权发生变化时，才允许升级本文主版本。根目录同名 DOCX 是 3.0 发布快照，不再作为日常修改源。

> **最终稿治理**
>
> 本文定版后，普通开发、缺陷修复、参数调优、页面调整和策略新增不得修改已经冻结的架构原则。外部规则或运行配置变化必须创建带生效日期的新版本，完成影响分析、回归、审批和受影响结果重算，禁止原位覆盖历史记录。设计完成、代码实现完成和投资证据成立是三个不同状态；模块勾选框只有在对应设计、实现、测试和验收证据全部满足时才能勾选。

## 1. 基准、目标与唯一主线

- [ ] 本模块已实现、测试并通过验收

本产品是使用真实市场数据运行的个人投资辅助系统：完成研究、正式回测、买入/卖出/持有推荐、组合风险控制、模拟订单、模拟成交、虚拟持仓、净值跟踪以及用户实际持仓的手工影子对账；唯一不执行券商实盘下单，也不托管真实资金和券商账户。

| 优先级 | 基准 | 决定内容 |
| --- | --- | --- |
| 1 | 本 Markdown | 产品目标、策略、风险、范围和验收 |
| 2 | Qlib / RD-Agent | 数据表达、研究、模型、组合、回测、执行与实验的技术基准 |
| 3 | 项目自研 | 仅补足上游无法满足的 Tushare、治理、A 股、持久模拟、配对双腿与运维能力 |

> **唯一主线**
>
> Tushare 不可变快照 → Qlib 数据集 → RD-Agent 研究 → 独立复算与准入 → Qlib 正式回测 → 策略审批 → 买卖推荐 → 核心/卫星分配 → 统一模拟交易/个人影子账户对账 → 表现复核与策略生命周期

“唯一主线”同时约束研究晋升和正式生产，但不表示每次日常运行都要重新执行 RD-Agent。两段链路的固定关系如下：

- **研究与晋升链**：项目冻结月度、周度、日度或 1min/5min 模板、数据、标签、成本、风险、样本边界和试验预算 → RD-Agent 提出并实现因子/模型候选，同时调用 Qlib 进行探索实验和迭代反馈 → 候选冻结 → 项目从锁定快照独立复算 → Qlib 在未参与候选选择的确认区间执行正式样本外回测、执行回放和 Recorder 固化 → 统计、成本、容量、风险和审批门 → 前向模拟 → 形成已审批的不可变策略版本。
- **正式生产链**：认证数据快照 → 已审批且已启用的冻结 Qlib 因子、模型和策略版本 → 项目组合/账户风险治理 → 唯一综合推荐 → 模拟订单、成交、账本、通知和归因。正式运行只读取已审批制品，不让 RD-Agent 在盘中、日度、周度或月度生产批次中临时改写策略。
- **反馈闭环**：正式回测、前向模拟、模型漂移、成本偏差和推荐质量形成新的研究任务，再由 RD-Agent 调用 Qlib 研究下一版挑战者；新版本未重新完成独立复算、正式 Qlib 回测、前向模拟和审批前，不得替换当前正式版本。

因此，RD-Agent 调用 Qlib 的自动研究是唯一受治理的自动挑战者入口；Qlib 在完整流程中承担两次不同角色：第一次作为 RD-Agent 研究循环中的实验与反馈引擎，第二次作为候选冻结后的独立正式复测、执行验证和生产计算引擎。月度、周度、日度和分时策略都遵守同一机制，差别只在数据频率、标签、决策时点、执行合同和准入证据。

### 1.1 个人投资授权

- [ ] 本模块已实现、测试并通过验收

| 授权项 | 定稿值 | 执行含义 |
| --- | --- | --- |
| 验收名义本金 | 人民币 500 万元 | 作为统一回测容量和主模拟账户初始资金，不等于用户实际可投资资产 |
| 投资市场 | A 股及境内场内 ETF | 数据源只使用 Tushare |
| 频率 | 中低频 | 系统支持月、周、日、1min/5min；月频核心和日度风控为主线，周频、Swing 和分钟卫星只在独立准入并启用后参与综合推荐 |
| 最大组合回撤 | 10% | 达到 10% 时停止新增买入推荐，并将模拟组合转为退出状态 |
| 核心目标 | 扣费后长期正收益并取得基准超额 | 不承诺每笔、每月或每年盈利 |
| 决策方式 | 系统建议、用户决定 | 系统输出依据和风险，不替代用户最终判断 |
| 人工执行边界 | 版本化 `human_execution_policy` | 系统必须证明建议数量、处理时限和操作复杂度适合个人手工执行；超出能力时只进入模拟盘 |

> **产品目标**
>
> 系统必须证明策略在锁定样本外、完整成本、不同市场阶段和持续模拟交易中具有正期望，再进入完全认证推荐。研究准入后、前向证据不足时只能输出明确标记的 `forward_observation` 观察建议并进入模拟交易，不能映射为个人影子账户的正式投资建议。不能满足准入线时，正确结果是保持现金或不推荐，而不是强行给出买卖信号。

系统只服务于用户本人及其本人控制的模拟账户和个人影子账户。任何向亲友、客户、社群或公众提供个性化证券建议、代管组合、收费服务或公开分发账户级推荐的行为，均超出本文授权范围，必须另行完成法律合规、数据许可、隐私、安全和产品责任评审。不得因为系统未自动下单，就把对外提供个性化投资建议视为仍属于个人自用。

### 1.2 系统必须输出的结果

- [ ] 本模块已实现、测试并通过验收

- 系统必须具备月度、周度、日度和 1min/5min 的策略调度、回测、模拟与推荐能力，但运行时只启动“到期、已审批且已启用”的策略实例。首期主线至少包含月频核心、每日风险监控和 5min 执行能力；周频策略、日度 Swing 和独立分钟卫星不得为了凑齐频率而强制启用。
- 内部计算不等于新买卖建议，更不等于用户通知。没有目标、动作、风险或执行状态的实质变化时，只更新检查时间和审计记录，不重复生成推荐或提醒。
- 各策略、各频率的原始信号分别保存和展示；只有经过资金预算、风险约束、当前持仓和账户政策净额合并后的唯一账户级综合推荐，才能进入模拟订单或映射到个人影子账户。
- 每条综合建议包含证券、动作、当前权重、最终目标权重、各频率贡献、冲突处理、建议执行窗口、原因、风险提示、策略版本和失效条件。
- 每次推荐同步进入统一模拟交易，记录计划、订单、成交、未成交、现金、持仓、成本、NAV、回撤和相对基准表现。
- 用户可选择把推荐映射到个人影子账户；账户状态可以来自手工、CSV 或经用户主动启用的只读适配器。系统展示建议数量、实际持仓、已确认人工成交、未执行差异和组合偏离，但不得向券商发单、撤单或划转资金。
- 控制台同时显示回测成绩、样本外成绩、模拟成绩和当前推荐，禁止把回测收益冒充模拟或真实收益。

### 1.3 资本口径与个人影子账户

- [ ] 本模块已实现、测试并通过验收

系统必须分开保存以下资本口径，禁止用一个“500 万元”字段同时代表研究、模拟和用户实际资产：

| 口径 | 定义 | 用途 |
| --- | --- | --- |
| `acceptance_notional` | 固定 500 万元 | 回测容量、成本压力、整手和准入横向比较 |
| `simulation_initial_capital` | 主模拟账户初始资金，默认 500 万元 | 前向模拟、回放、账本和系统验收 |
| `account_current_nav` | 指定模拟账户或个人影子账户的当前净值 | 生成当期建议数量、现金需求和目标持仓 |

个人影子账户不是由系统控制的券商真实账户，而是用户授权提供的只读投资状态。它必须支持手工录入或 CSV 导入现金、持仓、成本、可卖数量、人工成交、入金、出金、分红和其他公司行动；也可以接入默认关闭的 `broker_read_only_adapter`，只读取账户余额、持仓、可卖数量、当日成交和未完成委托。每次导入或同步必须保存来源类型、来源标识、权限证据、时间、水位、哈希和审计记录。只读适配器不得拥有委托、撤单、资金划转、银证转账、修改账户或查询无关敏感信息的权限；无法从接口、令牌范围和运行时网络策略证明为只读时不得启用。系统不得声称影子账户等同于券商最终结算结果；状态未达到策略新鲜度要求时，只输出目标权重和估算数量，并明确标记“待确认”。

### 1.4 个人投资政策与账户级约束

- [ ] 本模块已实现、测试并通过验收

用户必须为个人影子账户建立版本化投资政策，至少包含可投资证券类型、白名单/黑名单、禁止行业、单票/单行业上限、最低现金、最大总仓位、最大换手、最大可接受回撤、最低流动性、计划用款日期和税费假设。账户级约束只能比已审批策略更严格，不得放宽策略准入、市场规则、成本、容量或风险硬门。

推荐服务先生成策略目标，再叠加个人投资政策和当前账户状态。因个人限制无法达到模型目标时，系统应保留原始策略目标、约束后的可执行目标和未满足原因；不得为了适配个人账户重新训练模型或篡改策略历史。个人投资政策变更必须创建新版本，只影响生效日后的建议。

### 1.5 个人投资目标、风险能力与政策基准

- [ ] 本模块已实现、测试并通过验收

个人投资系统不能只用“500 万元验收本金”和“10% 最大回撤”代表用户的真实投资目标。用户在启用个人影子账户正式建议前，必须建立版本化的个人投资声明，至少包含：

- 实际可投资资产、应急资金和不得投入量化账户的资产。
- 投资期限、计划用款日期、预期入金、出金和最低可用现金。
- 名义收益目标、扣除通胀后的实际收益目标，以及不承诺收益的明确说明。
- 最大可承受人民币损失、最大可接受回撤、预期恢复期限和连续亏损容忍度。
- 风险承受意愿与客观风险承担能力；两者不一致时采用更严格者。
- 允许的资产类型、策略类型、持有期限、换手、集中度和人工干预规则。
- 用户无法及时处理盘中提醒、节假日、出差或系统不可用时的降级政策。

个人影子账户必须绑定一个版本化的账户级政策基准 `policy_benchmark_version`。政策基准根据长期目标资产配置确定，可以由多个全收益指数和现金收益组成；它不同于单个策略的研究基准。账户整体的超额收益、风险、归因和是否值得继续运行，必须相对于该政策基准评估，不能只与沪深 300 或各策略自己的基准比较。

个人投资声明和政策基准至少每年、重大资产变化、用款计划变化或风险承受能力变化时复核。变更只影响生效日后的建议，不得回写历史推荐或绩效。

### 1.6 人工执行能力与账户状态新鲜度

- [ ] 本模块已实现、测试并通过验收

本系统不依赖券商连接，且永不执行真实委托；即使用户启用可证明为只读的账户状态适配器，“系统可以生成订单计划”也不等于“个人可以及时、准确地手工执行”。每个个人影子账户必须绑定版本化 `human_execution_policy`，至少包含：

- 单日最多处理证券数、单批最多操作数、单笔最小金额、允许的交易时段和用户通常可响应时间。
- 用户能够处理的普通调仓、紧急退出、跨日未完成订单和部分成交数量上限。
- 建议送达后最晚确认时间、未确认事件升级规则、出差/休假/网络中断时的降级政策。
- 是否允许使用券商客户端的手工批量导入功能；即使允许，也只能导出订单计划文件，不得由本系统自动提交。

每个策略除了通过金融和系统准入，还必须通过人工可执行性验收。若目标持仓数、当日交易证券数、切片数量、建议有效期或所需响应速度超过个人政策，系统必须将该策略或本批建议标记为 `simulation_only`，不得映射为个人影子账户的正式数量建议。指数增强等高持仓策略可以保留完整模拟证据，同时为个人账户生成受约束的低复杂度替代方案；替代方案属于独立策略版本，必须重新回测和准入，不能临时删减持仓后沿用原策略成绩。

个人影子账户必须记录 `account_state_source`、`account_state_asof`、`account_snapshot_age`、`unconfirmed_order_count`、`cash_confidence`、`position_confidence`、`sellable_quantity_confidence` 和 `read_only_permission_evidence`。账户快照过期、存在未确认成交、现金或可卖数量不确定时：

- 月、周、日建议只能展示目标权重、估算数量和待确认风险，不得声称为精确可执行数量。
- 依赖即时账户状态的分钟新开仓建议不得映射到个人影子账户；模拟账户仍按独立执行合同运行。
- 风险退出仍生成最高优先级提示，但必须明确实际可卖数量未知，并要求用户先确认账户状态。
- 账户重新确认后创建新快照和新建议版本，不得覆盖旧建议。

### 1.7 投资决策优先级

- [ ] 本模块已实现、测试并通过验收

当收益目标、风险目标、账户限制和执行条件发生冲突时，系统必须按以下固定优先级处理，不得由后运行的模块覆盖前级硬门：

1. 法律、数据许可、非实盘授权和证券市场规则。
2. 数据可得性、PIT 血缘、时间语义、账本完整性和模型输入可信度。
3. 用户人民币损失上限、计划用款、最低现金、账户状态新鲜度和紧急风险退出。
4. 停牌、涨跌停、T+0/T+1、申报单位、容量、流动性和人工可执行能力。
5. 账户级总风险、核心/卫星预算、底层暴露、相关性和回撤状态。
6. 策略基准、跟踪误差、换手和策略模板约束。
7. Alpha、买点、收益预测和交易时机。

低优先级目标不能补偿高优先级硬门失败。发生冲突时必须保留原始目标、被覆盖目标、最终目标和覆盖原因。

### 1.8 个人可用首版与最终范围

- [ ] 本模块已实现、测试并通过验收

本文描述的是最终目标架构，不要求个人投资者一次建设全部策略。首个可用版本 `personal_mvp` 只要求完成：

- Tushare 日线/PIT 可行数据、证券规则、复权和不可变 Qlib 数据集。
- 一个 ETF 资产配置核心、一个股票核心策略及其简单基线。
- 正式样本外回测、完整成本、容量、月度推荐和每日风险检查。
- 最小“目标 → 订单计划 → 模拟成交 → 现金/持仓/NAV → 日终对账”闭环。
- 个人影子账户的低频持仓快照、建议差异和人工成交确认。
- 日报、周度风险复核和月度绩效归因。

周频、Swing、实时分钟、分钟独立策略、配对、复杂 ML、完整税务批次和高级归因按后续阶段逐项增加。未完成后续模块不得阻塞 `personal_mvp` 的研究和模拟使用，但系统不得据此宣称已完成“除实盘外全部功能”。

正式建设顺序固定为：

0. 数据权限、接口能力、PIT 可恢复率、运行成本和技术版本的可行性验收。
1. `personal_mvp`：市场规则、日线数据、ETF 资产配置、一个股票核心、日度风险门和最小模拟闭环。
2. 多频率调度和综合推荐、可选周频模板、Swing、全市场行业受控多因子、组合风险、绩效归因和前向观察。
3. 完整统一模拟交易、个人影子账户、税务与估值、告警和恢复。
4. Tushare 实时分钟、低频目标的 5min 执行、T+0 ETF 分钟均值回归、股票隔夜均值回归和通过准入的 ML 信号。
5. 研究型卫星：配对/统计套利。

卫星策略必须复用同一套准入、回测、审批、分配和模拟链。

### 1.9 家庭资产、多账户和系统外暴露边界

- [ ] 本模块已实现、测试并通过验收

个人投资风险不能只观察一个量化子账户。用户必须声明本系统覆盖范围是“单一量化账户”还是“家庭/个人总资产视图”，并记录版本化 `household_exposure_policy`：

- 单一量化账户模式下，系统只对导入的模拟账户或个人影子账户负责；报告必须明确未纳入其他券商账户、基金、现金、房产、负债、员工持股、限制性资产和未来用款，禁止把子账户风险冒充用户总资产风险。
- 家庭/个人总资产视图允许用户手工录入、CSV 导入或经单独批准的只读聚合工具提供其他账户和不可交易资产的汇总暴露，只用于资产配置、集中度、流动性和人民币损失评估。系统不得控制券商、银行或任何真实账户；除本设计定义的默认关闭、可证明只读的账户状态适配器外，不得直接连接真实账户系统。
- 同一证券、同一发行人、同一行业、同一底层指数或高度重合 ETF 在不同账户中的暴露必须合并观察。无法获得穿透信息时使用保守代理和陈旧度标记，不得声称已经完成精确总资产风险计算。
- 个人影子账户的精确订单建议仍只基于所选账户的可用现金、可卖数量和未完成订单；家庭汇总暴露可以收紧目标和风险预算，但不得虚构跨账户调拨或可执行数量。

### 1.10 投资目标、负债现金流与资金桶

- [ ] 本模块已实现、测试并通过验收

系统不能只回答“买什么”和“收益是否超过基准”，还必须回答个人资金目标是否有足够实现概率。每个个人投资声明必须建立版本化 `goal_liability_contract_version`，把资产分为至少三个互不混用的资金桶：

| 资金桶 | 目的 | 默认约束 |
| --- | --- | --- |
| 应急与近期用款 | 覆盖应急资金和未来 0–2 年确定用款 | 不进入高波动 Alpha；按可用性、期限和本金损失容忍度管理 |
| 中期目标 | 覆盖 2–5 年购房、教育、创业或其他计划支出 | 使用目标日期、金额、优先级和可延后程度形成负债现金流 |
| 长期增值 | 退休、长期财富增长和可承受波动资金 | 才允许承担经风险声明批准的核心/卫星风险 |

每个目标至少记录目标金额、目标日期、币种、通胀口径、优先级、最低可接受完成比例、可延后期限和允许使用的资金桶。系统必须基于外部入金、提款、税费、通胀、资产收益和压力情景报告：

- 目标资金缺口、目标成功率区间、最差合理情景和达到目标所需的保守收益率。
- 提款阶段的顺序风险、流动性覆盖月数和被迫在回撤期卖出的概率。
- 当前策略组合相对简单 ETF/现金方案是否提高目标效用，而不只是提高回测年化收益。
- 当目标成功率下降、近期用款临近或人民币损失上限收紧时，优先增加现金和降低风险；不得通过提高预期收益假设掩盖资金缺口。

目标成功率必须绑定路径模型、期限、通胀、入金/提款和参数不确定性；无法建立可信概率模型时，只报告历史与压力情景下的资金缺口范围，不得输出伪精确概率。

## 2. 固定边界与禁止事项

- [ ] 本模块已实现、测试并通过验收

- 当前生产行情、财务、成分和研究特征只使用 Tushare；权限不足时阻断相关运营验收，不在同一数据集或策略版本中静默切换其他商业数据商。交易所、监管机构、统计部门、中央银行、外汇管理、登记结算和指数发布方的公开资料，允许通过独立且版本化的 `control_reference_provider` 用于市场规则、税费、交易日历、官方基准核验、汇率、通胀、利率和公司行动控制面；它们不得未经新数据源版本和重新准入静默混入 Alpha 特征、成交价或策略收益。
- 数据访问必须通过项目标准化 provider 契约，禁止业务代码直接依赖 Tushare 私有字段。系统必须保留版本化 `provider_exit_plan`：覆盖接口停用、授权终止、长期故障、字段变化和历史数据无法重建时的只读运行、暂停新增风险、制品导出及迁移演练。引入替代数据商属于新的正式数据源版本，必须完成字段映射、PIT、单位、复权、基准、回测差异和重新准入，不能作为运行时自动故障切换。
- 支持基于日线数据的月度、周度、日度决策，以及分钟线研究、买卖推荐、执行回放和模拟成交；不做 Tick、Level-2、逐笔或毫秒级高频。
- 除实盘外全部建设；页面、调度器、回测、推荐、回放与模拟任务均不得向 QMT 或任何券商发出委托、撤单、改单、资金划转或账户控制请求。
- 允许用户手工录入、CSV 导入或主动启用可证明为只读的适配器，获取实际持仓、现金、可卖数量、当日成交和未完成委托用于影子对账。只读访问必须与研究、推荐和执行服务隔离；关闭适配器不影响模拟主线。
- QMT 仅作为默认关闭的可选插件附录保留，不进入主线验收。
- 不建设独立 C++ 回测引擎。
- Qlib/RD-Agent 已有等价能力必须迁入、包装或删除重复实现；新旧路径不得同时作为生产路径。
- 历史策略版本、模拟账户、成交、NAV 与审计记录保持不可变。
- 任何未经准入的研究结果只能标记为研究候选，不得显示成正式买卖推荐。

本文所称唯一排除是任何真实交易和账户控制。系统可以在用户主动授权、权限范围可验证、默认关闭和网络隔离的条件下读取账户状态，但不得提交或修改委托、撤单、资金操作、风险设置或任何券商账户状态。只读数据只进入本地个人影子账户；适配器不可用或证据不足时自动退回手工/CSV 和建议降级，不得扩大权限。

## 3. 责任边界与系统架构

- [ ] 本模块已实现、测试并通过验收

| 能力域 | 技术责任 | 项目约束 |
| --- | --- | --- |
| 数据/特征 | Qlib Dataset、表达式、处理器 | Tushare 下载、不可变快照、PIT 血缘、Qlib 转换 |
| 自动研究 | RD-Agent 候选因子/模型、实验迭代、挑战者编排 | 独立复算、统计准入、审批和审计 |
| 组合/风险 | Qlib `PortfolioOptimizer`、`ShrinkCovEstimator` 等基础能力 | 项目正式 QP、A 股约束、现金缩放、核心/卫星上限、约束复核与风险状态 |
| 回测/执行 | Qlib `BaseStrategy`/`WeightStrategyBase`、`BaseExecutor`/`SimulatorExecutor`、`Exchange`、分析与 Recorder | 按证券品种和日期执行 T+0/T+1、停牌、涨跌停、申报单位、费用与流动性 |
| 模拟交易 | Qlib 生成目标、订单计划与回测执行证据 | PostgreSQL 持久现金、订单、成交、持仓、NAV、事件与幂等 |
| 个人影子账户 | 项目账户与对账能力 | 手工/CSV 或可证明只读的适配器导入账户状态；生成建议差异，不得发单或控制账户 |
| 绩效/归因 | Qlib 标准分析 + 项目账户核算 | 全收益基准、现金收益、TWR/XIRR、费用和风险归因 |
| 配对 | Qlib 保存 Dataset、Recorder 与标准指标 | 协整/Kalman、卖空资格、借券成本与双腿原子成交 |
| 运维/UI | 项目能力 | 调度、告警、备份恢复及统一控制台 |

Qlib 与 RD-Agent 必须使用经验验证的固定版本，并记录版本与提交标识。任何升级都要重新运行导入、制品兼容、回测结果和量化回归。

### 3.1 运行责任分工

- [ ] 本模块已实现、测试并通过验收

系统不是由 RD-Agent 或 Qlib 单独负责运行，而是由项目调度器组织 Tushare、Qlib、RD-Agent、风险治理和模拟交易共同完成。调度器必须区分研究任务和正式生产任务：研究任务由 RD-Agent 调用 Qlib 生成并迭代挑战者；正式生产任务只运行已经冻结、审批且准入通过的 Qlib 策略版本。

| 运行工作 | 主要负责人 | 明确边界 |
| --- | --- | --- |
| 按配置启动月、周、日、1min/5min 到期任务 | 项目调度器 | 未启用策略跳过；已启用策略按交易日历、时区、重试、超时、幂等和错过窗口规则 fail closed |
| 下载日线、历史分钟和实时分钟 | Tushare + 项目数据适配器 | 权限、限流、批次、单位换算、完整性和不可变落盘由项目负责 |
| 因子、模型分数和基础权重 | Qlib | 只能读取已通过质量门的数据；项目正式 QP 和风险层不得被绕过 |
| 正式回测、执行回放和标准指标 | Qlib | 使用固定 `BaseStrategy`/`WeightStrategyBase`、`BaseExecutor`/`SimulatorExecutor`、`Exchange`、`qlib.workflow.R` 与 Recorder |
| 新因子、新模型和挑战者研究 | RD-Agent 调用 Qlib | 只在周度/月度或人工研究任务中运行；不得进入盘中交易链路 |
| 候选独立复算、反过拟合和准入 | 项目治理层 | RD-Agent 不能修改数据边界、试验预算和准入线 |
| 最终买卖推荐和 `NO_ACTION` | 项目推荐服务调用 Qlib 结果 | 只有已审批且未暂停的策略可以产生正式推荐 |
| 模拟订单、成交、现金、持仓和 NAV | 项目模拟交易系统 | PostgreSQL 持久记账；Qlib 提供目标和执行证据，不向券商发出委托、撤单、资金或账户控制请求 |
| 暂停、恢复、淘汰、告警和人工审批 | 项目风险治理层 | 风险门优先于策略信号，触发硬门时直接阻断 |

一句话分工：RD-Agent 是策略研究员，Qlib 是量化计算、回测和组合引擎，Tushare 是数据供应商，项目本身是总调度、治理、推荐和模拟交易平台。

## 4. 数据、频率与时间语义

- [ ] 本模块已实现、测试并通过验收

### 4.0 阶段 0：数据、权限与运行可行性

- [ ] 本模块已实现、测试并通过验收

在冻结任何正式策略股票池、样本跨度、分钟候选池或运行时间表之前，必须先完成阶段 0 可行性验收。每个计划使用的 Tushare 接口至少记录：

- 接口名称、权限等级、费用、授权期限、用途和再分发限制。
- 单次最大代码数、单次最大行数、调用频率、分页方式、可查询历史起点和字段单位。
- 日线、财务、成分、行业、公司行动、历史分钟和实时分钟的实测完整率。
- 请求延迟和数据到达延迟的 P50/P95/P99、失败率、重试后成功率和限流行为。
- 全量初始化、每日增量、历史修复和灾后重建所需时间、带宽、存储和人工操作。
- PIT 原生历史版本、公告重建、当前修订和不可恢复字段的覆盖比例。
- 股票实时分钟支持多代码而 ETF 实时分钟可能需要逐只请求等接口差异，并据此计算可支持的最大候选池。

阶段 0 必须产出版本化 `data_capability_matrix`、`permission_evidence`、容量预算和阻断清单。设计中的“候选池 300 只”“至少 504 日样本外”“全市场分钟能力”等只能是待验收目标；若实测权限、历史深度、延迟或 PIT 可恢复率不满足要求，必须缩小策略范围、降低证据等级或保持未启用，不得用技术推测代替运营能力。

- Tushare 原始响应按批次形成不可变快照；快照记录接口、参数、请求窗口、时间、校验和与权限证据。
- Qlib 数据集只从已冻结且通过质量门的快照生成，并记录快照血缘、构建配置、频率、日历和校验和。
- 原生频率为 `day`、`1min`、`5min`；`15min`、`30min`、`60min` 只由 Qlib 从分钟数据重采样，不增加下载线路。
- 月度、周度和日度策略都可以读取 `day` 数据，但必须使用不同的 `decision_frequency`、标签持有期、调仓日历和策略版本；不得把每天读取日线数据误称为每天调仓。
- 日线研究和分钟研究使用独立数据集与实验，禁止在同一策略版本中隐式混用。
- 任何信号最早在下一 Bar 成交；日线信号最早在下一交易日执行，禁止同 Bar 成交和未来信息。
- 股票池、行业、财务与退市状态必须采用 PIT 口径，包含历史退市证券，防止幸存者偏差。
- 每个 PIT 字段必须记录其历史版本可恢复等级：`native_vintage`、`announcement_reconstructed`、`current_revision` 或 `unavailable`。只有数据商原生历史版本或依据当时公告、成分变更和生效日期可复算的前两类数据，才能进入正式财务因子、股票池和基准回测；当前修订值回填历史只能用于研究敏感性分析，不得标记为正式 PIT 证据。
- 从现在开始保存不可变快照只能保证未来不被覆盖，不能自动证明过去任一日期的数据版本可恢复。每个数据集必须报告 PIT 覆盖率、近似重建比例、不可恢复字段和对结果的敏感性。

| 字段 | 含义 | 允许值/规则 |
| --- | --- | --- |
| `signal_frequency` | 生成信号的数据频率 | `day` / `1min` / `5min` / Qlib 重采样频率 |
| `decision_frequency` | 正式重新计算目标的频率 | `month` / `week` / `day` / `1min` / `5min` |
| `rebalance_frequency` | 允许改变账户目标仓位的调仓频率 | 必须由模板冻结，可以低于信号检查频率 |
| `monitor_frequency` | 风险、数据和目标有效性的检查频率 | 可高于调仓频率；检查结果不变时不得制造新推荐 |
| `signal_period` | 信号观察与持有周期 | 策略版本固定，不能静默改变 |
| `execution_frequency` | 执行切片频率 | `1min` 或 `5min`，必须晚于信号可用时点 |
| `execution_contract_hash` | 完整执行语义的内容哈希 | 回测、回放、模拟批次必须一致 |

### 4.1 证券主数据与市场规则版本

- [ ] 本模块已实现、测试并通过验收

不得把“A 股/ETF”作为足够精细的执行分类。项目必须为每只证券按生效日期保存市场规则，并由回测、回放、模拟和影子账户建议共同读取：

| 字段 | 含义 |
| --- | --- |
| `asset_subtype` | 主板股票、科创板、创业板、北交所、股票 ETF、债券 ETF、黄金 ETF、跨境 ETF、货币 ETF 等 |
| `exchange` / `board` | 上交所、深交所、北交所及具体板块 |
| `settlement_cycle` | 当日买入份额的可卖规则；股票 ETF 通常 T+1，部分债券、黄金、跨境和货币 ETF 可 T+0 |
| `lot_size` / `price_tick` | 买入申报单位、零股卖出规则和最小价格变动 |
| `price_limit_rule` | 10%、20%、30%、5% 或无普通涨跌幅限制等按品种、板块、风险状态和日期生效的规则 |
| `ipo_rule` / `auction_rule` | 上市初期、集合竞价、盘后固定价格和临时停牌等特殊规则 |
| `rule_effective_from/to` | 规则生效区间，历史回测不得使用当前规则覆盖过去 |
| `market_rule_version` | 进入 `execution_contract_hash` 的不可变规则版本 |

任何缺少可验证市场规则的证券不得进入正式回测、推荐或模拟成交。交易所规则变化时新增规则版本，并重跑所有覆盖受影响日期或品种的回测、回放和容量验证，不修改历史规则记录。

### 4.2 历史分钟、盘中实时分钟与数据水位

- [ ] 本模块已实现、测试并通过验收

- Tushare 历史分钟数据用于研究、回测、收盘后补齐和执行回放；其盘后更新不能冒充盘中实时数据。
- 盘中 1min/5min 策略必须使用 Tushare 实时分钟接口并具备对应权限；权限、延迟或完整性不达标时，盘中策略保持 `NO_ACTION`，但历史研究和回放仍可运行。
- 前一交易日收盘后根据日线策略、流动性和持仓形成次日盘中候选池。盘中实时分钟默认只拉取全部持仓和候选池，不每 5 分钟扫描全市场；候选池默认不超过 300 只，扩大前必须通过限流、延迟和完整性验收。
- 实时分钟响应先写入带抓取时间和源接口标识的暂存批次；只有已结束且通过 OHLCV、时间戳、重复行和连续性检查的 Bar 才能进入信号计算。
- 历史分钟与实时分钟在收盘后必须逐 Bar 对账；差异超过策略容忍度时，当日盘中推荐、成交和 NAV 标记为待复核，阻断后续正式使用。
- 每个接口必须记录 `source_event_time`、`provider_available_at`、`fetched_at` 和 `watermark_at`。研究只能使用当时水位线之前已可得的数据，不得用后来补发或修订的数据改写历史决策。
- 历史回放、前向日线模拟和盘中实时模拟是三种不同运行模式，必须分别标记。盘后历史分钟数据可以复核盘中模拟，但不得冒充盘中实时输入。
- 日线推荐只有在所需日线、复权因子、停复牌、涨跌停和 PIT 参考数据达到策略规定水位后才能生成。迟到数据只能产生更正事件、风险标记和后续重算，不得静默覆盖已经发布的推荐。
- Tushare 是生产因子、行情和回测数据的唯一数据源，但交易日历、市场规则、公司行动、指数调整和关键异常必须使用交易所、监管机构或中国结算公开信息建立独立控制面核验。外部控制数据只用于验证、阻断和人工复核，除非建立新的正式数据源版本，否则不得静默替换 Tushare 数值进入因子或收益计算。
- 单一数据源的质量门必须包含跨接口恒等式、日线与分钟聚合一致性、价格与成交额数量级、复权连续性、指数成分权重和公司行动守恒检查。发现矛盾时保持原始证据并 fail closed，不得自动选择“看起来更合理”的值。

### 4.3 Tushare 字段与公司行动口径

- [ ] 本模块已实现、测试并通过验收

| 数据项 | 原始口径 | 系统标准口径 |
| --- | --- | --- |
| 日线 `vol` | 手 | 乘以 100 后存储为股；禁止直接作为 Qlib `volume` |
| 日线 `amount` | 千元 | 乘以 1,000 后存储为人民币元 |
| 分钟 `vol` / `amount` | 股 / 元 | 保持股 / 元，不再重复换算 |
| OHLC | 未复权价格 | 原始价永久保留，执行、涨跌停与账本只使用原始价 |
| `adj_factor` | 复权因子 | 原始因子永久保存；执行和账本使用原始价，收益使用相邻时点复权比率，研究价格必须固化锚点；不得使用未来公司行动 |
| 财务数据 | 公告日与报告期并存 | 最早在实际公告日之后可用；修订按当时可见版本进入新快照 |
| 行业/指数成分 | 纳入、剔除日期 | 按交易日 PIT 展开，禁止用当前成分回填历史 |
| 停牌/退市 | 状态与日期 | 停牌不成交；退市证券保留在历史股票池并按真实可交易阶段处理 |

数据与公司行动规则：

- 每个数据集必须检查主键重复、交易日缺口、OHLC 关系、负数、零成交量、异常跳变、跨源字段单位和日历一致性。
- 复权数学固定为：执行价和估值原始输入使用 `raw_price(t)`；锚定复权价使用 `adjusted_price(t, b) = raw_price(t) × adj_factor(t) / adj_factor(b)`；单期复权总收益使用 `raw_price(t) × adj_factor(t) / [raw_price(t-1) × adj_factor(t-1)] - 1`。锚点 `b`、复权因子快照和公式版本必须进入数据集哈希。
- 禁止把随查询结束日变化的前复权历史序列作为无版本的永久事实保存。任何依赖前复权价的因子必须绑定明确锚点或改写为只依赖相邻收益、滚动比率和当时可知复权因子的表达式。
- 现金分红、送转、拆并股、配股、合并、换股、代码变更、ETF 份额折算和退市现金结算等公司行动在模拟账本中调整现金、数量或证券身份；信号计算与模拟记账不得靠同一列复权价同时完成。
- 除权除息、送转、配股、合并、换股或份额折算生效前，所有受影响的未完成订单必须按冻结规则取消、调整价格和数量，或进入人工复核；不得沿用公司行动前的限价、数量或参考价继续模拟成交。股息默认进入现金，不得在账户账本中假设自动再投资；若基准或独立策略采用分红再投资，必须使用独立的基准/策略规则和下一可执行时点。
- 停牌或成交量为零时订单保持未成交；一字涨停禁止买入，一字跌停禁止卖出；其他情况按可用分钟 Bar、参与率和成本模型判断。
- 退市前只使用当时已公开信息；无法成交的退市持仓保持风险标记，按实际现金结算处理，不得用事后可知价格提前退出。

### 4.4 基准、现金与绩效数据口径

- [ ] 本模块已实现、测试并通过验收

- 策略基准优先使用包含现金分红再投资的全收益口径；价格指数只能作为辅助展示，不得单独用于超额收益、Information Ratio 和归因验收。Tushare 没有可直接使用的目标全收益序列时，项目从冻结的 PIT 成分、权重、价格和公司行动重建基准，并保存复算方法与校验证据。
- 每个基准必须声明 `benchmark_evidence_type`：`official_total_return_index` 表示由指数发布方提供并可验证的官方全收益序列；`synthetic_total_return_proxy` 表示项目依据 PIT 成分、权重、价格和公司行动重建的合成代理；`investable_benchmark_portfolio` 表示按真实可交易、费用、整手和再平衡约束构造的可投资基准组合。三者必须分别保存和展示，禁止把项目合成序列称为官方全收益指数。
- `synthetic_total_return_proxy` 必须固化除数/权重近似、自由流通口径、公司行动、再平衡时点、缺失处理和版本，并报告与可验证官方价格指数、公开基准或交叉复算结果的跟踪差异。无法证明指数公司专有除数调整和特殊公司行动处理完全一致时，只能标记为合成代理；误差超过策略阈值时不得用于正式超额收益和 Information Ratio 准入。
- `investable_benchmark_portfolio` 用于回答个人在相同市场、交易和成本约束下可获得的简单替代收益，不能替代研究基准；正式报告应同时展示策略相对研究基准和相对可投资基准组合的增量价值。
- 指数增强使用对应指数的 PIT 成分、权重和全收益基准；全市场行业受控多因子使用全 A 宽基全收益基准，沪深 300 仅作辅助比较。
- 账户整体使用个人投资声明中冻结的混合政策基准；策略基准、分配比较基准和账户政策基准必须分别保存，禁止用表现最好看的基准替换正式基准。
- 现金部分按版本化的现金收益率或无风险收益曲线计息；选择零收益必须作为显式压力情景，不能作为未说明的默认值。
- 绩效数据必须支持时间加权收益 `TWR`、资金加权收益 `XIRR`、已实现/未实现损益、股息和现金收益；存在外部入金或出金时不得直接用 NAV 比值代替投资收益。
- 基准成分、基准权重、分红、现金利率和收益计算规则均进入不可变绩效契约。

### 4.5 交易成本与冲击模型

- [ ] 本模块已实现、测试并通过验收

以下为 2026-07-16 的初始保守模拟基准，不是已经由真实成交证明的最终成本模型。税费规则、券商佣金或交易制度变化时，只新增 `cost_model_version` 并重跑受影响结果，不修改历史记录。

| 成本项 | 定稿基准 | 计算规则 |
| --- | --- | --- |
| 券商佣金 | 成交额 0.03%，单笔最低 5 元 | 买卖双边；允许账户配置覆盖，但不得低于真实合同 |
| 证券交易印花税 | 卖出成交额 0.05% | 仅股票卖出；ETF 等按品种规则配置 |
| A 股交易过户费 | 成交额 0.001% | 买卖双边 |
| 基础滑点 | 日线 5 bps；分钟 VWAP 2 bps | 初始保守代理；买入加价、卖出减价，必须说明是否已包含买卖价差 |
| 市场冲击 | `25 bps × sqrt(订单额 / 20日平均成交额)` | 初始经验公式；只有在确认未与基础滑点、价差代理重复计量后才相加，单边最高 100 bps |
| 成交量参与率 | 单 Bar 和全日均不超过 1% | 超过部分延迟到下一可执行 Bar，不得强行成交 |
| 融券情景成本 | 年化 8% | 只用于研究型股票多空模拟；不代表真实可借券 |
| 现金收益 | 版本化无风险/现金收益率 | 模拟现金和基准现金部分按日计提；零收益仅作为显式压力情景 |
| 分红与税费 | 按证券类型、持有期和生效规则版本 | 股票股息税、ETF 分红和份额折算进入账本，不得仅靠复权价体现 |
| 压力测试 | 基础成本 2 倍 | 正式准入必须仍满足收益、回撤和容量门槛 |

固定成交额门槛只用于形成初始候选池，不能代替容量验证。正式容量门必须根据目标持仓、1% 参与率、可执行分钟 Bar、最大执行天数、冲击成本和压力情景动态计算，并报告建仓天数、减仓天数和完全清仓天数。

成本模型不能永久停留在固定经验公式。模拟账户和个人影子账户必须保存模型价、建议执行窗口、用户确认成交价、同期市场 VWAP、参与率、波动率、价差和流动性分组，用实际偏差定期校准滑点与冲击模型。校准至少按证券类型、成交额分组、波动率、交易时段和买卖方向进行；样本不足时继续使用保守基准，不得为了提高历史收益下调成本。

成本模型校准必须使用时间上早于评估区间的成交样本，并建立独立版本和样本外验证。回测、执行回放、模拟盘和影子账户建议使用的成本版本不一致时，结果不得横向比较或进入正式准入。

成本契约必须分别保存显式费用、买卖价差代理、市场移动、瞬时冲击、永久冲击、人工延迟和未成交机会成本。模型必须区分开盘、连续竞价、午后、尾盘、买卖方向、证券类型、波动率和流动性分组；无法识别某项成本时使用保守合并项，但必须标记组成未知，禁止在多个字段中重复收费或重复扣减。

### 4.6 绩效指标数学契约

- [ ] 本模块已实现、测试并通过验收

所有正式回测、前向模拟、个人影子账户和准入结果必须绑定不可变 `metric_contract_version`。同名指标只有在契约完全一致时才能横向比较。契约至少冻结：

| 指标 | 固定定义要求 |
| --- | --- |
| 累计收益 | 使用日度净收益几何复利；禁止用日收益简单求和代替 |
| 年化收益 | 根据实际频率和有效日历年化；日频默认 252，月/周策略仍从日度 NAV 计算 |
| Sharpe Ratio | 使用策略净收益减版本化现金/无风险收益后的超额收益；同时报告算术和几何收益口径差异 |
| Information Ratio | 使用策略相对冻结研究基准的主动净收益均值除以主动收益标准差；基准版本和跟踪误差定义必须一致 |
| Alpha / Beta | 必须声明模型；默认只允许单基准回归 Alpha 或冻结的多因子风险模型 Alpha，不能把简单超额收益直接命名为 Alpha |
| 最大回撤 | 明确区分绝对 NAV 最大回撤、主动收益曲线回撤和政策基准相对回撤 |
| Tracking Error | 使用日度主动净收益标准差年化；ex-ante 与 ex-post 分开保存 |
| Sortino / 下行风险 | 冻结最低可接受收益、目标频率和下行偏差公式 |
| VaR / Expected Shortfall | 冻结置信水平、预测期限、估计方法和压力窗口 |
| `P(MDD > x)` | 冻结投资期限、路径生成方法、区块长度、波动聚集/跳空/相关性压力和置信区间 |

月频、周频、重叠持有期和分钟策略的统计检验必须使用有效独立事件数和依赖长度，不得仅改变年化因子。指标公式、基准、现金利率、估计窗口或路径模型变化必须创建新契约，并重算所有受影响结果。

### 4.7 收益、标签与特征物化数学契约

- [ ] 本模块已实现、测试并通过验收

每个正式数据集和策略必须绑定 `return_contract_version`、`label_contract_version` 与 `feature_materialization_contract_hash`，防止同名收益、标签或特征在研究、回测和实时运行中具有不同含义。

`return_contract_version` 至少冻结：

- 简单收益、对数收益、原始价格收益、复权总收益和基准主动收益的适用场景；账户 NAV 和投资绩效默认使用简单净收益几何复利，对数收益只能用于明确声明的统计模型。
- 价格时点、公司行动、现金分红、停牌、无成交、退市、缺失价格、跨日和跨午休处理。
- 无法成交证券的收益不能简单删除。必须保留当时持仓和可交易状态，并按执行与估值合同处理，防止只保留成功成交样本。

`label_contract_version` 至少冻结：

- `feature_cutoff_ts`、`decision_ts`、`label_start_ts`、`label_end_ts`、最早可执行时点、持有期和退出价格。
- 绝对/基准相对标签、成本前/成本后标签、重叠区间、退市证券、停牌证券和未成交样本的处理。
- 月、周、日、分钟及股票 T+1/ETF T+0 标签属于不同合同，不得仅修改年化因子或窗口参数后复用统计证据。

特征物化必须满足：

- 研究批处理、历史回放、前向模拟和盘中生产共用同一组件定义、输入契约和核心计算代码；只允许数据访问适配器不同，禁止为实时链另写一套隐含公式。
- 每份特征快照保存输入快照、组件版本、窗口、频率、处理器拟合区间、列顺序、缺失处理、代码哈希和数值环境。
- 固定历史切片必须同时运行离线构建与按事件时间逐步重放，特征、预测和目标权重在冻结容差内一致；超出容差时阻断正式使用。
- 数值性质测试至少覆盖：提高成本不能提高同一成交路径的净收益，收紧容量不能增加成交量，拆股/送转前后经济价值守恒，修改决策时点后的数据不能改变过去特征、信号或订单。

截面因子和预测推断必须以日期或独立事件为主要统计单位。IC/Rank IC 使用每日截面统计后再形成时间序列；回归标准误必须按时间依赖采用 HAC、日期聚类、双向聚类或预注册的等价方法。不得把同一天的证券数、分钟 Bar 数或重叠标签数当作相互独立样本夸大显著性。

### 4.8 本位币、汇率、通胀与固定收益风险契约

- [ ] 本模块已实现、测试并通过验收

账户本位币固定为人民币，所有非人民币底层暴露必须同时保存本地资产收益、汇率收益和人民币总收益。每个跨境 ETF、境外指数代理和外币目标必须绑定 `currency_contract_version`，至少冻结：

- 交易币种、底层资产币种、账户本位币、汇率报价方向、汇率来源、事件时间、发布可用时间和陈旧度。
- 境内 ETF 交易日历、底层市场交易日历、时区、节假日错位、隔夜信息窗口和没有同步收盘价时的估值规则。
- 人民币收益分解、汇率压力、底层市场休市但 ETF 继续交易时的折溢价风险，以及是否允许汇率对冲。未批准汇率对冲时不得把汇率暴露当作已消除。
- 跨境额度、申赎受限、IOPV/参考净值陈旧、持续溢价和停牌等失效条件；关键参考数据不完整时只能降级或 `NO_ACTION`。

名义收益与实际收益必须绑定 `inflation_contract_version`。通胀数据保存统计期、首次发布日期、修订版本、可用时间和来源；历史决策只能使用当时已发布版本。实际 TWR、实际 XIRR、实际政策基准和目标金额换算必须使用同一通胀契约，禁止用后来修订的 CPI 静默改写历史投资判断。

债券和货币 ETF 必须绑定 `fixed_income_risk_contract_version`，至少包含久期、修正久期、凸性、收益率曲线、信用利差、发行人/行业集中、底层持仓陈旧度和流动性。压力测试至少覆盖平行上移、变陡、变平、信用利差扩大、流动性折价和大额赎回。无法获得足够新的底层持仓或利率/信用风险数据时，只能使用保守代理、收紧上限或禁用依赖精确久期和信用风险的优化，不得把债券 ETF 简化为“低波动股票”。

## 5. 策略版本、研究和准入

- [ ] 本模块已实现、测试并通过验收

策略分类拆成三个正交字段，禁止继续用一个 `strategy_type=multifactor` 同时表示资产配置、趋势、均值回归和截面选股：

| 字段 | 允许值与含义 |
| --- | --- |
| `engine_type` | `single_leg_long`、`pair_relative_value`、`execution_algorithm`；决定账户、订单和执行引擎 |
| `strategy_family` | `asset_allocation`、`cross_sectional_alpha`、`trend`、`mean_reversion`、`relative_value`；决定经济假设、标签和准入类别 |
| `recipe_id` | `etf_asset_allocation`、`personal_stock_core`、`index_enhancement`、`full_market_multifactor`、`weekly_tactical_overlay`、`swing_trend`、`minute_mean_reversion_etf`、`minute_mean_reversion_stock`、`pair_stat_arb` 或受控 `custom`；决定具体冻结模板 |

每个不可变策略版本必须固化：

- 配方和配方版本、`engine_type`、`strategy_family`、`recipe_id`、信号数据频率、`decision_frequency`、`rebalance_frequency`、`monitor_frequency`、信号周期和执行频率。
- 风险参数、成本模型、执行窗口、切片、参与率、跨日规则与执行契约哈希。
- 指标数学契约、策略类别准入合同、市场状态分类和简单基线版本。
- 数据集与不可变快照血缘、Qlib/RD-Agent 版本及提交标识、候选代码哈希。
- 独立复算结果、Qlib 实验/Recorder 标识、样本内外边界、压力测试与审批证据。

### 5.1 项目研究组件库与基于 Qlib 的正式策略模板

- [ ] 本模块已实现、测试并通过验收

第一版文档提到的策略思想和常用指标原则上全部进入项目研究组件库，并由 Qlib Expression、Processor、Model、Strategy 和 Recorder 承载计算与证据；不得把每个指标都包装成一套独立正式策略。研究组件是可复用零件，正式模板是项目冻结的产品合同，决定零件可以放在哪个插槽、如何组合、怎样交易和如何验收。这些模板不是 Qlib 官方内置模板。

| 研究组件组 | 第一版内容 | 项目中基于 Qlib 的表达方式 | 可供哪些模板使用 |
| --- | --- | --- | --- |
| 趋势 | 双均线、均线结构、突破、ADX、MACD | Qlib Expression、因子处理器和方向/强度特征 | 周频战术调仓、Swing、ETF 配置、指数增强挑战者、受控 `custom` |
| 均值回归 | 布林带位置/宽度、RSI、乖离率、VWAP偏离、Z-score | 日线或分钟 Qlib Expression；严格标记频率、会话和下一 Bar | T+0 ETF 分钟回归、股票隔夜回归、Swing 买点过滤 |
| 风险与仓位 | 波动率目标、止损止盈、回撤、流动性、成交量参与率 | Qlib Strategy/Optimizer/Exchange 加项目 A 股约束 | 全部正式模板 |
| 股票因子 | 动量、反转、价值、质量、成长、低波动、流动性 | Qlib Dataset、Expression、Processor 和模型特征 | 个人低复杂度股票核心、指数增强、全市场多因子 |
| ETF 选择与配置 | 资产类别、跟踪指数、规模、成交额、跟踪误差、折溢价、动量、趋势、波动和相关性 | Qlib Dataset/Expression/Optimizer 加项目 ETF 主数据和交易规则 | ETF 资产配置、T+0 ETF 分钟回归 |
| 配对统计 | 相关性、协整、Kalman对冲比率、价差Z-score | Qlib Dataset/Recorder 加项目配对研究与双腿适配层 | 配对/统计套利 |
| 机器学习 | 线性模型、树模型、时序模型及其特征 | Qlib Model、Dataset、Workflow；由RD-Agent提出候选 | 作为已有模板的信号增强，不自动成为独立正式策略 |
| 轮动与事件 | 行业动量、资金变化、财务和公司事件 | PIT Qlib特征；保持研究状态直到另行批准 | 受控 `custom`，默认不进入首期正式模板 |

研究组件库至少保存 `component_id`、版本、`slot_type`、Qlib 表达式或模型入口、特征频率、窗口单位、输入字段、最早可知时间、参数范围、输出语义、测试、代码哈希和适用模板。日线、1min 和 5min 的同名指标是不同组件版本，不得共享参数或统计证据。新增或修改组件必须创建新版本，禁止原位覆盖历史定义。

系统必须实现以下基于 Qlib 的正式模板能力；“模板存在”不等于“策略已经批准运行”：

1. 首期个人必备核心模板：`etf_asset_allocation`（ETF 资产配置与选择）、`personal_stock_core`（默认 10–30 只、月度低换手、可手工执行的股票核心）。
2. 完整模拟核心模板：`index_enhancement`（指数增强，默认约 100 只）和 `full_market_multifactor`（全市场行业受控多因子）。兼容接口与验收名称固定为 `index_enhancement`（指数增强）。它们必须具备完整回测和模拟能力；只有通过人工执行能力门或建立独立低复杂度版本后，才能映射为个人影子账户正式数量建议。
3. 可选批准模板：`weekly_tactical_overlay`（周频股票/ETF 战术覆盖）、`swing_trend`（日度 Swing）、`minute_mean_reversion_etf`（T+0 ETF 分钟回归）、`minute_mean_reversion_stock`（股票 T+1 隔夜回归）。

`pair_stat_arb` 是 `engine_type=pair_relative_value` 的项目专用双腿策略模板；兼容验收语义为“`pair` 是项目专用双腿策略模板”。它复用 Qlib Dataset、表达式、Workflow/Recorder 和统一治理，但正式双腿回测与执行由项目专用引擎完成，不得称为 Qlib 原生策略模板。

可选批准模板必须完成开发、回测、模拟和启停能力验收，但在没有通过独立准入时保持 `runtime_enabled=false`。只有 `approval_status=approved` 且 `runtime_enabled=true` 的策略实例才能参与正式账户目标；已批准但暂不启用、前向观察、暂停或退役策略都不得贡献正式仓位。

每个模板必须冻结以下内容，RD-Agent 无权在一次研究活动中改变：

- 资产类别、PIT 股票池或 ETF 池、基准和标签定义。
- `feature_cutoff_ts`、`decision_ts`、`label_start_ts`、`label_end_ts`、信号频率、调仓频率、执行频率和下一 Bar 规则。
- 必填/可选插槽、每个插槽允许的组件类型、最大组件数和参数范围。
- 组合构造、现金与杠杆边界、成本、容量、回撤、市场规则和准入指标。
- 训练/验证/最终测试边界以及主要评价指标。

模板插槽固定为：

| 插槽 | 作用 | 组合规则 |
| --- | --- | --- |
| `eligibility_gate` | 可交易、流动性、上市时间、监管和数据质量硬门 | 不通过直接排除，不能用高分补偿 |
| `universe_dedup` | 对跟踪同一指数或底层持仓高度重合的候选去重 | 只在需要去重的模板启用；先于排名，不能把重复暴露伪装成分散化 |
| `direction_regime_gate` | 上涨/下跌方向和市场环境硬门 | ADX、放量、布林带宽度等无方向指标不能单独填充该槽 |
| `alpha_rank` | 在已合格证券中排序或预测预期收益 | 允许规则因子或 Qlib Model；必须有明确标签 |
| `entry_timing` | 决定是否立即进入、等待或 `NO_ACTION` | 布林带、RSI、乖离率等可用于时机，不得改变资产池和标签 |
| `exit_state` | 趋势破坏、持有期、止损止盈和风险退出 | 必须与 T+0/T+1、可卖数量和下一 Bar 一致 |
| `portfolio_risk` | 权重、现金、行业/风格、波动率上限和容量 | 不能由单个信号组件绕过 |
| `execution` | TWAP/VWAP/下一 Bar、切片、参与率和跨日 | 只能读取冻结的执行契约 |

不是每个模板都必须使用全部插槽；必填槽、可选槽和空槽行为必须由模板版本预先固定。RD-Agent 可以在允许的槽位中选择组件、组合组件、调整预注册参数或提出新组件候选，但不能把所有指标无规则相加，也不能将无方向指标当作上涨方向证明。

统一组合顺序固定为：资格硬门 → 必要时同类去重 → 方向/环境硬门 → Alpha 排名 → 买点时机 → 退出状态 → 组合风险 → 执行。硬门先做布尔过滤，`alpha_rank` 才负责比较强弱，`entry_timing` 只能延后或放弃本次入场，`exit_state` 和组合风险可以覆盖买入信号。RD-Agent 只提交各槽位的组件绑定和参数，不能改变这个控制顺序。

正式模板应优先复用 Qlib `BaseStrategy`、`WeightStrategyBase`、`BaseExecutor`/`SimulatorExecutor`、`Exchange`、`qlib.workflow.R` 和 Recorder。不同模板共享的指标必须实现为组件，不得复制成多份隐含公式。只有在持仓状态机或执行语义确实不同且 Qlib 现有接口无法表达时，才新增项目自定义 Strategy 或执行适配器。

第一版内容“全部写入”是指全部进入可测试、可版本化的研究组件库，不是全部同时启用、同时分配资金或同时生成正式推荐。某个组件只有被模板插槽引用，并随完整策略版本通过准入和审批后，才影响正式推荐。

### 5.2 多频率能力、策略启用与唯一综合推荐

- [ ] 本模块已实现、测试并通过验收

月度、周度、日度和分钟级是系统支持的四种决策频率，不是必须同时启用的四个策略层。每个实际启用的策略必须拥有自己的标签、持有期、成本、回测、试验台账、准入状态、资本预算和风险预算；未启用频率不生成占位信号，也不阻塞其他策略运行。

| 能力 | 系统要求 | 何时形成新目标 | 运行规则 |
| --- | --- | --- | --- |
| 月频核心策略 | 必备，首期至少一个核心策略通过准入 | 月度决策日 | 新目标持续有效到下次月度决策、失效或风险覆盖；其余日期不重复生成月频信号 |
| 周频策略 | 模板、调度和回测能力必备；正式启用可选 | 仅存在已审批且启用的周频策略时，在周度决策日运行 | 未启用时贡献为零，不生成 `HOLD`/`NO_ACTION` 占位批次 |
| 日度风险监控 | 必备，每个交易日运行 | 仅风险状态、可交易状态或目标有效性变化时形成决策事件 | 可以减少、暂停或清零任何策略目标，但不得凭空增加 Alpha 仓位 |
| 日频 Alpha/Swing | 模板和回测能力必备；正式启用可选 | 已审批且启用时按日度模板运行 | 使用独立卫星预算，不能覆盖硬风险退出 |
| 1min/5min 执行 | 必备能力 | 存在待执行综合目标时，每个完整 Bar 检查是否释放订单 | 只能执行、延迟或取消既定目标，不能扩大目标或反转方向 |
| 1min/5min 独立策略 | 模板、实时数据、回测和模拟能力必备；正式启用可选 | 已审批且启用时按完整 Bar 运行 | 使用独立卫星预算，遵守 T+0/T+1，并进入账户级净额合并 |

资本预算、风险预算和目标波动率必须分开保存：

| 字段 | 定义 |
| --- | --- |
| `capital_budget_s` | 策略在风险缩放前允许占用的账户 NAV 比例 |
| `target_volatility_s` | 策略独立 NAV 的目标波动率；只能向现金缩放，不允许普通现金账户加杠杆强行达到 |
| `risk_budget_s` | 希望该策略贡献的组合总风险比例，不等于资本权重 |
| `member_cap_s` | 策略资本占比硬上限 |
| `marginal_risk_contribution_s` / `total_risk_contribution_s` | 使用同一协方差与最终权重计算的实际风险贡献 |

设当前有效且正式启用的策略集合为 `A(t)`。每个策略最近一次有效目标会持续到其下一决策、显式失效、暂停或退役。账户合成先按资本预算形成证券级预目标：

`w_capital(t) = Σ[s ∈ A(t)] capital_budget_s × volatility_scale_s(t) × w_s(t)`

其中 `w_s(t)` 是策略内部归一化目标，`volatility_scale_s(t) ∈ [0, 1]`。若分配方法声称实现指定风险预算，项目必须再基于策略 NAV 协方差求解资本缩放系数，使最终风险贡献在冻结容忍度内接近 `risk_budget_s`；不能把 `risk_budget_s` 直接乘证券权重冒充风险预算。

日度风险监控和执行择时是覆盖门，不作为 Alpha 权重相加。项目风险层再根据核心/卫星上限、现金、容量、相关性、市场规则、个人投资政策和当前持仓，将 `w_capital(t)` 投影为唯一的 `final_target_weight`。硬风险退出、不可交易和账户约束优先级最高；普通策略之间按资本预算净额相加，并在最终证券权重上重新计算实际资本占比和风险贡献，不采用“谁最后运行谁覆盖”的规则。

同一证券在不同已启用策略中同时出现买入和卖出时，系统必须在账户层先净额合并，禁止生成自买自卖的两组订单。例如月度目标 20%、可选周频覆盖层未启用、日度卫星 +2%，风险约束将总仓位限制为 20%，则最终目标仍为 20%；若以后启用的周频覆盖层给出 -5%，综合目标才变为 17%。策略级信号明细继续保留，用于解释、归因和独立考核，但不得直接发单。

5min 执行检查只决定“现在执行还是等待”。若综合目标要求买入但当前执行条件不合格，账户状态保存 `pending_target_weight`、等待原因和失效时间；后续完整 Bar 继续静默检查。独立分钟卫星产生的仓位只有在自身已批准并启用时，才按独立资本预算参加上述净额合并，并在组合层重新计算实际风险贡献。

账户层必须设置动态不交易区间 `no_trade_band`。只有目标偏离产生的预期净收益，超过佣金最低收费、税费、滑点、冲击、预测误差和人工执行延迟缓冲后，才生成新的买卖建议；否则保持当前持仓。固定 0.5 个百分点通知阈值只用于提醒去重，不能替代经济可交易性判断。整手约束、小额账户和最低 5 元佣金必须进入该判断。

`weekly_tactical_overlay` 是可选批准模板，其冻结合同只在申请准入或正式启用时生效：

- 每周最后一个交易日收盘并完成数据冻结后决策，最早在下一交易日的下一可执行 Bar 调仓；节假日周按交易所实际日历处理。
- 标签为下一完整周或下一次周频调仓前的扣费后收益，不得使用本周尚未结束的数据。
- 股票和 ETF 只能来自已审批候选池；默认周度覆盖层绝对调整总额不超过账户 NAV 的 20%，周度换手不超过 10%，最终组合保持只做多、无杠杆，并在正常风险状态下继续满足“核心占风险资产资本至少 70%、卫星至多 30%”。现金缩放或风险退出时不得为满足该比例增加风险。
- 没有已审批且启用的周频策略时，调度器跳过周频正式信号任务；周度绩效复核和 RD-Agent 研究任务仍正常运行，但不能冒充周频买卖建议。

### 5.3 预测分数、预期收益与不确定性契约

- [ ] 本模块已实现、测试并通过验收

任何因子、规则或模型输出都必须声明 `score_semantics`，至少区分：

- `rank_score`：只表示横截面相对顺序，不具有收益率或概率量纲。
- `expected_return`：表示冻结持有期内的预期总收益或基准相对收益。
- `event_probability`：表示明确定义事件在冻结窗口内发生的概率。

未经样本外校准的 `rank_score` 只能用于排序、分组和相对选股，不能直接当作预期收益、上涨概率、置信度或经济收益进入仓位优化和动态 `no_trade_band`。每个正式预测契约必须固化：

- `forecast_horizon`、标签定义、绝对/相对收益口径、预测生成时点和最早可执行时点。
- `calibration_method`、滚动校准窗口、训练截止时间、截面/时序校准方式、收缩目标和最小样本数。
- `expected_return`、`forecast_uncertainty`、置信区间或历史预测分位，以及模型失效和回退条件。
- 校准代码、输入预测快照、实际标签快照、参数、随机种子、版本和样本外诊断。

收益预测必须使用时间上早于被评估区间的数据校准，并在每个外层 walk-forward 折中重新拟合。排序模型需要通过分组收益单调性、分数—后续收益映射稳定性和换手后净收益验证；概率模型需要通过可靠性图、Brier Score 或同类校准指标验证。没有稳定校准证据时，系统只能展示原始分数和历史分位，不得显示伪造的“预计收益率”或“成功概率”。

账户层经济可交易性判断固定使用保守净收益：

`expected_net_benefit_lcb = shrink(expected_return) - expected_total_cost - uncertainty_buffer - manual_delay_buffer`

只有 `expected_net_benefit_lcb` 为正、超过动态 `no_trade_band` 且满足最小申报单位时才允许新增交易。`shrink`、不确定性缓冲和人工延迟缓冲必须预注册并版本化；不得在看到结果后为提高交易数量而下调。无法获得可信预期收益的排名策略，可以使用冻结的分位—收益保守映射或只做相对权重优化，但必须在推荐中明确“无收益率点预测”。

### 5.4 RD-Agent 挑战者流程

- [ ] 本模块已实现、测试并通过验收

本节定义唯一受治理的自动研究入口。月度、周度、日度和 1min/5min 候选都可以由 RD-Agent 使用相应频率的历史数据调用 Qlib 研究；“RD-Agent 不参与盘中生产循环”只禁止它在实时生产批次中临时生成或修改策略，不限制它离线研究分钟因子、分钟模型和执行算法。

1. 项目先冻结模板、插槽、数据、标签、成本、风险和评价指标，再把可用组件清单交给 RD-Agent。
2. RD-Agent 输出 `template_id`、`slot_bindings`、组件版本、参数、研究假设和预期经济逻辑；可以提出新组件，但不得直接写入组件库。
3. RD-Agent 负责候选因子/模型生成、组件组合、研究迭代和挑战者实验，并在每轮中调用 Qlib 完成因子计算、训练、预测、研究回测和结果反馈；不得改变冻结字段，不得直接修改正在运行的正式模板，也不得建立第二套正式回测循环。
4. 候选输出不得直接进入组合。项目必须在 RD-Agent 研究任务之外，从锁定数据集独立复算每个组件及最终组合，检查方向、覆盖、缺失、稳定性、频率、标签和泄露；独立复算不得直接相信 RD-Agent 已导出的因子值、预测或汇总指标。
5. 冻结后的挑战者必须装入同一模板，使用 Qlib Dataset、Strategy/Executor/Exchange、Workflow/Recorder，在未参与候选选择的确认区间执行正式样本外回测，与不可变基线比较，并提供单组件增量与消融证据。RD-Agent 研究循环中的 Qlib 结果属于探索证据，候选冻结后的独立 Qlib 复测才属于确认性准入证据。
6. 通过统计、成本、容量、稳健性、血缘和审批门后，候选才能形成新的组件版本和模板版本；否则只保留为研究制品。
7. RD-Agent 调用模板的含义是“在冻结模板的允许扩展点内做研究”，不是让它临时生成一套策略后直接用于第二天推荐。

### 5.5 正式 Qlib 链

- [ ] 本模块已实现、测试并通过验收

- 策略间逆波动率和等风险贡献使用 Qlib `PortfolioOptimizer` 生成基础风险资产权重；成员上限、非等风险预算、15% 年化波动率上限的现金缩放及全部约束复核由项目层完成。普通现金账户禁止通过杠杆强行达到 15%。
- 指数增强和全市场约束组合以项目基准相对 QP 为正式求解器，严格检查求解状态、单票、行业、风格、跟踪误差和换手；Qlib `EnhancedIndexingOptimizer` 只作为回归对照，除非项目完成等价约束和 fail-closed 包装验证。
- 资产/策略协方差优先使用固定版本 Qlib `ShrinkCovEstimator`；中国市场行业、规模、价值、成长、波动和 Beta 暴露由项目 PIT 元数据定义。若使用 Qlib `StructuredCovEstimator`，必须明确它是 PCA/FA 潜在统计风险模型，不得冒充现成基本面风险模型。
- 标准绩效指标使用 Qlib 分析组件，年化日数固定为 252，累计收益使用几何口径。
- 多头模板正式回测使用 Qlib Strategy、Executor 和 Exchange，实验通过 Qlib Workflow/Recorder 固化；配对使用项目双腿引擎并复用 Qlib 数据与 Recorder 证据。
- A 股和 ETF 专用限制通过项目约束、市场规则和 Exchange 扩展实现；所有上游优化结果都必须由项目层重新验算，失败时输出 `NO_ACTION`。

### 5.6 RD-Agent 试验预算与反过拟合

- [ ] 本模块已实现、测试并通过验收

- 每个模板必须在研究前冻结 `market_regime_contract_version`。市场阶段不得在看到策略结果后人工挑选，至少使用当时可观测的基准趋势、波动率分位、市场宽度、流动性和回撤状态定义 `bull`、`bear`、`sideways`、`high_volatility`、`liquidity_stress` 等标签；分类阈值、重叠规则和未知状态处理必须预注册。事件压力窗口可以补充极端情景，但不得替代完整连续样本外结果。
- 每个正式策略必须绑定 `baseline_suite_version`。默认简单基线至少包含全现金、对应宽基/研究基准买入持有、低成本可投资 ETF 基准、等权资产配置、固定长期资产配置、逆波动率配置，以及与候选具有相同股票池和调仓频率的简单规则基线。执行算法还必须比较立即成交、TWAP 和 VWAP 基线。复杂策略只有在扣费、容量和不确定性后相对适用简单基线具有可验证增量价值时才能晋升。
- 每个研究程序和正式策略必须绑定 `platform_total_cost_contract_version`，分别记录数据订阅、LLM/API、CPU/GPU、存储、备份、通知、人工维护时间和故障恢复时间。交易绩效仍报告纯投资净收益；投资决策报告还必须按预注册分摊方法展示扣除平台现金成本后的净增量价值，并把个人维护时间按用户设定的影子价格单独列示。不得把沉没的公共平台成本重复分摊给每个策略，也不得为了让复杂策略通过而把其独占研究和运行成本隐藏在公共费用中。
- 样本外长度必须同时用日历跨度和有效独立决策事件描述。日线研究的基础时间切分固定为训练 756 个交易日、验证 252 个交易日、最终锁箱至少 504 个交易日；这是最低日历跨度，不等于独立样本数。月频核心在外层 walk-forward 与最终锁箱合计至少需要 60 个历史样本外调仓事件；周频模板申请准入时至少需要 104 个历史样本外周度决策事件；Swing、配对和分钟模板申请准入时必须通过功效分析预注册最少有效决策日、成交事件和完整持仓周期。
- 每个因子和模型必须固化预测标签、标签持有期、标签最早可知时间、交易时点、买卖价格规则、绝对/基准相对收益口径、复权与缺失样本处理。训练/验证/测试之间按真实标签区间执行 purge，并按模板的 Bar/交易日/事件结束时间设置 embargo；窗口不足时研究失败关闭。
- Walk-forward 必须采用嵌套结构：内层训练/验证只负责选择组件、模型和参数，外层滚动样本外只负责估计泛化表现，最终锁箱只确认一次。任何窗口都只能使用当时可获得的数据、模型和参数；缩尾阈值、填充器、时序标准化、特征选择、PCA 和模型参数只能在当前训练折拟合。
- 研究试验预算不得固定为与有效样本无关的“100 个候选 × 30 组参数”。每个 `research_program_id` 必须先根据有效独立样本数 `N_eff`、标签重叠、候选相关性、模型自由度、预期效应大小和检验功效生成 `trial_budget_contract`。预算必须同时设置候选数、参数组合数、随机种子数、特征子集数和累计原子试验上限；`N_eff` 较小的月频策略必须使用更少、更强先验的候选。无法证明试验预算与统计把握度相容时，只允许探索研究，不得进入正式晋升流程。
- 凡被计算绩效并用于排序、筛选、淘汰或人工判断的公式、方向、窗口、组件组合、特征子集、模型、超参数、随机种子选择、持仓规则和执行参数，均生成独立 `atomic_trial_id`，不得把一次批处理或一次 RD-Agent 任务冒充一次试验。预算耗尽后只能结束研究或由新数据形成新的研究阶段，不得通过改名、换入口或拆分任务继续搜索同一锁箱。
- 人工试验、脚本试验和 RD-Agent 自动试验共享同一个 `experiment_family` 预算与全局试验台账；同一经济假设跨任务、跨模板或改名后仍归入不可重置的 `research_program_id`，禁止通过换入口清零失败历史。
- 最终测试集对同一 `experiment_family` 只允许解封一次。查看最终测试结果后再调参，必须建立新研究活动并使用更晚的全新测试区间。
- 收益、Sharpe、IR 和 Alpha 必须由扣费后日度组合 NAV 计算并报告区块 Bootstrap 置信区间；分钟结果先聚合到日度，IC 以每日截面 IC 时间序列统计，不得把证券数或分钟 Bar 数冒充独立样本数。
- 多候选比较默认使用 Benjamini-Hochberg，FDR `q ≤ 0.10`；候选相关结构不满足其条件时改用 BY、分层 FDR 或置换检验。Deflated Sharpe Probability 必须 `≥ 0.95`，并使用完整原子试验数、候选相关性、偏度和峰度；CSCV/PBO 必须报告，`PBO > 20%` 时不得晋升。
- 每个研究程序必须运行负对照和泄露哨兵：随机打乱标签、按证券或日期置换信号、将信号整体平移、加入已知无经济含义的随机因子，以及故意注入决策后字段验证反泄露测试确实会失败。候选若不能稳定优于负对照，或泄露哨兵未被阻断，不得准入。
- 除统计显著性外必须报告经济显著性：扣费后单位换手收益、单位风险收益、相对简单基线的增量收益、置信区间下界、最差合理成本下的净收益和可执行容量。仅有较小 p 值但经济收益不足以覆盖成本与模型不确定性的候选不得准入。
- 区块 Bootstrap 必须冻结区块长度选择方法，并对不同合理区块长度做敏感性分析；标签和持仓区间重叠时必须按最大依赖长度设置区块、purge 和有效样本数，禁止把重叠收益当作独立观察。
- 必须通过参数邻域、双倍成本、换手收紧、持仓收紧和零保留缓冲测试；只在单一点参数优秀的候选不得准入。
- 随机种子、训练代码、依赖锁、输入列顺序、模型权重、预测快照、日志和失败记录均作为不可变制品保存；无法复现相同预测的模型不得准入。
- RD-Agent 不得自行修改准入线、数据边界、成本模型或最终测试集；这些内容只能由新策略版本显式变更。

### 5.7 正式策略准入线

- [ ] 本模块已实现、测试并通过验收

正式准入必须使用与策略经济目标一致的 `admission_contract_version`，不得再用一张统一指标表要求所有策略同时满足绝对回撤、超额收益、IR 和 Sharpe。所有合同共同要求：合格 PIT、下一 Bar、完整成本、容量、简单基线增量、有效样本、置信区间、负对照、压力测试、可复现制品和前向观察。

| 策略类别 | 主要准入目标 | 主要硬门 | 不适合直接作为统一硬门的指标 |
| --- | --- | --- | --- |
| 指数增强 | 在受控主动风险下稳定取得扣费后基准超额 | 主动收益、IR、ex-ante/ex-post Tracking Error、主动回撤、行业/风格偏离、换手和容量 | 绝对 NAV 最大回撤不得机械限制为 10%；基准大跌时绝对回撤可能显著超过主动回撤 |
| 全市场多因子/股票核心 | 相对宽基和可投资简单基线取得扣费后增量价值 | 超额收益、IR、Sharpe、绝对与主动回撤、暴露、换手、容量 | 单一沪深 300 超额或单一 Sharpe |
| ETF 资产配置 | 在用户政策基准下改善收益—风险—用款效用 | 相对政策基准和可投资基线的收益、波动、回撤、ES/CDaR、恢复期、人民币损失概率和换手 | 必须每年超额 3%；允许相近收益但显著降低风险或提高流动性 |
| 周频/Swing/分钟卫星 | 在独立卫星预算内提供扣费后正增量 | 有效决策/成交事件、净收益、Sharpe/Sortino、回撤、换手、容量、延迟和执行证据 | 仅用 504 个交易日替代有效交易事件数 |
| 配对/统计套利 | 在真实可执行假设下获得市场中性或相对价值收益 | 形成期外协整稳定性、净收益、尾部风险、双腿执行、非原子压力、借券证据和容量 | 普通多头基准 IR；没有真实借券证据时不得形成股票空腿个人建议 |
| 执行算法 | 在不改变上层目标的前提下降低实现差额 | Implementation Shortfall、完成率、未成交机会成本、参与率、延迟和压力场景 | 组合 Sharpe、Alpha 或策略年化收益 |

本版初始准入合同的数值基准如下；这些数值属于各合同的版本化默认值，不是跨策略永久统一真理：

| 策略类别 | 初始数值基准 |
| --- | --- |
| 指数增强 | ≥504 个样本外交易日且合计 ≥60 个样本外调仓事件；扣费后年化主动收益 ≥3%；IR ≥0.50；ex-ante/ex-post Tracking Error 均满足模板上限，默认 ≤3%；双倍成本后主动收益仍为正 |
| 全市场多因子/股票核心 | ≥504 个样本外交易日；扣费后年化超额收益 ≥3%；Sharpe ≥0.80；IR ≥0.50；绝对最大回撤默认 ≤10%；双倍成本后收益仍为正 |
| ETF 资产配置 | ≥504 个样本外交易日且 ≥60 个样本外调仓事件；相对政策基准或可投资简单基线的预注册效用增量置信下界为正；账户目标回撤和人民币损失概率满足个人投资声明 |
| 周频卫星 | ≥104 个历史样本外周度决策事件；扣费后年化超额收益默认 ≥4% 或无合适基准时绝对收益 ≥6%；Sharpe ≥1.00；最大回撤默认 ≤8% |
| 日频/分钟卫星 | 由功效分析预注册最少有效决策、成交和完整持仓周期；扣费后年化超额收益默认 ≥4% 或绝对收益 ≥6%；Sharpe ≥1.00；最大回撤默认 ≤8%；实时分钟策略另需延迟与日终对账通过 |
| 配对/统计套利 | 形成期外的有效交易事件数由功效分析确定；扣费后绝对收益、Sharpe 和最大回撤默认沿用卫星门槛；理想原子与非原子压力结果都必须通过 |
| 执行算法 | 在相同订单集、市场窗口和参与率约束下，Implementation Shortfall 相对适用基线的保守置信上界更低；完成率和尾部未成交机会成本满足执行合同 |

每个策略合同必须明确主要指标、次要指标、硬门、观察门和失败回退。绝对账户 10% 回撤是账户级风险处置线，不是指数增强、执行算法或所有成员策略的统一准入上限。指数增强仍必须报告绝对回撤，但审批重点是主动风险是否符合合同；账户层根据用户损失能力决定是否进一步现金缩放。

ETF 资产配置可以通过以下任一预注册效用路径证明价值：在相近风险下提高扣费后收益、在相近收益下降低回撤/ES、提高流动性和用款匹配，或提高冻结效用函数的保守置信下界。不得为了通过准入在结果出来后选择最有利的效用函数。

准入评估必须同时报告年化换手率和策略规定的日/周/月换手约束。年化换手率没有脱离策略类型的统一硬下限；实际硬门是策略规定的参与率、完整成本、可执行容量、简单基线增量和相应合同的压力测试全部通过。

正式准入不能只验证 500 万元。`acceptance_notional=500万元` 继续用于策略横向比较，同时必须在至少三个资本档位和用户当前账户 NAV 下运行离散化可执行性测试，资本档位默认覆盖 50 万元、100 万元和 500 万元，可根据个人投资声明新增档位。每个档位必须报告可持仓证券数、整手误差、最低佣金拖累、现金残余、目标偏离、可执行订单比例和容量；某档位不合格时只能声明该策略不适用于该资金规模，不得用 500 万元成绩替代个人账户证据。

适用于该策略合同的收益、Sharpe、IR、Alpha、Tracking Error、最大回撤、Implementation Shortfall 和成本后收益必须同时报告点估计、有效样本数和适用的区块 Bootstrap 置信区间。正式准入不得只依据单个点估计；至少要求主要经济指标的保守置信下界满足预注册条件，或给出经过审批的等价概率门。月频、周频和重叠持有期策略必须按实际独立决策事件和依赖长度计算有效样本，禁止用交易日数、证券数或分钟 Bar 数夸大统计把握度。

### 5.8 模型注册、漂移、重训与回滚

- [ ] 本模块已实现、测试并通过验收

- 每个正式因子和模型进入模型注册表，记录 `model_version`、特征契约、标签契约、训练窗口、训练数据哈希、代码/权重哈希、随机种子、Qlib/RD-Agent 身份和审批状态。
- 每日或每次预测监控特征缺失率、分布漂移、预测分布、截面 IC/Rank IC、换手、覆盖率和行业/风格暴露；每周和每月汇总模型表现与数据漂移。
- ML 模型还必须报告预测校准、分组收益单调性和不确定性；没有可验证置信度时只能展示原始分数和历史分位，禁止生成伪造的“成功概率”。
- 触发严重数据漂移、预测异常、连续 IC 恶化、样本外指标跌破暂停线或输入契约变化时，模型先暂停新增风险，再进入冻结版本复盘；不得边运行边覆盖模型。
- 重训必须创建新模型版本，重新执行独立复算、walk-forward、成本、容量、最终测试和审批。旧模型保持可回放，并可在新版本异常时回滚。
- 自动重训只能产生候选版本，不能自动把候选晋升为正式推荐版本；生产切换必须经过明确审批和可逆部署。

### 5.9 优化器目标函数、数值环境与离散修复合同

- [ ] 本模块已实现、测试并通过验收

任何正式组合优化必须绑定不可变 `optimizer_contract_version`，不能只声明“使用 QP”“使用风险平价”或“使用 Qlib 优化器”。合同至少冻结：

- 输入语义：`rank_score`、样本外校准 `expected_return`、基准权重、协方差、特异风险、当前持仓、成本、容量和约束的单位、时点及缺失处理。
- 目标函数、全部约束、惩罚项、缩放方式和量纲。基准相对股票组合的默认形式为最大化“收缩后的保守预期主动收益 − 主动风险惩罚 − 换手惩罚 − 显式交易成本 − 不确定性惩罚”，各系数必须预注册并版本化。
- 未经收益校准的 `rank_score` 不得直接当作百分比预期收益。只使用排名时，优化器必须采用冻结的秩权重/分位映射，并明确其不是均值—方差收益预测。
- 求解器名称与版本、线性代数库、BLAS/LAPACK、操作系统/架构、随机种子、浮点精度、可行性容差、最优性容差、最大迭代数、超时和确定性设置。
- 求解结果必须检查状态、目标值、原始/对偶残差、约束违例、条件数和解的稳定性。`optimal_inaccurate`、超时、不可行、非有限值或超过容差时失败关闭，不得直接采用“看起来合理”的权重。
- 多个等价最优解必须有确定性选择规则；相同输入和数值环境应产生相同连续权重。

连续权重不是最终可执行组合。项目必须实现版本化 `discrete_repair_contract`：

1. 将目标按证券申报单位、最低佣金、现金和可卖数量离散化。
2. 重新计算现金、单票、行业/风格、资产类别、Tracking Error、换手、容量和风险约束。
3. 在冻结目标函数下使用确定性修复算法分配剩余现金或删除不可行订单。
4. 修复后仍违反任一硬约束时输出 `NO_ACTION`、降级到预注册基线或缩小风险，不得只保存连续解的合格证明。

优化器回归必须包含小型解析案例、病态协方差、强负相关、不可行约束、极小账户、最低佣金、整手误差和跨平台数值容差；升级求解器或数值库必须重跑全部量化回归。

### 5.10 RD-Agent/LLM 知识时间边界与证据限制

- [ ] 本模块已实现、测试并通过验收

LLM 可能在预训练、联网检索、论文或提示词中知道历史测试期之后发生的事件、公开策略和市场结论。因此普通 PIT 和代码反泄露测试不能证明 RD-Agent 的研究思想在历史时点真正未知。每次研究必须记录：

- LLM 提供商、模型标识、可获得的知识截止声明、联网状态、检索资料 URL/哈希、资料发布日期、提示词和工具调用。
- `idea_available_at`：该研究思想、论文、公开代码或数据方法最早可验证的公开时间。
- `historical_knowledge_risk`：`low`、`medium`、`high` 或 `unknown`，并说明历史锁箱可能受到的知识污染。

若研究思想或 LLM 知识可能晚于历史测试期：

- 历史回测只能证明实现和参数在冻结数据上的行为，不能单独证明该思想当时可发现。
- 候选必须主要依赖形成后的真实前向模拟周期和新到市场数据晋升；不得因为历史回测优秀降低前向证据要求。
- 无法判断知识时间边界的候选最多进入 `forward_observation`，除非经过人工批准并使用更晚的全新锁箱与充分前向证据。

RD-Agent 和 LLM 是本系统自动挑战者研究的固定技术主线，但不是正式生产运行依赖。尚未启动自动研究时，项目冻结的 Qlib 基线仍可完成回测、模拟和生产运行；一旦启动新因子、新模型或自动挑战者研究，就必须统一走“RD-Agent 调用 Qlib 探索 → 独立复算 → Qlib 正式复测 → 前向模拟 → 审批”的主线，不得另建第二套自动研究入口。LLM 不可用、额度耗尽、服务中断或模型下线时，系统只暂停新的自动候选研究，已审批策略的数据更新、Qlib 推理、风险监控、推荐、模拟账本、通知和恢复必须继续运行。

### 5.11 自适应自动研究与序贯统计契约

- [ ] 本模块已实现、测试并通过验收

RD-Agent 会根据前一轮结果决定下一轮候选，这属于自适应、序贯搜索。普通 FDR、DSR、PBO 和一次性样本外检验不能单独证明这种研究过程没有选择偏差。每个自动研究程序必须绑定 `adaptive_research_inference_contract_version`：

- 明确区分 `exploratory_evidence` 与 `confirmatory_evidence`。探索阶段可以使用验证集指导下一轮，但其 p 值、Sharpe 和排名不得直接作为正式晋升证据。
- 最终锁箱数据、标签和成绩对 RD-Agent、研究提示词、自动调参器和人工研究者保持不可见；只能由独立准入任务解封一次。任何摘要、排名、通过/失败反馈都视为查看锁箱。
- 查看锁箱后，该 `experiment_family` 的锁箱永久关闭。继续研究必须等待新市场数据形成更晚的独立区间，不能通过改名、换模型、换提示词或只隐藏具体数值继续使用旧锁箱。
- 若研究过程允许根据中间结果提前停止、扩展候选或重新分配预算，必须预注册序贯检验、alpha spending、always-valid/e-value 或经批准的等价方法；没有有效序贯校正时，中间显著性只作为探索证据。
- 研究停止规则、候选生成策略、人工否决、提示词修改和失败重试都进入原子试验台账。晋升主要依赖独立确认锁箱和形成后的真实前向数据，不得用更多自动试验代替新的独立证据。
- 自适应合同必须覆盖跨 `research_program_id` 的同一经济假设，防止把一个连续搜索过程拆成多个程序后重置统计预算。

## 6. 股票核心与指数增强

- [ ] 本模块已实现、测试并通过验收

### 6.1 个人低复杂度股票核心

`personal_stock_core` 是首期个人影子账户默认股票核心。它使用与其他股票核心相同的 PIT、因子、成本、容量、回测和准入链，但把人工执行能力作为模板硬约束，而不是在推荐阶段临时删减股票。

| 参数 | 默认值 | 验收语义 |
| --- | --- | --- |
| 持仓数 | 10–30 | 具体数量由账户 NAV、整手误差、集中度和人工执行政策冻结 |
| 调仓 | 月度 | 非调仓日只做风险与有效性检查 |
| 单票/单行业 | 默认 8% / 30% | 必须根据实际持仓数、行业集中和个人政策收紧 |
| 单次调仓证券数 | 不超过 `human_execution_policy` | 超限则整批 `simulation_only`，不得临时删单 |
| 执行 | 下一交易日或低切片 5min 计划 | 不要求个人手工执行数百个切片 |
| 基线 | 可投资宽基 ETF、等权低换手股票组合 | 必须证明复杂度增加带来扣费后增量价值 |

该模板可以使用六因子或经批准的简单模型，但必须重新训练、回测和准入。它不是把 100 只指数增强结果截取前 20 只，也不得沿用指数增强的收益、Tracking Error 或容量证据。

### 6.2 指数增强

以项目六因子 Qlib Expression 基线为不可变起点；RD-Agent 挑战者只能在模板插槽内组合或增强组件，并在独立复算和样本外准入后形成新版本。

| 因子组 | 基线权重 | 处理要求 |
| --- | --- | --- |
| 动量 | 20% | 12-1 月价格动量；PIT、缩尾、z-score、行业/市值处理 |
| 反转 | 10% | 短期反转；与动量使用不同窗口并独立标准化 |
| 价值 | 20% | EP、BP 分别处理负值/异常值并各自缩尾、z-score 后再合成 |
| 质量 | 20% | ROE、ROA 分别做 PIT 缩尾、z-score 后再合成 |
| 成长 | 10% | 营收和利润增速分别处理低基数异常、缩尾、z-score 后再合成 |
| 低波动 | 20% | 使用历史已实现波动率，方向固定为低波动优先 |

原子指标必须先各自完成单位、异常值、缺失和截面标准化，再形成价值、质量或成长子因子；禁止先把不同量纲的原始指标直接平均，再只对合成结果标准化。

| 参数 | 默认值 | 验收语义 |
| --- | --- | --- |
| 基准 | 沪深 300 PIT 权重 + 对应全收益口径 | 优化、超额收益、IR 和归因使用同口径；SH000300 价格指数只辅助展示 |
| 调仓 | 月度 | 非调仓日只进行风险检查；不得因每日运行任务而每日重选股票 |
| 股票池 | 沪深 300 PIT 成分为主 | 允许受控非成分候选时，非成分股总权重默认不超过 10% |
| 持仓数 | 100 | 按优化后可交易目标计 |
| 单票上限 | 2% | 任何目标和执行后持仓均不得突破 |
| 行业偏离 | ±3% | 相对基准 PIT 行业权重 |
| 跟踪误差 | ≤ 3% | 同口径年化 ex-ante/ex-post 证据 |
| 每日换手 | ≤ 15% | 跨日 VWAP 计划同样受限 |
| 流动性 | 20 日平均成交额 ≥ 5 亿元作为初筛 | 正式容量按目标订单、参与率、冲击和最大执行天数动态验证 |
| 执行 | 5min；3 个交易日 VWAP | 10:00–11:20、13:30–14:50 |

指数增强的策略准入以主动收益、Information Ratio、Tracking Error、主动回撤和暴露约束为主要指标。绝对 NAV 回撤必须报告并进入账户级风险缩放，但不得因为基准自身发生大幅回撤就自动判定指数增强研究失败。若系统通过趋势择时或大量现金主动控制绝对回撤，该行为必须在模板中显式定义为独立的“指数增强 + 风险覆盖”策略，并分别归因基准暴露、选股 Alpha 和择时贡献。

默认 100 只指数增强首先认证为完整模拟能力。只有实际账户本金、整手误差、当日交易证券数、切片数量、批量导入方式和用户响应时间全部通过 `human_execution_policy`，该版本才能输出个人影子账户正式数量建议；否则只展示模拟结果、目标权重和低复杂度替代策略入口。

任何 3 个交易日或其他跨日执行计划都必须绑定 `transition_portfolio_contract_version`。执行期间的实际持仓不是旧目标也不是新目标；每次成交、每个日终及重大风险事件后，系统必须使用实际持仓和剩余订单重新计算现金、Tracking Error、行业/风格暴露、单票上限、容量、可卖数量和压力风险。过渡组合违反硬门时必须重排、缩减、取消剩余订单或进入 `NO_ACTION`，不得为了完成原目标继续执行。允许在不改变上层最终目标和 Alpha 方向的前提下优化剩余订单顺序与切片；若要改变最终目标，必须产生新的策略决策事件并保留原计划。

## 7. 核心策略二：全市场行业受控多因子

- [ ] 本模块已实现、测试并通过验收

- 股票池为 PIT 全 A 股可交易股票；包含历史退市状态，排除当时不可交易证券。
- 使用与指数增强相同的项目六因子 Qlib Expression 基线和 RD-Agent 模板插槽流程。
- 全 A 宽基全收益指数作为主绩效基准；SH000300 只用于辅助报告，不作为优化基准或唯一超额收益口径。
- 行业目标按当期全市场流通市值权重计算；行业、规模、Beta 和流动性属于需要控制的意外风险暴露。价值、成长和低波动是计划保留的 Alpha 暴露，只设置防集中区间，不得在信号生成后又强制回归为零。若要研究严格风格中性策略，必须建立不同模板和独立实验家族。

| 参数 | 默认值 | 约束 |
| --- | --- | --- |
| 调仓 | 月度 | 信号形成后下一可执行 Bar |
| 持仓数 | 100 | PIT 可交易股票池 |
| 单票/单行业 | 5% / 15% | 目标及执行后同时约束 |
| 年化波动率上限 | 15% | 项目根据 Qlib 协方差估计向现金缩放；不加杠杆，实际波动率可以低于 15% |
| 流动性 | 20 日平均成交额 ≥ 5 亿元作为初筛 | 正式容量按目标订单、参与率、冲击和最大执行天数动态验证 |
| 执行 | 5min；3 个交易日 VWAP | 使用统一执行窗口 |
| 回撤 | 8% | 暂停新增风险，等待复核 |

## 8. 核心策略三：ETF 资产配置

- [ ] 本模块已实现、测试并通过验收

`etf_asset_allocation` 用于正式 ETF 推荐，不等同于使用股票复制指数的“指数增强”。资产池覆盖境内上市的宽基、行业、债券、黄金、货币和跨境 ETF，并按底层资产、跟踪指数和交易规则分类。

| 模板插槽 | 第一版规则 | RD-Agent 可研究范围 |
| --- | --- | --- |
| `eligibility_gate` | 基金规模、上市时间、成交额、价差、跟踪误差、折溢价、清盘风险和数据完整性 | 在预注册范围内调整阈值，不得绕过硬门 |
| `universe_dedup` | 相同或高度重合指数只保留成本、流动性和跟踪质量更优者 | 可提出评分改进，但必须报告底层持仓重合度 |
| `direction_regime_gate` | 中期趋势和风险环境；不满足时允许持有现金或货币 ETF | 可组合均线、突破等方向组件 |
| `alpha_rank` | 中期动量、趋势稳定性、跟踪质量和折溢价风险 | 可使用规则因子或 Qlib Model |
| `entry_timing` | RSI、布林带和乖离率只用于避免极端追高或延后调仓 | 不得单独决定长期资产配置方向 |
| `portfolio_risk` | 逆波动率/等风险贡献基础权重，叠加资产类别上限、相关性、现金和集中度约束 | 可研究观察窗与风险预算，但必须进入独立试验台账 |
| `exit_state` | 趋势失效、持续异常溢价、流动性恶化、跟踪异常或组合回撤 | 退出必须遵守对应 ETF 的 T+0/T+1 规则 |

- 默认月度调仓，标签为下一完整调仓周期的扣费后总收益或相对配置基准收益，不使用下一日收益代替月度目标。
- 股票、债券、黄金、跨境和货币 ETF 的交易日历、汇率、底层市场休市、折溢价和 T+0/T+1 分开建模。
- Qlib 生成风险资产基础权重，项目层实施资产类别上限、现金缩放、整手、容量和个人账户约束。
- ETF 推荐必须展示底层资产、跟踪指数、同类替代品、主要风险和推荐失效条件，不能只展示代码和分数。
- ETF 选择和持有成本必须综合管理费、托管费、指数使用费等已披露持续费用、历史跟踪差异、买卖价差、折溢价、申赎和份额折算风险、跨境额度或底层市场休市风险以及实际成交冲击。基金费率属于持有期净值中的持续拖累，不得与交易佣金重复扣减；同类 ETF 排名必须分别展示显式费率、历史跟踪差异和可交易成本。
- ETF 资产配置的正式价值不限定为固定 3% 年化超额。策略必须相对于政策基准和可投资简单基线，使用预注册效用合同证明提高扣费后收益、降低回撤/Expected Shortfall、改善恢复期和用款匹配中的至少一项，同时不在其他关键维度造成不可接受恶化。
- 跨境、债券和货币 ETF 必须同时通过 `currency_contract_version`、`inflation_contract_version` 和适用的 `fixed_income_risk_contract_version`；所需控制/参考数据缺失时缩小资产池，不得沿用宽基股票 ETF 的风险合同。

## 9. 卫星策略一：配对/统计套利

- [ ] 本模块已实现、测试并通过验收

配对是卫星系统，不是独立主线，也不是 Qlib 原生正式模板。项目保留配对专用研究、双腿回测与执行能力，同时复用 Qlib 数据、表达式、Workflow/Recorder、统一指标、审批、分配和模拟接口。

- 研究包含相关性、Engle–Granger 协整、Kalman 动态对冲比率、Z-score、容量、费用、借券与鲁棒性验证。
- 配对、形成窗口、协整阈值、对冲比率模型和退出参数分别计入 `atomic_trial_id`；配对筛选只能发生在当前训练/形成窗口，随后必须在未参与筛选的交易窗口验证，并处理共享证券造成的候选相关性。
- 日线形成信号，下一交易日使用 1min 执行窗口；禁止同时同 Bar 偷看成交。
- Tushare 只能提供公开融资融券标的等资格线索，不能证明个人券商账户的真实可借数量和费率；股票空腿统一标记为假设性融券模拟。
- 双腿订单共享 `atomic_group_id`，并记录 `leg_no`、`position_side` 和 `borrow_cost`；两腿全部成交或全部拒绝。
- 模拟账本可以使用原子组保持会计一致，但必须另外报告人工执行时无法保证原子成交的双腿价差和先后成交风险。
- 配对策略必须额外运行非原子人工执行压力测试，至少覆盖单腿先成交、另一腿延迟 1/5/10 分钟、部分成交、价格跳空、涨跌停、临时停牌和无法借券。正式研究报告必须同时展示理想原子结果与非原子压力结果；任何依赖不可实现原子成交才盈利的策略不得形成个人投资建议。
- 配对页面只提供研究、审批和模拟，不提供实盘或券商发单。
- 没有人工提供的可借数量和费率证据时，股票多空配对不得进入正式投资推荐，只能进入研究报告；ETF 或 long-long 相对价值可按普通卫星准入。

## 10. 其他卫星

- [ ] 本模块已实现、测试并通过验收

### 10.1 Swing

- [ ] 本模块已实现、测试并通过验收

Swing 使用基于 Qlib 的 `swing_trend` 模板，指标按固定先后关系工作：

1. `eligibility_gate`：先过滤 ST、停牌、流动性不足、重大监管和财务质量不合格证券。
2. `direction_regime_gate`：默认要求收盘价高于 MA20、MA20 高于 MA60、`+DI > -DI`；ADX 只衡量趋势强度，不能单独证明上涨。
3. `alpha_rank`：在方向合格的股票中使用突破强度、ADX、量能、质量和风险调整后动量排名。
4. `entry_timing`：布林带、RSI、乖离率和短期回踩只决定立即买入、等待或 `NO_ACTION`，不能推翻方向门。
5. `exit_state`：趋势破坏、持有期、ATR/波动率止损、跟踪止盈和成员风险门共同决定减仓或退出。

原固定 7% 止损、12% 减半和 20% 清仓只作为待验证的基线参数，不是不可改变的投资规律；RD-Agent 可以在预注册范围内比较固定百分比与 ATR/波动率规则，但每种组合都计入独立试验。成员回撤达到 8% 暂停新增风险、达到 10% 退出并复核，与统一组合风险门保持一致。

### 10.2 分钟均值回归

- [ ] 本模块已实现、测试并通过验收

分钟均值回归拆成两个不同模板、标签和实验家族：

| 模板 | 可交易资产 | 标签与退出语义 |
| --- | --- | --- |
| `minute_mean_reversion_etf` | 经规则确认可 T+0 的 ETF | 可预测未来若干完整 1min/5min Bar，并在下一可执行 Bar 买入和退出 |
| `minute_mean_reversion_stock` | A 股股票 | 买入后必须持有到下一可卖交易日，标签延伸到下一交易日可执行卖出窗口；禁止假设当日买入后几分钟卖出 |

两者都可复用布林带、RSI、VWAP 偏离、乖离率和 Z-score 组件，但不得共享标签、参数或样本外证据。默认 5min 信号、下一根完整 1min 或 5min Bar 执行；滚动窗口必须明确是否在开盘、午休和跨日处重置，禁止把跨午休或隔夜的连续 Bar 静默解释成“当日 60/120 分钟”。

### 10.3 ML、行业轮动与事件驱动

- [ ] 本模块已实现、测试并通过验收

- ML 只作为 RD-Agent 因子或模型来源，不建设独立 ML 引擎。
- 行业轮动和事件驱动本轮不建设独立系统；未来若批准，仍必须进入同一 Qlib/RD-Agent 主线。

## 11. 核心/卫星分配与组合风险

- [ ] 本模块已实现、测试并通过验收

分配成员必须记录 `role=core|satellite`、资本预算、风险预算、目标波动率和成员上限；Qlib 负责逆波动率或等风险贡献基础解，项目负责资本预算到风险预算的映射、成员上限、现金缩放、相关性、流动性和风险状态，并在落地前重新验证全部约束。

组合风险必须同时使用证券代码层和底层经济暴露层。ETF、指数增强、全市场选股和行业 ETF 可能重复持有同一批股票，系统必须对 ETF 底层成分进行穿透或使用可解释的代理暴露，汇总行业、风格、单一发行人、地区、货币、久期、商品和主要成分集中度。无法获得足够新的 ETF 持仓时必须标记暴露陈旧度、使用保守上限并阻断依赖精确穿透的优化。

| 风险规则 | 默认值 | 处置 |
| --- | --- | --- |
| 核心占风险资产资本 | 正常状态下 ≥ 70% | 分母是现金缩放前且通过可交易门的风险资产资本，不是账户总 NAV；风险缩放后可以低于账户 NAV 的 70% |
| 卫星占风险资产资本 | 正常状态下 ≤ 30% | 与核心使用同一分母；`risk_off`、流动性阻断和个人政策可以进一步压低 |
| 单卫星资本占比 | ≤ 15% | 按账户 NAV 计算，并同时受成员资本上限与风险预算限制 |
| 单策略资本占比 | ≤ 70% | 按账户 NAV 计算，核心与卫星均适用 |
| 年化波动率上限 | 15% | 项目根据 Qlib 基础权重和协方差向现金缩放；不得使用杠杆强行达到 |
| 相关性 | 样本点估计默认 ≤ 0.70 | 同时检查置信上界、压力期相关、下行相关和加入成员后的边际风险改善；不得只靠单一点估计 |
| 尾部依赖 | 压力期相关、下行 Beta 和 Expected Shortfall | 普通相关性通过但尾部风险过高时仍阻断 |
| 流动性风险 | 建仓/减仓/完全清仓天数 | 任一压力情景超过策略规定期限时降权或拒绝 |
| 成员回撤 | 6% / 8% / 10% | 分别告警 / 暂停新增风险 / 退出并复核 |
| 组合回撤 | 6% / 8% / 10% | 分别降至 70% / 降至 30% / 停止买入并退出风险持仓 |

逆波动率和等风险贡献基础权重使用 Qlib 求解；非等风险预算、成员上限和年化波动率上限由项目层映射并复核。分配审批只能使用认证模拟 NAV，不得用回测收益冒充模拟证据。审批必须同时报告成员的边际风险贡献、风险预算偏差、组合 Beta、跟踪误差、压力 VaR/Expected Shortfall、行业/风格集中度和清仓天数。

核心/卫星比例是正常风险状态下的战略构成约束，低于法律、数据、账户状态、现金、流动性和回撤硬门。进入 `risk_reduced`、`risk_off`、存在不可交易资产或个人最低现金要求提高时，核心占账户 NAV 可以低于 70%，不得为了满足比例重新增加风险；报告必须同时展示现金缩放前风险资产构成、现金缩放后账户资本占比和实际风险贡献。

协方差和优化证据必须额外报告样本重叠区间、有效观察数、缺失处理、收缩方法、最小/最大特征值、条件数和正半定检查。协方差矩阵病态、有效样本不足、输入策略历史不重叠或任一风险序列未认证时，组合优化失败关闭，不得继续使用求解器返回的表面数值。

风险贡献规则必须与分配方法一致。只有声称为等风险贡献/指定风险预算的分配结果，才要求每个成员风险贡献为有限正值并在冻结容忍度内接近目标预算。最小方差、对冲型或一般资产配置组合可以出现负边际/总风险贡献，因为该成员可能在当前协方差下提供对冲；但必须报告方向、稳定性、置信区间、压力情景和移除该成员后的风险变化，禁止把数值异常误称为对冲价值。强负相关、接近奇异协方差或成员上限可能使风险预算问题无经济可解释解；此时系统只能按预注册规则回退到逆波动率、最小方差或固定权重，或者直接输出 `NO_ACTION`。回退方法、触发原因和新风险贡献必须进入审批证据。

相关性准入必须报告估计窗口、有效样本数和置信区间或 Bootstrap 分布。样本相关性低于 0.70 但置信上界、下行相关或压力期相关超过合同阈值时，不得声称已经获得稳定分散化；反之，相关性略高于固定点阈值但加入成员后能稳定降低 ES、回撤超调或人民币损失概率时，可按预注册组合效用合同审批，而不是机械拒绝。

成员选择、协方差窗口、风险预算、相关性阈值和现金缩放参数不得使用同一段 NAV 反复选择后再自证有效。首期优先采用冻结的简单分配规则；任何调参型分配器必须建立独立 `experiment_family`，经过自己的嵌套 walk-forward、最终锁箱和后续前向观察。

15% 年化波动率上限与 10% 组合回撤处置线不是天然兼容。正式启用前必须在牛市、熊市、急跌、流动性收缩和跳空情景下验证两者的联合触发频率；若历史和压力证据显示会频繁进入退出状态，应通过新版本降低波动率上限或调整风险预算，不得在运行时临时放宽回撤线。

10% 组合回撤是风险处置触发线，不是“最大只会亏 10%”的保证。跳空、连续跌停、停牌和流动性枯竭可能导致最终损失超过触发线。组合审批必须报告 `P(MDD > 10%)`、Conditional Drawdown at Risk、Expected Shortfall、触发后的回撤超调、最长恢复时间和 `P(人民币损失 > 个人上限)`；并使用个人投资声明中的人民币损失能力反推风险预算。

任何最大回撤超限概率必须绑定 `drawdown_probability_contract`，至少包含 1 年、3 年或用户投资期限之一的明确预测区间、历史区块 Bootstrap/蒙特卡洛/情景树方法、区块长度、波动聚集、跳空、相关性突变、流动性冲击、参数不确定性和置信区间。不同期限和路径模型的概率不得直接比较。无法建立可信概率模型时，只报告历史与压力情景频率，不得输出伪精确概率。

固定回撤减仓/退出政策必须与至少三个预注册对照共同回测：不使用回撤止损、波动率目标缩放、趋势/市场状态过滤，以及可选的渐进风险预算缩减。审批必须比较净收益、回撤超调、恢复时间、低位卖出和反弹后追回成本；不得因为 10% 数字直观就假设固定止损天然优于其他风险控制。若用户损失上限与15%波动率或现有回撤政策冲突，采用更严格约束并增加现金，不得只保留看起来更宽松的百分比目标。

### 11.1 暂停、恢复与淘汰

- [ ] 本模块已实现、测试并通过验收

- 任何数据血缘、未来信息、成本、账本、重复记账或指标口径错误，立即暂停受影响策略和推荐，不等待收益表现确认。
- 成员回撤达到 8%、最近 60 个交易日超额收益 `≤ -5%`、最近 60 日 IR `< 0`，或连续两个滚动窗口未达准入线时，暂停新增买入推荐。
- 暂停后必须使用冻结版本复盘；只有数据和代码问题已修复、重新回测通过，并连续 20 个模拟交易日恢复正常，才允许重新启用。
- 成员回撤达到 10%、连续 120 个交易日未恢复、连续两个季度未达最低线，或策略经济逻辑已失效时，策略版本进入 `retired`，不得恢复，只能以新版本重新准入。
- 组合回撤达到 10% 时，模拟账户停止新增买入、生成退出建议并进入人工复核；系统不得为维持持仓而放宽风险阈值。

回撤风险状态机固定为：

`normal → caution → risk_reduced → risk_off → cooldown → staged_reentry → normal`

- 回撤使用剔除外部现金流影响的账户高水位 NAV 计算；盘中估算只能触发临时保护，正式状态由日终认证 NAV 确认。数据修订不得静默回写历史风险状态，只能产生更正事件和后续处置。
- `risk_off` 后进入至少 10 个交易日的 `cooldown`，期间只允许必要退出和现金管理，不因单日反弹立即恢复风险。
- 重新入场必须满足数据与账本正常、严重风险已关闭、策略仍通过生命周期复核、波动和流动性恢复、政策基准风险未继续恶化等预注册条件。
- `staged_reentry` 默认按 25% → 50% → 75% → 100% 风险预算分阶段恢复，每阶段至少经过 5 个交易日或一个策略决策周期；再次触发风险门时回到更严格状态。
- 回撤阈值、冷静期和恢复阶梯属于版本化风险政策，只能通过新版本和压力验证调整，禁止在亏损后临时放宽。

### 11.2 绩效核算与收益归因

- [ ] 本模块已实现、测试并通过验收

- 模拟账户和个人影子账户分别计算 `TWR`、`XIRR`、总收益、超额收益、已实现/未实现损益、股息、现金收益、费用和滑点；不同账户的结果不得拼接成一条收益曲线。
- 核心策略至少提供选股、行业、风格、仓位、交易时机、费用和残差归因；指数增强还必须提供相对基准的行业、风格和个股贡献。
- 组合层必须展示各策略收益贡献、边际风险贡献、相关性变化和现金拖累/保护作用；不得只展示总 NAV 和 Sharpe。
- 统一账户使用真实现金、持仓、订单和成交总账，同时为每个策略维护不可交易的 `strategy_virtual_subledger`。策略虚拟分账记录其独立目标、虚拟持仓、内部净额抵消、分配到的外部成交、成本、未成交和机会成本，用于公平归因；虚拟分账不得产生第二套真实订单。
- 同一证券被不同策略反向交易时，账户只执行净额订单。被内部净额抵消的数量按冻结归因规则以共同参考价在策略虚拟分账间转移；外部佣金、价差、冲击和未成交机会成本按边际订单、方向和执行参与度分配，不得简单平均或只分配给最后运行的策略。归因规则改变必须创建新版本，不得回写历史策略成绩。
- 回测、前向模拟、个人影子账户和用户自报实际收益使用相同指标名称时必须标明 `evidence_type`，禁止把任一层级冒充另一层级。
- 每个日终绩效快照固化基准版本、现金利率版本、公司行动、外部现金流和估值水位；缺少任一必要输入时绩效标记为未认证。

## 12. 统一模拟交易

- [ ] 本模块已实现、测试并通过验收

### 12.1 正式推荐

- [ ] 本模块已实现、测试并通过验收

月度核心及其他已审批、已启用策略只在各自决策时点的数据质量门通过后生成新目标；盘中策略只使用已完成 Bar 并在下一 Bar 执行。系统不得使用决策后才公布的数据改写历史建议。只有 `approval_status=approved`、`runtime_enabled=true` 且未暂停的策略可以参与账户级综合推荐；`forward_observation` 只能生成策略级观察信号并进入隔离模拟账户，不得进入个人影子账户的正式综合建议。

系统必须同时提供两种视图：

- `strategy_signal`：按实际启用的策略动态展示原始目标、方向和证据；未启用的周、日或分钟策略不显示占位信号。该视图用于研究、解释和归因，不得直接生成账户订单。
- `integrated_recommendation`：将所有已审批信号按资本预算净额合并，再根据协方差计算风险贡献并叠加风险和账户约束，为每只证券生成唯一可执行建议；模拟盘和个人影子账户只读取这一层。

系统必须区分三种事件：

- `evaluation_event`：月末、周末、日终或每个完整 Bar 的内部计算和检查，可以很频繁。
- `decision_event`：`final_target_weight`、动作、风险状态、目标有效性或订单释放状态发生实质变化。
- `notification_event`：需要用户关注的综合买卖、风险退出、目标失效或执行异常提醒；只有决策事件满足通知策略时才创建。

月频目标在月末发布后持续有效，日常风险检查不得重复推送同一月频建议。5min 可以每个完整 Bar 静默评估，但连续相同的等待、`HOLD` 或 `NO_ACTION` 不得重复创建通知。默认在以下任一情况发生时通知：综合动作改变；最终目标权重变化至少 0.5 个百分点且形成至少一个申报单位；风险状态升级或触发 `EXIT`；待执行目标变为可执行；订单失败、取消或建议失效。阈值进入版本化账户通知政策，硬风险事件不得被静音。

动作语义必须冻结为 `action_semantics_version`，并且由当前可执行持仓与最终目标共同决定：

| 动作 | 严格语义 |
| --- | --- |
| `BUY` | 最终目标高于当前可执行持仓，经济可交易性与全部硬门通过，需要新增净买入 |
| `SELL` | 最终目标低于当前可执行持仓，但目标仍可大于零，需要普通减仓 |
| `HOLD` | 当前持仓与仍有效的最终目标位于不交易区间内，不需要订单；表示维持已有有效目标，不表示系统没有计算 |
| `EXIT` | 风险、策略失效、退役或强制规则要求目标降为零或最低风险状态；优先级高于普通 `SELL` |
| `NO_ACTION` | 当前没有可发布的新账户决策，原因可以是无合格机会、数据/账户状态不足、策略未启用或目标无实质变化；不得用它掩盖应当明确展示的硬阻断或失败 |

`HOLD`、`NO_ACTION`、等待执行和订单未成交是四种不同状态。推荐必须另存 `action_reason_code`、`blocking_reason_code` 和 `target_validity_state`；前端不得只显示一个中文“观望”把它们合并。

| 字段 | 必填内容 | 用途 |
| --- | --- | --- |
| `recommendation_level` | `strategy_signal` / `integrated_recommendation` | 阻止策略级信号被误当成账户操作 |
| `event_type` | `evaluation_event` / `decision_event` / `notification_event` | 区分内部检查、决策变化和用户提醒 |
| `action` | `BUY` / `SELL` / `HOLD` / `EXIT` / `NO_ACTION` | 按冻结动作语义生成；无合格机会时必须允许 `NO_ACTION` |
| `action_reason_code` / `blocking_reason_code` / `target_validity_state` | 动作原因、阻断原因和目标有效性 | 区分维持目标、无新决策、风险退出、等待执行和失败 |
| `current_weight` / `final_target_weight` | 当前与最终综合目标权重 | 计算唯一建议交易量和组合风险 |
| `source_signal_ids` / `frequency_contributions` | 当前实际启用的策略来源及各自权重贡献 | 展示综合建议如何形成；未启用频率不得出现占位来源 |
| `pre_risk_target_weight` / `pending_target_weight` | 风控前目标及因执行择时暂缓的目标 | 区分目标被风险削减与等待买点 |
| `conflict_resolution` | 净额合并、风险覆盖和账户约束处理记录 | 解释不同频率信号冲突 |
| `expected_gross_benefit` / `expected_total_cost` / `no_trade_band` | 预期毛收益、完整成本与不交易阈值 | 只有经济收益足以覆盖成本和不确定性时才建议交易 |
| `quantity_hint` | 按用户选择的模拟账户或个人影子账户当前 NAV、现金、持仓和品种申报单位计算 | 仅供人工参考；即使启用只读账户同步，也不得据此生成或发送真实委托；未选择账户时才使用 500 万元验收口径 |
| `execution_window` | 建议日期、时段、最长延迟 | 模拟成交与人工执行参考 |
| `reason` | 因子、模型、风险或退出原因 | 可解释与复核 |
| `evidence` | 主要因子/模型贡献、历史分位、风险归因和证据类型 | 区分规则解释、模型分数和已校准概率 |
| `risk_flags` | 停牌、涨跌停、流动性、回撤、数据状态 | 任何硬阻断必须显示 |
| `lineage` | 数据集、策略版本、Recorder、成本与执行契约 | 保证推荐可复算 |
| `valid_until` | 失效时间或失效条件 | 过期建议不得继续显示为有效 |
| `last_evaluated_at` / `last_changed_at` / `last_notified_at` | 最近检查、变化和通知时间 | 防止把高频检查误解为高频建议 |
| `notification_reason` / `notification_dedup_key` | 通知原因与稳定去重键 | 连续相同状态不得重复推送 |

### 12.2 模拟订单、成交与账本

- [ ] 本模块已实现、测试并通过验收

- 只使用 `simulation_*` 持久账本，不恢复 `paper_*` 或 `pair_paper_*` 为活跃生产路径。
- 主模拟账户初始现金固定为 500 万元；另可建立压力账户，但不得替代主账户验收。之后的建议数量使用账户当前 NAV，不得持续按初始本金计算。
- 账户以 `source_type + source_id + execution_dataset` 绑定推荐组合、已审批策略版本或已审批分配版本；旧 recommendation 账户自动映射。
- Qlib 负责生成目标、订单计划和回测执行证据；PostgreSQL 负责现金、订单、成交、持仓、NAV、事件、重启恢复和审计。
- 长仓执行适配器支持 1min/5min、按证券主数据确定的 T+0/T+1、停牌、涨跌停、申报单位、税费、流动性和参与率。
- 配对适配器支持卖空资格、借券成本和双腿原子成交；任一腿不可执行时整组拒绝。
- 策略、Qlib 回测、执行回放和模拟批次共享 `execution_contract_hash`；不一致时 fail closed。
- 批次、订单、成交和 NAV 具有稳定幂等键；重启和重试不得重复记账。
- 未成交订单必须记录原因并按策略规则取消或延期；禁止在没有可成交 Bar 时按收盘价虚构成交。
- 每日收盘后对账现金、持仓、成本、公司行动、NAV、回撤和基准收益；任何不平衡都阻断下一批推荐。
- 每张模拟订单必须经过明确生命周期：`planned → released → accepted → partially_filled → filled | cancelled | rejected | expired`。订单需要保存订单类型、限价或参考价、有效期、释放时间、撤单/改价原因、冻结资金或数量、每笔成交和剩余数量；跨日订单必须由策略合同明确允许，不得默认永续有效。
- 模拟成交和个人建议必须区分集合竞价、连续竞价、收盘集合竞价和不可交易时段。没有盘口数据时不得声称精确模拟限价队列位置，只能按保守的 Bar 成交假设和参与率模型处理。

| 模拟来源 | 前置条件 | 执行适配器 |
| --- | --- | --- |
| `recommendation` | 推荐组合有效且血缘一致 | `long` |
| `strategy_version` | 策略版本已审批，Qlib 证据完整 | `long` 或 `pair` |
| `allocation_version` | 分配已审批，成员模拟 NAV 已认证 | 按成员路由，统一账户记账 |

#### 12.2.1 执行证据等级与实现差额

仅有日线或分钟 OHLCV 时，系统无法知道盘口买卖价差、队列位置、逐笔成交先后和真实限价单是否能够成交，因此必须为每个执行结果标记证据等级：

| `execution_evidence_type` | 定义 | 允许声明 |
| --- | --- | --- |
| `historical_bar_backtest` | 使用历史日线或分钟 Bar 的规则化回测 | 只证明在冻结 Bar 假设下的理论可执行性 |
| `historical_execution_replay` | 使用历史分钟数据、下一 Bar、参与率和订单状态回放 | 证明保守执行模型下的历史结果，不证明真实排队成交 |
| `realtime_bar_simulation` | 当时实际抓取的实时完整 Bar 驱动模拟订单 | 证明系统在当时数据水位和延迟下能够运行 |
| `user_confirmed_execution` | 用户确认或导入真实人工成交 | 只证明该次用户确认结果；不等于券商只读同步，更不等于自动交易 |
| `broker_read_only_execution_snapshot` | 用户主动启用的只读适配器返回当日成交和订单终态 | 只证明券商接口报告的只读状态；不得产生任何委托权限，仍需处理结算延迟和接口差异 |

模拟订单必须保存决策价、到达价、计划基准价、模拟成交价、同期市场 VWAP/TWAP、用户确认成交价、未成交数量和失效时点，并计算 Implementation Shortfall，至少拆分为市场移动、模拟滑点/冲击、费用、人工延迟、未成交机会成本和用户执行偏差。`user_confirmed_execution` 和通过一致性校验的 `broker_read_only_execution_snapshot` 可用于校准真实成交价与执行偏差；人工响应和人工操作延迟仍需结合通知、用户确认与账户快照时间线估计。历史 Bar 回放不得冒充真实成交样本。

没有 Tick、Level-2 或逐笔数据时，系统不得宣称精确复现盘口、限价队列或撮合顺序。分钟提醒的产品名称和界面必须使用“分钟级信号”“建议执行窗口”或“下一 Bar 模拟”，不得使用暗示确定成交或绝对精确性的表述。

在用户未提供当日最新账户快照、只读同步不可用，或人工成交和未完成委托状态不完整时，分钟级能力只能承诺市场/策略信号、模拟订单和通用执行窗口，不能承诺个人账户的精确可买数量、精确可卖数量或已考虑全部未成交委托的个性化开仓建议。此时分钟新开仓个人建议必须降级为 `simulation_only` 或目标权重提示；紧急退出仍可提醒，但必须明确可卖数量待用户确认。

#### 12.2.2 多日执行与过渡组合风险

任何跨 Bar、跨午休或跨交易日执行都必须保存旧目标、新目标、当前实际持仓、已成交数量、剩余数量、失效时间和 `transition_portfolio_contract_version`。系统不得只在计划开始和全部完成时检查组合约束。

- 每次部分成交后重算过渡组合的风险、现金、底层暴露、Tracking Error、行业/风格偏离、换手、容量和清仓天数。
- 卖出和买入顺序必须考虑资金释放、T+1 可卖、风险暴露和人工执行能力；禁止默认先完成所有买入再等待卖出。
- 剩余订单的顺序和切片可以在冻结执行目标内动态修复，但不得利用执行期间新出现的 Alpha 信息偷偷改变正式目标。
- 原目标失效、数据水位失败、风险状态升级或过渡组合超限时，未完成订单必须取消、缩减或生成新的决策事件。
- 绩效归因必须分开报告目标选择收益、执行期间市场移动、过渡组合暴露、成交成本和未成交机会成本。

### 12.3 会计、交收、税务与估值

- [ ] 本模块已实现、测试并通过验收

模拟账户和个人影子账户必须使用可审计的复式记账或具备等价守恒约束的双边分录。订单、成交、公司行动、费用、税款、入金、出金和估值变动不得直接覆盖余额；每个事件必须生成可追溯分录，并满足：

- `资产 = 负债 + 净资产`。
- `期末净资产 = 期初净资产 + 投资损益 + 外部净现金流`。
- 现金、证券数量、成本、应收应付和累计费用可由事件流独立重放得到。

账户必须区分：

- 账面现金、可用资金、冻结资金、在途资金、可取资金和最低保留现金。
- 总持仓、可卖数量、冻结数量、待交收数量和公司行动待处理数量。
- 应收股息、应收退市结算款、应付费用、应付税款和其他待结算项目。

每次买入形成不可变 `tax_lot_id`，记录证券、买入日期、成交数量、剩余数量、原始成本、费用和公司行动调整。卖出、转托管式手工调整和公司行动必须按版本化批次匹配规则减少具体税务批次；平均成本只能用于展示，不得替代持仓期限和股息红利税计算所需的批次记录。

股息红利税、印花税、过户费及其他税费按照证券类型、持有期限、发生日期和规则版本计算。无法精确确定个人券商实际扣税时，模拟账本保存估算税和实际导入税两列，并将差异进入影子账户对账，不得修改历史成交价格来抵消。

估值必须保存 `valuation_price`、`valuation_source`、`valuation_at`、`price_age` 和 `valuation_policy_version`。正常交易证券优先使用当日有效收盘价；停牌、长期无成交、退市整理、异常 ETF 溢价或数据缺失时采用预注册估值层级并显示陈旧价格风险。缺少可靠估值时 NAV 标记为未认证，禁止把最后可见价格静默当作当前可变现价值。

### 12.4 个人影子账户、只读同步、人工成交与偏差对账

- [ ] 本模块已实现、测试并通过验收

- 个人影子账户支持手工录入、CSV 导入和默认关闭的只读同步，至少包含账户日期、现金、证券、持仓数量、可卖数量、成本、当日成交、未完成委托、费用、入金、出金、分红和备注。
- 每次导入先形成不可变候选快照，经过证券代码、重复行、现金守恒、持仓非负、申报单位和日期检查后才能生效；修正错误必须追加新版本，不能覆盖旧快照。
- 只读同步必须使用独立进程、独立网络出口、最小权限令牌和允许列表接口；运行时拒绝任何委托、撤单、改单、资金、账户设置和通用浏览请求。每次启动和定期运行都验证权限范围，权限扩大、接口行为不明或审计缺失时立即禁用并回退到手工/CSV。
- 正式推荐同时生成“模拟账户计划”和“个人影子账户建议”。后者根据用户实际持仓计算差额，并明确区分目标、建议数量、用户确认成交和未执行数量。
- 用户可以确认全部成交、部分成交、未成交或在系统外成交；系统据此更新影子账户并记录原因，但不得自动假设用户按建议价格完成交易。
- 每日对账展示影子账户与模型组合在现金、持仓、行业、风格、风险、收益和费用上的偏差。偏差超过阈值时，系统给出再平衡建议或风险提示，不得篡改历史建议来消除偏差。
- 个人影子账户数据属于敏感本地数据，必须加密存储、最小权限访问并纳入备份恢复；日志和研究制品不得包含完整账户明细。
- 每次生成个人建议前必须检查账户状态新鲜度。超过账户政策规定的最大快照年龄、存在未确认订单或现金/可卖数量置信度不足时，建议降级为目标权重与待确认提示；依赖即时状态的分钟新开仓正式建议直接阻断。
- 人工干预必须记录 `manual_override_type`、原因、时间、原建议、实际动作和实际成交。模型原始计划、模拟执行、通知送达、用户确认和用户实际收益分别核算；不得只选择执行看起来有利的建议，再用个人账户收益反向证明模型有效。

## 13. 审批、审计与 readiness

- [ ] 本模块已实现、测试并通过验收

- 策略、因子、分配和模拟操作均执行认证、权限检查、审批状态机和不可变审计。
- readiness 汇总数据血缘、水位线、市场规则版本、Qlib/RD-Agent 版本、测试/制品、模型漂移、策略状态、模拟/影子账户证据、风险事件、过期批次和恢复能力。
- 任何严重风险、过期批次、血缘不一致、频率混用、契约哈希不一致或 Tushare 权限缺口，都必须显式阻断相应阶段。
- 删除重复实现之前必须完成新旧结果回归与生产引用扫描；删除后扫描确认不存在第二条活跃研究、回测、优化或模拟路径。

## 14. 调度、告警、备份恢复与界面

- [ ] 本模块已实现、测试并通过验收

- 连续研究、参数实验、数据构建、Qlib 回测、模拟回放和分配刷新由统一调度器编排，具备重试、取消、超时和幂等。
- 告警覆盖数据缺口、血缘、准入失败、模型漂移、回测异常、执行阻断、账户偏差、回撤状态、过期批次、备份和恢复演练。
- 备份包含 PostgreSQL、加密的个人影子账户、不可变快照索引、Qlib 数据集/Recorder 制品和必要配置；恢复后重新校验密钥指纹、哈希与 readiness。
- 持久账本和审批状态默认恢复点目标 `RPO ≤ 15 分钟`、恢复时间目标 `RTO ≤ 4 小时`；不可变市场数据和 Qlib 制品可从哈希索引重建，但重建成功前系统保持 fail closed。至少每季度完成一次隔离恢复演练。
- 系统必须提供全局 `safe_mode`。进入安全模式后停止产生新 Alpha 风险、释放新开仓订单和晋升研究候选，但继续数据落盘、账本对账、已有订单终态处理、必要退出提醒、备份和审计。退出安全模式需要 readiness 全部恢复并留下明确审批记录。
- 开发、研究验证和正式模拟环境必须隔离数据库、密钥、制品目录和任务队列。开发或 RD-Agent 候选不得读取正式个人影子账户明细，不得把未审批模型写入正式模拟环境。
- 运营 SLO 至少覆盖数据任务成功率、数据水位达标率、日终对账完成率、推荐生成延迟、分钟信号端到端延迟、通知送达率、账本不平衡次数和备份恢复演练成功率。SLO 阈值按阶段和策略版本冻结；连续违约必须自动降级或进入 `safe_mode`，不能只显示告警后继续运行。
- 回放与恢复必须支持确定性虚拟市场时钟，固定事件顺序、交易日历、随机种子、数据水位和依赖版本。相同快照、策略版本、账户起点和 `execution_contract_hash` 必须重放出相同决策、订单、成交、分录和 NAV；不一致时阻断认证。
- 系统固定生成每日行动简报、周度研究与风险复核、月度绩效与归因报告、季度策略生命周期报告，以及模拟账户—个人影子账户偏差报告。每份报告必须标明证据类型、数据水位、策略/模型版本、未解决风险和是否需要用户操作。
- 分钟数据、原始快照、Qlib 数据集、Recorder、模型权重、日志和账本必须建立容量预算、分区、压缩、冷热分层、保留周期与归档校验。账本、审批、推荐和正式证据不得因磁盘压力自动删除；研究临时制品只能按已审批规则清理。
- Tushare 数据权限、用途限制和再分发边界进入数据治理。个人本地研究与未来公网、多用户或商业化场景分别审批；权限或许可不覆盖目标用途时系统保持阻断，不得因技术上可以下载就默认允许对外展示或分发。
- 控制台统一为数据、RD-Agent、因子准入、Qlib 回测、审批、核心/卫星分配和模拟交易。
- 配对入口只暴露研究、审批和模拟；QMT/券商交易网关不得从主导航、调度和模拟任务触发。可选只读账户状态适配器只能出现在账户设置、数据源状态和对账页面，不得与买卖按钮、委托预览或交易网关共用入口。

### 14.1 固定运行日历

- [ ] 本模块已实现、测试并通过验收

具体时间可以配置，但任务频率、先后关系和阻断规则不得改变。所有时间均使用 `Asia/Shanghai`，并以交易所交易日历为准。

| 频率与默认时间 | 任务与启用条件 | 主要执行者 | 产出 |
| --- | --- | --- | --- |
| 每个交易日 17:30 起 | 开始拉取日线、复权、参考数据和历史分钟；各数据域达到独立水位后分别冻结，不能因到达固定时钟就假设完整；日度风险门必跑，Swing 仅在已审批且启用时运行 | 项目调度器调用 Tushare 与 Qlib | 已认证数据快照、风险检查；有启用日频策略时才产生日频策略信号 |
| 每月最后一个交易日 18:00 | 所有已审批月频核心策略完整计算、核心/卫星基础分配、资产类别、行业和跟踪误差检查 | 项目调度器调用 Qlib | 下一月 `w_monthly_core`；不合格时保持现金或 `NO_ACTION` |
| 每周最后一个交易日 18:15 | 仅当存在已审批且启用的周频策略时运行；否则跳过正式周频信号任务 | 项目调度器调用 Qlib | 启用时产生下一周周频目标；未启用时不生成占位信号 |
| 任一到期策略或风险状态完成计算后 | 只等待本次应运行且已启用的任务；净额合并当前所有有效目标并执行风险、容量、市场规则和个人账户约束 | 项目推荐与风险治理层 | 目标或动作变化时更新唯一 `integrated_recommendation`；状态不变只记录评估 |
| 每周五 20:00 或周末 | 最近一周绩效、因子 IC、成本、容量、回撤和数据质量复核；按预算运行挑战者研究 | 项目治理层；RD-Agent 调用 Qlib | 周报、风险状态和研究候选；不得直接替换正式策略 |
| 每季度首个周末 | 准入线重检、策略继续/暂停/淘汰决定、成本和上游版本复核 | 项目治理层 + 人工审批 | 生命周期决定和新版本需求 |
| 每个交易日 08:30 | 服务、交易日历、数据权限、市场规则版本、当前持仓、按品种计算的可卖数量、停牌/风险状态和未完成订单检查 | 项目调度器与风险治理层 | 当日 readiness；失败则禁止盘中正式推荐 |
| 盘中每个完整 1min/5min Bar 后 | 有待执行目标时检查执行时机；分钟卫星仅在已审批且启用时运行；每次 Bar 只形成内部评估，变化后才形成决策或通知 | 项目调度器调用 Tushare、Qlib 和模拟交易系统 | 状态变化时更新综合建议或模拟成交；连续相同状态不重复提醒 |
| 每个交易日 14:50 | 停止创建新的盘中开仓信号；继续处理允许的退出、未成交订单和盘中批次封账 | 项目模拟交易系统 | 当日盘中订单终态 |
| 每个交易日 20:30 | 模拟账户对账、公司行动、费用、NAV、回撤、基准比较、历史/实时分钟对账和告警 | 项目模拟与风险治理层 | 日终账本、风险状态和阻断决定 |

同一天同时属于周末和月末时，固定顺序为日线数据冻结与日度风险层 → 月度战略层 → 已启用的周度策略 → 唯一综合推荐。月度或周度策略在非调仓日只接受风险和有效性检查，原目标继续有效；没有实质变化时不创建新策略批次，也不重复输出 `HOLD`、`NO_ACTION` 或用户通知。

历史分钟数据可能在收盘后较长时间内陆续完成，17:30 只是默认拉取起点，不是完整性保证。日线策略只等待自身所需的日线、复权、市场规则和 PIT 水位；需要历史分钟执行复核的批次等待分钟水位至策略最晚截止时间，仍不完整时标记 `pending_review` 或 `NO_ACTION`。不同接口的单次代码数、行数、调用频率和权限分别配置并压测；A 股实时分钟支持的多代码批量能力不得套用到仅支持单代码的 ETF 接口，候选池上限必须按最慢接口、实际延迟分位数和备用重试预算计算。

### 14.2 1分钟/5分钟策略与5分钟执行的区别

- [ ] 本模块已实现、测试并通过验收

必须分开记录并展示以下两种模式：

| 模式 | 信号何时变化 | 分钟数据的作用 | 典型策略 |
| --- | --- | --- | --- |
| 月度及已启用的周度/日线信号 + 5min 执行 | 只在各自正式决策时点改变目标仓位 | 按 5min VWAP 或买点门切片执行既定综合目标，不重新决定选股方向 | ETF 配置、指数增强，以及已批准启用的周频、全市场多因子或 Swing |
| 1min/5min 盘中信号 | 每个已完成的 1min/5min Bar 都可以重新计算 | 既用于生成信号，也用于下一 Bar 模拟成交 | T+0 ETF 分钟回归、股票隔夜回归的入场和以后批准的盘中卫星 |

低频目标的 5min 执行择时与独立分钟策略必须使用不同的 `strategy_version`、资本预算、风险预算和归因记录。前者只能释放、延迟或取消既定订单；后者可以在自己的卫星预算内形成新仓位，但仍必须先进入唯一账户级综合推荐。

每个完整 Bar 都可以产生 `evaluation_event`，但只有目标、动作、风险或订单释放状态发生实质变化时才产生 `decision_event`；只有符合通知政策时才产生 `notification_event`。因此“5分钟运行”不等于“每5分钟给用户发一次买卖提示”。

1min/5min 盘中策略固定流程：

1. 前一交易日收盘后建立次日候选池，并冻结策略版本、候选池、成本模型和执行契约。
2. 交易日 08:30 完成 readiness；任何权限、数据、持仓或风险硬门失败时，当日盘中策略不启动。
3. 交易时段内，每个完整 Bar 结束后拉取实时分钟数据；5min 策略默认在 09:35–11:25、13:05–14:50 的完整 5min Bar 结束后生成新开仓信号，集合竞价、午间休市和没有下一可执行 Bar 的时点不生成新开仓信号。
4. 项目数据适配器完成去重、单位、时间戳和完整性检查后，Qlib 计算因子、模型分数和目标仓位。
5. 项目风险治理层检查审批状态、按品种计算的 T+0/T+1 可卖数量、停牌、涨跌停、参与率、现金、仓位、回撤和失效条件。
6. 通过风险门后重算综合状态；状态不变时只记录评估，状态变化时创建决策事件；被风险阻断时保存 `NO_ACTION` 或风险退出原因，不得静默跳过。
7. 信号只能在下一可执行 Bar 模拟成交。例如 10:05 完成的 5min Bar 产生信号，最早使用 10:06 的 1min Bar 或 10:10 的下一根 5min Bar。
8. 模拟交易系统计算佣金、印花税、过户费、滑点、冲击成本和未成交数量，并立即更新持仓与风险状态。
9. 收盘后使用历史分钟数据复核实时分钟、信号、成交和 NAV；复核失败时冻结当日结果并告警。

分钟 Bar 较短不自动等于高频。只要不使用 Tick/Level-2、没有毫秒级抢单、持仓周期和换手率受控，1min/5min 策略可属于本系统允许的中低频盘中策略；但必须逐个策略通过成本、容量、换手和样本外准入。

RD-Agent 不参与上述盘中循环。它只在周度、月度或人工研究任务中提出和改进候选；盘中生产链只运行已经冻结、审批且准入通过的 Qlib 策略版本。

### 14.3 RD-Agent 沙箱、资源预算与供应链安全

- [ ] 本模块已实现、测试并通过验收

- RD-Agent 生成和执行的代码必须运行在一次性隔离沙箱中，默认禁止访问生产数据库、Tushare Token、账户数据、部署密钥和宿主机敏感目录。
- 沙箱默认断网；确需下载公开依赖或论文时使用域名白名单、内容哈希和缓存代理。生产数据以只读快照挂载，输出只能写入指定候选制品目录。
- 每个研究活动固化 LLM 金额/Token、候选次数、CPU、内存、GPU、磁盘、并发、单次运行时间和总运行时间预算；超限时终止并保留失败证据，不得自动扩容绕过预算。
- 每次研究固化 LLM 提供商、模型标识、模型版本、温度等采样参数、系统/任务提示词哈希和工具权限；模型或提示词变化视为新的研究运行条件。
- 新增 Python/系统依赖必须经过包名和版本白名单、许可证检查、漏洞扫描和锁文件固化；禁止候选代码执行任意安装脚本、特权容器、Docker Socket 或未审核二进制文件。
- 候选代码先通过静态扫描、单元测试、确定性复算、资源上限和数据访问审计，再允许进入 Qlib 挑战者实验。研究成功不代表安全审核通过。
- 日志、提示词、LLM 响应和研究制品必须脱敏；任何密钥、个人影子账户明细或未授权数据泄露都属于严重风险，立即吊销相关运行制品并暂停自动研究。

### 14.4 通知送达、确认与人工执行闭环

- [ ] 本模块已实现、测试并通过验收

`notification_event` 只是需要通知的业务事件，不等于用户已经收到。通知服务必须支持至少一个主渠道和一个备用渠道，并保存：

- `decision_at`、`notification_created_at`、`send_attempted_at`、`sent_at`、`delivered_at`、`acknowledged_at`、`expired_at`。
- 渠道、模板版本、通知内容哈希、失败原因、重试次数、备用渠道升级和稳定去重键。
- 用户确认的全部执行、部分执行、未执行、延后执行和系统外成交，以及确认时间和实际成交信息。

普通调仓、风险告警和紧急退出使用不同通知等级。硬风险事件不得被免打扰策略静音；主渠道失败或规定时间内未送达时必须自动切换备用渠道。通知重复发送必须复用同一业务事件，不得生成多份互相冲突的建议。

每个策略版本必须冻结端到端延迟预算：从数据源事件、Bar 完成、数据抓取、质量检查、信号计算、风险合成、通知创建到用户送达分别计时。只有实测延迟分位数满足策略持有期和执行窗口时，才能声明具备相应分钟提醒能力；Tushare 或通知渠道没有确定延迟保证时，系统必须使用保守失效时间并允许 `NO_ACTION`。

通知服务、系统时钟、时区或网络不可用时：

- 月/周/日建议可以保留为控制台待确认事件，但不得声称已提醒用户。
- 依赖即时人工响应的分钟新开仓策略 fail closed；已有风险仓位继续生成控制台退出事件和恢复后的升级通知。
- 所有服务使用可信时钟同步并监控时间漂移；超过容忍度时禁止生成依赖精确 Bar 截止时间的信号。

用户未确认不等于未看到，也不等于已经执行。模拟账户继续按其独立执行合同运行；个人影子账户只在用户确认成交、导入新快照或接收到通过权限与一致性校验的只读账户快照后更新。系统必须展示“模型计划、模拟成交、通知送达、用户确认、实际成交”五层状态，禁止把其中任一层冒充另一层。

通知和推荐质量不能只按“发送成功”验收。系统必须按策略、频率和通知等级报告：每周/月提醒数量、重复率、过期率、无效后撤回率、用户确认率、确认延迟、实际执行率、部分执行率、人工偏离、错过执行窗口比例、实际 Implementation Shortfall 和提醒后未产生任何可执行变化的比例。指标只用于评估产品可用性和人工执行摩擦，不得选择性删除用户未执行或亏损的建议来美化策略成绩。提醒过密、重复或多数过期时必须降低通知频率、收紧经济可交易性门或将相关策略降级为 `simulation_only`。

### 14.5 上游故障、基线回退与供应商退出

- [ ] 本模块已实现、测试并通过验收

每个正式策略必须冻结 `fallback_policy_version`，回退只允许沿预注册顺序执行，禁止故障发生后临时选择表现最好的一条路径：

1. 当前已审批模型和策略版本正常时继续运行。
2. 当前版本发生可逆的推理或制品故障，且旧批准版本的数据/特征/市场规则仍兼容时，可回退到最近一个未退役的已审批版本。
3. 旧批准版本不兼容或证据过期时，只能回退到该策略合同预注册的简单基线、保持现有目标、降低风险或全现金。
4. 数据水位、PIT、市场规则、账户账本或执行契约失败时不得回退到另一模型继续新增风险，直接进入 `NO_ACTION`/`safe_mode`，同时保留必要退出提示。

Qlib、RD-Agent、LLM、Tushare、求解器、数据库、通知渠道和对象存储必须作为独立故障域处理：

- RD-Agent/LLM 故障只暂停新研究，不影响已审批生产推理。
- Qlib 推理或求解器故障阻断新目标，但不阻断账本、对账、已有订单终态和风险通知。
- Tushare 短期故障保持最后一个已认证目标的有效性规则，并禁止使用过期数据新增风险；长期或授权终止按 `provider_exit_plan` 进入只读、导出、迁移和重新准入。
- 主通知渠道故障按备用渠道升级；全部通知不可用时，依赖即时响应的分钟新开仓保持关闭。

至少每年演练一次旧批准版本回退、简单基线回退、全现金、Tushare 长期不可用、LLM/RD-Agent 完全不可用和求解器升级回滚。任何回退产生新的目标时都必须生成新的决策事件、显示证据等级和回退原因，不得覆盖原建议。

## 15. 公共接口与迁移规则

- [ ] 本模块已实现、测试并通过验收

| 对象 | 新增/固定字段 | 迁移原则 |
| --- | --- | --- |
| 研究组件 | `component_id`、版本、`slot_type`、频率、窗口单位、输入/输出契约、参数范围、代码哈希、适用模板 | 第一版策略思想全部进入组件库；历史定义不可原位覆盖 |
| 模板合同 | `template_id`、冻结字段、必填/可选插槽、槽位类型、最大组件数、标签和评价契约 | RD-Agent 只能提交 `slot_bindings`，改变冻结字段必须创建模板主版本 |
| 策略分类 | `engine_type`、`strategy_family`、`recipe_id` | 执行引擎、经济假设和具体配方分离；旧 `strategy_type` 只作历史兼容读取，不得作为新生产字段 |
| 原子试验 | `atomic_trial_id`、`experiment_family`、`research_program_id`、模板和槽位绑定 | 任一被评分配置都逐项登记，跨入口和改名不得清零 |
| 试验预算 | `trial_budget_contract`、`N_eff`、候选/参数/种子/特征子集上限、功效假设 | 预算由有效样本和模型复杂度决定，不使用固定候选乘参数上限 |
| 自适应研究统计 | `adaptive_research_inference_contract_version`、探索/确认阶段、锁箱可见性、序贯方法和跨程序预算 | RD-Agent 自适应搜索不得把普通验证显著性冒充独立确认 |
| 市场状态与基线 | `market_regime_contract_version`、`baseline_suite_version` | 阶段定义和简单基线必须预注册，不得按结果选择 |
| PIT 数据质量 | `pit_quality`、`provider_available_at`、`reconstruction_method`、`revision_id`、`coverage_ratio` | 当前修订值不得冒充历史可得版本；正式回测只允许合格等级 |
| 收益/标签/特征合同 | `return_contract_version`、`label_contract_version`、`feature_materialization_contract_hash` | 离线研究、历史回放和实时逐步重放使用同一语义与核心计算代码 |
| 策略配方 | `full_market_multifactor`、`weekly_tactical_overlay` | 旧配方不变，新契约用新版本 |
| 组合方式 | `industry_neutral_qp` | 保留历史标识；正式实现是项目基准相对 QP，Qlib 优化器只作经约束复核的对照 |
| 策略版本 | `signal_frequency`、`decision_frequency`、`rebalance_frequency`、`monitor_frequency`、`signal_period`、`execution_frequency`、`execution_contract_hash` | 历史行不可变 |
| 策略运行状态 | `approval_status`、`runtime_enabled`、`paused_at`、`retired_at` | 只有已审批、已启用且未暂停的版本贡献正式目标；启停不修改历史策略版本 |
| 综合推荐 | `source_signal_ids`、`frequency_contributions`、`pre_risk_target_weight`、`final_target_weight`、`pending_target_weight`、`conflict_resolution` | 策略级信号分别保存；只有账户级净额结果进入订单和影子账户 |
| 推荐事件 | `event_type`、`last_evaluated_at`、`last_changed_at`、`last_notified_at`、`notification_reason`、`notification_dedup_key` | 高频检查可以静默；相同决策不得重复通知 |
| 证券市场规则 | `asset_subtype`、`settlement_cycle`、`lot_size`、`price_tick`、`market_rule_version` | 按生效日期新增版本，禁止覆盖历史 |
| 上游适配器 | Qlib/RD-Agent version + commit | 升级触发兼容和量化回归 |
| 数据 Provider | `provider_contract_version`、`provider_exit_plan_version`、字段映射、PIT/单位/复权能力 | Alpha 和市场数据当前只启用 Tushare；官方控制/参考数据使用隔离 Provider，替代研究数据商必须重新准入 |
| 模型版本 | 特征/标签契约、训练数据、代码/权重哈希、随机种子、漂移状态 | 重训创建新版本，旧版本可回放和回滚 |
| LLM 研究身份 | 模型/提示词/工具、知识截止声明、检索资料、`idea_available_at`、`historical_knowledge_risk` | 历史知识可能污染锁箱时提高前向证据要求 |
| 预测契约 | `score_semantics`、`forecast_horizon`、`calibration_method`、`expected_return`、`forecast_uncertainty`、校准窗口和版本 | 未经样本外校准的排名分数不得冒充收益率或概率 |
| 指标数学契约 | `metric_contract_version`、公式、基准、现金利率、年化、回撤和风险概率方法 | 同名指标只在契约一致时比较 |
| 优化器合同 | `optimizer_contract_version`、目标函数、约束、惩罚、求解器/数值环境、容差、`discrete_repair_contract` | 连续解和整手修复后都必须重新验证全部硬约束 |
| 策略准入合同 | `admission_contract_version`、策略类别、主要/次要指标、硬门和回退 | 指数增强、ETF 配置、卫星、配对和执行算法分别准入 |
| 基准证据 | `benchmark_evidence_type`、官方/合成/可投资类型、构建版本、跟踪差异和校验证据 | 合成全收益代理不得冒充官方指数；误差超限时不得用于正式准入 |
| 分配成员 | `role`、`capital_budget`、`target_volatility`、`risk_budget`、`member_cap`、实际风险贡献 | 资本权重与风险预算分离；旧 `risk_budget × strategy_weight` 不得继续作为生产合成公式 |
| 组合风险诊断 | 有效样本、特征值、条件数、风险贡献、预算偏差、相关性置信区间、回撤超调和损失概率 | ERC 风险预算不成立时失败关闭；其他组合允许可解释且经压力验证的对冲性负贡献 |
| 回撤概率契约 | `drawdown_probability_contract`、预测期限、路径方法、区块长度、压力假设和置信区间 | 不同期限或模型的 `P(MDD>x)` 不得直接比较 |
| 模拟账户 | `source_type`、`source_id`、`execution_dataset` | 旧 recommendation 自动映射 |
| 个人影子账户 | `account_snapshot_id`、`import_hash`、`account_current_nav`、`account_state_source`、`account_state_asof`、状态置信度、只读权限证据、`confirmed_at` | 手工/CSV 或默认关闭的只读同步；修正采用追加版本，状态过期时降级建议 |
| 人工执行政策 | 单日证券数、单批操作数、响应窗口、未确认上限、降级和升级规则 | 超出个人处理能力的策略或批次只能进入模拟盘 |
| 个人投资政策 | 白/黑名单、账户级仓位/现金/回撤/流动性限制、生效日期 | 只能收紧正式策略约束，变更创建新版本 |
| 订单/成交/持仓 | `atomic_group_id`、`leg_no`、`position_side`、`borrow_cost` | 配对原子组不可部分落账 |
| 执行证据 | `execution_evidence_type`、决策价、到达价、基准价、模拟/人工成交价、Implementation Shortfall | Bar 回放不得冒充真实排队成交或用户实际执行 |
| 绩效快照 | `benchmark_version`、`cash_rate_version`、`evidence_type`、`external_cash_flows` | 回测、模拟、影子账户分开核算 |
| 本位币/通胀/固定收益 | `currency_contract_version`、`inflation_contract_version`、`fixed_income_risk_contract_version`、数据可用时间和陈旧度 | 人民币总收益、实际收益、久期/信用风险和跨境错时不得隐式处理 |
| 目标负债 | `goal_liability_contract_version`、资金桶、目标日期/金额/币种/优先级、成功率区间和资金缺口 | 投资建议必须服务个人用款目标，不只追求回测收益 |
| 平台总成本 | `platform_total_cost_contract_version`、数据/LLM/算力/存储/通知/维护时间和分摊版本 | 同时报告投资净收益与扣除平台成本后的个人净增量价值 |
| 过渡组合 | `transition_portfolio_contract_version`、旧/新目标、实际持仓、剩余订单和过渡风险 | 多日执行每次部分成交后重算约束，不只检查起点和终点 |
| 个人投资声明 | 可投资资产、投资期限、用款计划、风险能力、人民币损失上限、`policy_benchmark_version` | 至少年度复核；变更只影响生效日后建议 |
| 家庭/系统外暴露 | `household_exposure_policy`、覆盖范围、账户/资产汇总、陈旧度和置信度 | 子账户报告不得冒充总资产风险；家庭视图只读且不生成跨账户调拨 |
| 风险状态 | `risk_state`、`high_watermark_nav`、`cooldown_until`、`reentry_stage`、`risk_policy_version` | 回撤退出、冷静期和分阶段恢复均不可跳过 |
| 账户分录 | `journal_entry_id`、借贷方向、账户科目、金额/数量、事件来源、规则版本 | 余额必须可由不可变分录重放并通过会计恒等式 |
| 策略虚拟分账 | `strategy_virtual_subledger`、内部净额转移、外部成交/成本/机会成本分配版本 | 只用于归因，不得形成第二套真实订单 |
| 持仓税务批次 | `tax_lot_id`、买入日期、原始/剩余数量、成本、持有期、税务规则版本 | 平均成本不得替代持有期税务和批次核算 |
| 估值记录 | `valuation_price`、`valuation_source`、`valuation_at`、`price_age`、`valuation_policy_version` | 陈旧或不可验证价格使 NAV 进入未认证状态 |
| 通知投递 | 发送/送达/确认/过期时间、渠道、重试、升级、内容哈希 | 业务通知与实际送达分离；硬风险使用备用渠道 |
| 动作语义与推荐质量 | `action_semantics_version`、原因/阻断代码、目标有效性、提醒数量/重复/过期/执行偏差指标 | `HOLD`、`NO_ACTION`、等待和未成交不得合并；质量指标不得选择性删除失败建议 |
| 故障回退 | `fallback_policy_version`、旧批准版本、简单基线、现金和触发原因 | 只能沿预注册顺序回退；数据/账本硬门失败时禁止通过换模型新增风险 |
| 研究与运营制品 | 容量预算、分区、压缩、冷热层、保留周期、归档哈希和许可范围 | 正式证据不可因磁盘压力自动删除；数据用途必须在授权范围内 |

## 16. 测试与验收

- [ ] 本模块已实现、测试并通过验收

### 16.1 自动化覆盖

- [ ] 本模块已实现、测试并通过验收

- 文档白名单、唯一 Markdown 权威稿、两个冻结 DOCX、OOXML 有效性、本地链接和退役路径扫描。
- Qlib/RD-Agent 固定版本、导入、运行、Recorder/制品兼容，以及仓库无第二条活跃生产路径。
- 阶段 0 数据能力矩阵、权限证据、历史深度、PIT 可恢复率、接口延迟/限流、存储和重建容量。
- 标准化 provider 字段契约和 `provider_exit_plan`；Tushare 字段变化、长期故障、授权终止及替代供应商迁移不得触发静默换源。
- PIT 股票池、行业、财务和退市股票；因子、频率、重采样、下一 Bar 执行和反泄露。
- PIT 可恢复等级、历史修订、公告重建、覆盖率和当前修订值不得进入正式历史回测。
- 复权锚点、相邻复权总收益、公司行动守恒，以及改变查询结束日不得静默改变同版本历史输入。
- 证券品种、板块、T+0/T+1、申报单位、价格步长、涨跌幅、上市初期规则及其按日期版本化。
- Qlib 优化器、风险模型、指标口径；累计收益几何口径和年化 252。
- `return_contract_version`、`label_contract_version` 与 `feature_materialization_contract_hash`；离线批处理和按事件逐步重放的特征、预测与目标在冻结容差内一致。
- 截面 IC/回归使用日期或独立事件为统计单位，HAC/聚类标准误和重叠标签依赖处理正确；证券数和分钟 Bar 数不得充当独立样本数。
- 人民币本位币、汇率报价方向、异步交易日历、跨境 ETF 溢价、通胀 vintage、实际 TWR/XIRR、债券久期/凸性/信用利差和底层持仓陈旧度。
- 全收益基准、现金计息、TWR/XIRR、外部现金流、收益归因和绩效证据类型隔离。
- 官方全收益指数、合成全收益代理和可投资基准组合隔离；合成基准构建版本、跟踪差异和禁止冒充官方指数。
- TWAP/VWAP 窗口、切片、跨日、费用、参与率、停牌、涨跌停和 T+1。
- `signal_frequency`、`decision_frequency`、`rebalance_frequency` 与 `monitor_frequency` 不得混淆；覆盖月度、周度、日度和盘中能力，验证任意已启用策略子集都能独立运行，隔离可选周频正式决策与固定周度研究任务，并验证实时分钟权限、完整 Bar、候选池、下一 Bar 信号与日终历史分钟对账。
- 数据可用时间、水位线、迟到/修订数据更正事件；历史回放、前向日线模拟和盘中实时模拟不得混用。
- 模板冻结字段、槽位类型、RD-Agent `slot_bindings`、原子试验台账、标签持有期、purge/embargo、嵌套 walk-forward、有效事件数、区块 Bootstrap、FDR/DSR/PBO、模型注册、漂移、重训、回滚和全局试验预算。
- `engine_type`、`strategy_family` 与 `recipe_id` 分类隔离；旧 `strategy_type` 不得驱动新生产行为。
- `optimizer_contract_version` 的目标函数、惩罚量纲、求解残差、不可行/不精确状态、数值环境、确定性和 `discrete_repair_contract`；整手修复后重新验证现金、行业、Tracking Error、换手、容量和风险。
- 试验预算随 `N_eff`、候选相关性和模型复杂度收缩；预算耗尽和改名/换入口绕过必须被阻断。
- 自适应研究的探索/确认隔离、锁箱不可见和一次性解封、序贯停止校正、跨程序同一经济假设预算及查看锁箱后的永久关闭。
- 市场状态分类和简单基线预注册；改变区间或分类阈值不得静默改变同版本结果。
- `rank_score`、`expected_return` 和 `event_probability` 语义隔离；滚动样本外校准、预测不确定性、分组收益单调性、概率可靠性和保守净收益下界。
- 标签随机化、信号置换/平移、随机因子和未来字段泄露哨兵；统计显著性与经济显著性同时通过。
- RD-Agent 沙箱断网、只读数据、密钥隔离、资源预算、依赖白名单、漏洞/许可证扫描和日志脱敏。
- RD-Agent/LLM 知识截止、检索资料和 `idea_available_at` 记录；历史知识风险较高或未知的候选不得仅凭历史回测晋升，LLM 完全不可用时正式生产链继续运行。
- ETF 同类去重和资产配置、Swing 方向门与退出、T+0 ETF/股票 T+1 分钟标签隔离、配对双腿原子成交、卖空资格和借券成本。
- ETF 管理/托管等持续费率、跟踪差异、价差、折溢价和交易冲击分项且不重复扣减；公司行动前未完成订单调整/取消和股息进入现金规则正确。
- ETF 与直接股票持仓的底层暴露穿透、陈旧度、重复集中度，以及配对单腿延迟和部分成交压力。
- 协方差有效样本、特征值、条件数、正半定、风险贡献、风险预算偏差、相关性置信区间和求解失败回退；ERC 阻断负贡献，一般组合验证对冲性负贡献；回撤超调、CDaR、Expected Shortfall、恢复时间及人民币损失概率。
- `capital_budget`、`target_volatility`、`risk_budget` 和实际风险贡献分离；禁止恢复 `risk_budget × strategy_weight` 合成。核心/卫星比例以正常状态风险资产资本为分母，风险退出不得为了满足 70% 核心比例而加仓。
- 指数增强主动回撤/Tracking Error 与账户绝对回撤隔离；ETF 配置允许按预注册效用路径准入；执行算法只按实现差额合同准入。
- `personal_stock_core` 与 100 只指数增强为独立模板和证据；高持仓指数增强未通过人工执行能力门时只能进入模拟盘。
- 实际启用的月、周、日、分钟策略级信号分别保存；未启用频率不生成占位信号；同一证券相反目标先净额合并，硬风险覆盖优先，最终每只证券只能产生一条账户级综合建议。
- 计算、决策和通知事件隔离；月频目标非调仓日不重复发布，连续相同的 5min 等待、`HOLD` 或 `NO_ACTION` 不重复通知，通知去重和硬风险直达规则通过测试。
- `BUY`、`SELL`、`HOLD`、`EXIT`、`NO_ACTION`、等待和未成交语义隔离；动作原因、阻断原因和目标有效性可追溯。
- 个人影子账户导入、人工部分成交、入金/出金、持仓偏差、加密和不可变修正。
- 券商只读适配器的默认关闭、权限范围验证、独立网络/进程、接口允许列表、禁止委托/撤单/资金操作和权限异常自动禁用。
- 个人影子账户快照年龄、现金/持仓/可卖数量置信度、未确认成交、人工干预归因和状态过期时的建议降级。
- 订单全生命周期、冻结/在途资金、复式分录、税务批次、持有期股息税、估值层级、陈旧价格和会计恒等式。
- 个人投资政策版本、约束叠加、无法执行原因和账户级建议差异。
- 个人投资声明、政策基准、人民币损失上限、风险能力与用款计划；回撤冷静期和分阶段重新入场。
- 资金桶、目标负债现金流、目标资金缺口、通胀后金额、提款顺序风险和目标成功率区间；无法可信估计时禁止伪精确概率。
- 单一量化子账户与家庭/个人总资产视图隔离；系统外资产只能收紧风险，不得虚构跨账户现金或订单。
- 动态不交易区间、最低佣金、成本与预测不确定性缓冲，以及人工成交反向校准成本模型。
- 数据订阅、LLM/API、算力、存储、通知、人工维护和恢复时间的总成本分项、分摊一致性及复杂策略相对简单基线的个人净增量价值。
- 50 万元、100 万元、500 万元和用户实际 NAV 下的整手、最低佣金、现金残余、持仓数与可执行比例；人工执行证券数、操作数和响应时限。
- 历史 Bar 回测、历史执行回放、实时 Bar 模拟和用户确认成交证据等级；Implementation Shortfall、未成交机会成本和禁止模拟盘口队列。
- 跨日/多 Bar 过渡组合在每次部分成交后的 Tracking Error、现金、暴露、容量、剩余订单修复和目标失效取消。
- 账户总账与策略虚拟分账一致；内部净额抵消、外部成交和成本/机会成本能够按冻结规则归因且不产生第二套订单。
- 通知发送、送达、确认、备用渠道、端到端延迟、时钟漂移和分钟策略通知失败时的 fail-closed。
- 推荐质量统计覆盖提醒数量、重复、过期、撤回、确认、实际执行、人工偏差和错过窗口；不得删除未执行或亏损建议美化成绩。
- 无最新账户快照、只读同步或人工成交状态时，分钟个人开仓建议必须降级为目标权重或 `simulation_only`。
- PostgreSQL/制品备份、密钥恢复、`RPO/RTO` 和隔离恢复演练。
- 开发/验证/正式模拟环境隔离、全局 `safe_mode` 和运营 SLO 违约降级。
- 旧批准版本、简单基线、保持目标、降低风险和全现金的预注册回退顺序；Qlib、RD-Agent/LLM、Tushare、求解器和通知渠道故障域演练。
- 虚拟市场时钟确定性回放、固定日报/周报/月报/季度报告、制品容量与保留策略、数据授权与再分发阻断。
- 回测、回放与模拟 `execution_contract_hash` 一致；重启/重试幂等；不产生 QMT/券商真实委托、撤单或资金请求，只读查询严格受授权和允许列表约束。

### 16.2 量化与运营完成标准

- [ ] 本模块已实现、测试并通过验收

- 首期必选的 ETF 资产配置和 `personal_stock_core` 分别至少 504 个样本外交易日，并通过多本金和人工执行能力门。指数增强按主动收益、IR、Tracking Error 和主动回撤合同独立准入；默认先认证完整模拟能力，通过人工执行能力后才能形成个人正式数量建议。全市场多因子若进入正式池，同样按自己的合同满足全部门槛。
- 月频核心还必须在外层 walk-forward 与最终锁箱中合计达到至少 60 个历史样本外调仓事件；504 个交易日不能被解释成 504 个独立月频样本。
- 周频战术模板若申请正式准入，至少完成 104 个历史样本外周度决策事件，并在完全认证推荐前完成至少 26 个真实前向周度决策周期；建议继续观察满 52 周。没有通过准入时保持未启用，不阻塞主系统验收。
- 至少 3 个滚动 252 日窗口和 5 个最差 20 日事件窗口。
- 市场阶段使用预注册合同覆盖，简单基线套件固定；不得在看到结果后重定义牛熊区间或替换基线。
- 通过双倍成本、收紧换手、持仓和容量压力测试。
- 至少形成一个已审批 ETF 核心、一个已审批股票核心和一个已审批核心分配；周频、Swing、分钟卫星和全市场多因子必须具备完整模板、回测、模拟和启停能力，但是否进入正式推荐取决于各自独立准入；配对卫星不是首期主系统验收的阻塞项。
- 每个核心成员及组合至少完成连续 60 个交易日的前向模拟；该期限只证明数据、调度、账本、恢复和推荐链可以连续运行，不能单独证明月频策略具有稳定投资价值。
- 月频核心完成至少 6 个真实前向调仓周期后最多进入 `provisional_investment_evidence`，建议继续覆盖12个月和不同市场阶段；不足6次时状态为 `forward_observation`，两种状态都可以生成明确标记的观察建议和模拟证据，但不得显示为已完成长期投资验证。
- 前向证据分为 `operationally_certified`、`provisional_investment_evidence` 和 `fully_certified`。连续运行和6次月度决策只能证明运营稳定与初步投资证据；月频完全认证默认至少12个真实前向调仓周期并覆盖不止一种市场环境。因数据历史不足例外时必须人工审批并保持较低证据等级。
- 日频、周频和分钟卫星申请准入时必须另外定义最少有效决策事件、成交事件和完整持仓周期；仅运行满日历天数但没有足够信号或成交，不算投资表现验收通过。
- 前向模拟期间不得修改历史推荐、成交、成本或 NAV；与同版本回放不一致时必须阻断。
- 主模拟账户、压力账户和个人影子账户分别完成日终对账；个人影子账户至少完成一次导入、部分成交、外部现金流、公司行动和修正版本演练。
- 主模拟账户必须完成订单状态、冻结/在途资金、税务批次、股息税、估值异常和复式分录重放验收；任一会计恒等式不平衡时不得认证 NAV。
- 主模拟账户必须在50万元、100万元、500万元和个人账户当前 NAV 下完成离散订单与费用敏感性验收；高持仓或高切片策略还必须通过人工执行能力门，否则只能认证模拟能力。
- 至少各完成一次历史 Bar 回测、历史执行回放、实时 Bar 模拟和用户确认人工成交对账，Implementation Shortfall 能从决策价追溯到实际成交或未成交终态。
- 至少完成一次 10% 组合回撤状态机演练，覆盖风险退出、冷静期、分阶段重新入场和二次触发回退。
- 回撤状态机必须与无固定止损、波动率缩放、趋势过滤和渐进减仓对照比较，并绑定明确的回撤概率期限与路径模型。
- 个人投资声明和政策基准完成审批；账户级报告能够同时展示绝对收益、策略基准超额和政策基准超额。
- 主通知渠道和备用渠道完成发送、失败重试、送达、确认、去重和硬风险升级演练；分钟提醒的端到端实测延迟满足已启用策略契约。
- 确定性虚拟时钟重放产生相同决策、订单、分录和 NAV；日报、周报、月报、季度报告及制品归档/恢复演练通过。
- 不存在未解决严重风险、过期批次或血缘不一致。
- Tushare 权限不足时可完成代码验收，但运营验收必须保持阻断。

## 17. 固定实施顺序

- [ ] 本模块已实现、测试并通过验收

| 阶段 | 必须完成 | 进入下一阶段的条件 |
| --- | --- | --- |
| 第零阶段 | Tushare 接口权限、费用、限流、历史深度、PIT 可恢复率、分钟延迟、候选池容量、存储/重建预算，标准化 provider/退出计划，以及 Qlib/RD-Agent 固定版本可行性 | 形成可审计 `data_capability_matrix` 和 `provider_exit_plan`；不可行能力被缩小范围或保持未启用，不再以设计假设进入实施 |
| 第一阶段 `personal_mvp` | Tushare 日线/PIT 数据及可恢复等级、收益/标签/特征物化契约、复权契约、证券市场规则、Qlib 数据集、ETF 资产配置、`personal_stock_core`、简单基线、最小目标/负债资金桶、平台总成本、日度风险门、指标/预测/优化/准入契约，以及最小“目标→订单→模拟成交→账本→对账”闭环 | ETF 与低复杂度股票核心按各自合同完成回测、离散修复、多本金、人工执行和前向观察；月度建议、每日风险检查、资金缺口报告和最小模拟闭环可供个人持续使用 |
| 第二阶段 | 指数增强完整模拟、可选周频模板与调度能力、Swing、全市场行业受控多因子、多频率净额合并、资本/风险预算隔离、ETF/股票穿透风险、跨境/通胀/固定收益合同、协方差与风险贡献硬门、政策基准、全收益基准、绩效归因、动态不交易区间、过渡组合风险与可复算综合推荐 | 在周频/日频策略分别关闭和启用的情景下，唯一账户级综合推荐、冲突处理、优化器连续/离散约束、跨日执行过渡风险、回测、风险诊断与模拟证据都通过 |
| 第三阶段 | 多本金统一模拟账户、复式分录、交收与税务批次、估值层级、完整个人投资声明与目标负债、人工执行政策、账户状态新鲜度、个人影子账户、可选只读同步、人工成交确认、回撤恢复状态机、通知送达、告警和备份恢复 | 连续 60 日运营模拟、120 日确定性回放、多本金离散订单、会计重放、只读权限隔离、通知演练、回撤恢复、目标资金缺口和影子账户演练通过；月频策略继续累计前向调仓周期 |
| 第四阶段 | Tushare 实时分钟接入、离线/实时特征一致性、盘中调度、通知端到端延迟验收、执行证据等级与 Implementation Shortfall、低频目标的 5min 执行择时、T+0 ETF 分钟回归、股票隔夜回归、ML 挑战者和自适应研究统计隔离 | 完成实时分钟权限、接口级容量、完整性、下一 Bar、T+0/T+1 标签、跨频率净额合并、人工执行能力、通知 fail-closed、日终对账、锁箱不可见/一次性解封和 LLM 知识时间风险验收 |
| 第五阶段 | 配对/统计套利研究与假设性融券模拟 | 有明确借券假设；股票空腿默认不进入正式推荐 |

> **定版完成定义**
>
> 第一阶段通过只代表 `personal_mvp` 可持续用于个人研究、回测、月度建议、每日风险检查、目标资金缺口观察和模拟，不代表最终范围完成。当第四阶段验收通过时，系统已经具备月度、周度、日度和 1min/5min 的模板、调度、回测、模拟、启停和综合推荐能力，并完成预测校准、自适应研究隔离、分策略准入、跨资产金融合同、风险诊断、多本金验收、过渡组合风险、统一模拟交易、策略虚拟分账、执行证据、复式账本、税务与估值、个人影子账户、可选只读同步、人工执行政策、目标负债、回撤恢复和通知送达闭环，但不要求四种频率的 Alpha 策略同时启用。月频核心和每日风险监控为主线；其他策略只有通过独立准入并显式启用后才进入正式推荐。系统能力完成不等于每个检查周期都必须买卖；没有新决策时保持原目标且不重复通知。第五阶段完成后，才算连同配对/统计套利研究、非原子执行压力与假设性融券模拟在内的全部最终稿范围完成。

## 附录 A：QMT 可选插件边界

- [ ] 本模块已实现、测试并通过验收

QMT/券商交易网关保留为默认关闭的可选插件，不属于当前研究、回测、审批或模拟主线。启用插件需要另行产品批准、独立安全评审和验收；在此之前：

- `BROKER_FEATURE_ENABLED` 默认且必须为 `false`。
- 主页面、计划任务、模拟批次和回放任务不得调用网关。
- 任何实盘环境控制、真实账户写操作或真实委托均不受本方案授权。

可选只读账户状态适配器必须与交易网关使用独立开关、独立凭据、独立进程和独立网络策略：

```text
BROKER_READ_ONLY_FEATURE_ENABLED=false
```

只有账户状态与对账服务可以调用经过白名单审查的余额、持仓、可卖数量、当日成交和未完成委托查询接口。它不得与交易网关共享凭据、代码路径、会话、网络出口或权限角色；一旦检测到权限扩张、写接口可达或响应语义异常，必须自动停用。真实账户控制和交易始终不受授权；满足本契约的只读状态查询属于明确批准的例外。

## 附录 B：外部规则与技术来源

- [ ] 本模块已实现、测试并通过验收

- [Tushare A 股日线行情](https://tushare.pro/document/1?doc_id=27)
- [Tushare 历史分钟行情](https://tushare.pro/document/2?doc_id=370)
- [Tushare A 股实时分钟行情](https://tushare.pro/document/2?doc_id=374)
- [Tushare ETF 实时分钟-日累计](https://tushare.pro/document/2?doc_id=470)
- [上海证券交易所 ETF T+0/T+1 规则说明](https://www.sse.com.cn/assortment/fund/etf/question/c/c_20240118_5734755.shtml)
- [上海证券交易所交易规则（2026 年修订）](https://www.sse.com.cn/lawandrules/sselawsrules2025/trade/universal/c/c_20260424_10816492.shtml)
- [深圳证券交易所交易规则（2026 年修订）](https://www.szse.cn/lawrules/rule/allrules/bussiness/t20260424_620190.html)
- [北京证券交易所交易规则（2026 年）](https://www.bse.cn/jygl_list/200028217.html)
- [财政部、税务总局证券交易印花税公告](https://www.mof.gov.cn/jrttts/202308/t20230828_3904235.htm)
- [中国结算上海/深圳市场收费表](https://www.chinaclear.cn/)
- [Qlib 官方仓库与文档](https://github.com/microsoft/qlib)
- [RD-Agent 官方仓库](https://github.com/microsoft/rd-agent)

外部链接只用于解释当前参数来源。系统运行时必须把实际采用的接口版本、权限证据、费率生效日和上游提交标识固化到不可变制品中。

## 附录 C：修订记录

- [ ] 本模块已实现、测试并通过验收

| 版本 | 日期 | 状态 | 修订摘要 |
| --- | --- | --- | --- |
| 4.4 | 2026-07-16 | 最终架构与产品基准 | 新增默认关闭且严格只读的券商账户状态适配器；增加目标负债与资金桶、汇率/通胀/固定收益合同、自适应研究序贯统计、平台总成本和多日执行过渡组合风险；明确 RD-Agent 调用 Qlib 的唯一自动研究入口、研究期与确认期两次 Qlib 角色、冻结 Qlib 正式生产链；同步扩展接口、测试、实施顺序和验收 |
| 4.3 | 2026-07-16 | 已取代 | 分离资本预算、目标波动率和风险预算，新增优化器/离散修复、收益标签特征物化和LLM知识时间契约；将个人低复杂度股票核心与100只指数增强分开；增加策略三层分类、核心/卫星分母、动作语义、家庭系统外暴露、ETF总成本、公司行动未完成订单、推荐质量、故障回退和数据供应商退出计划 |
| 4.2 | 2026-07-16 | 已取代 | 增加阶段0数据/权限可行性、个人首版范围、固定投资决策优先级和本人自用边界；新增指标数学、试验预算、市场状态、简单基线、分策略准入和回撤概率契约；修正指数增强绝对回撤、ETF配置固定超额、一般组合负风险贡献和固定相关性硬门；增加策略虚拟分账、分钟个性化建议边界、回撤政策对照、安全模式、环境隔离和运营SLO |
| 4.1 | 2026-07-16 | 已取代 | 补齐预测分数到收益/概率的样本外校准与不确定性契约、协方差和风险贡献失败关闭、回撤超调与人民币损失概率、50万/100万/500万元及实际NAV多本金验收、人工执行能力和账户状态新鲜度、执行证据等级与Implementation Shortfall、确定性回放、标准报告、制品容量与数据许可治理；修正固定17:30冻结历史分钟的假设 |
| 4.0 | 2026-07-16 | 已取代 | 冻结最终设计治理边界；新增个人投资目标与政策基准、PIT 可恢复等级、严格复权数学、单数据源独立控制面、成本实盘偏差校准、负对照与经济显著性、动态不交易区间、ETF/股票穿透风险、配对非原子压力、回撤冷静期与分阶段恢复、订单生命周期、复式账本、交收/税务批次、估值层级及通知送达确认闭环；增加模块完成勾选框和最终 TODO List |
| 3.5 | 2026-07-16 | 定稿 | 将多频率能力与策略启用解耦：月频核心和日度风控为主线，周频、Swing 和分钟卫星仅在独立准入后启用；新增 `monitor_frequency` 及计算/决策/通知事件，禁止月频目标和连续相同 5min 状态重复提醒；保留账户级唯一综合推荐 |
| 3.4 | 2026-07-16 | 已取代 | 新增正式周频战术调仓；固定月、周、日、1min/5min 四层职责、样本要求和运行顺序；规定策略级信号分别保存、账户层净额合并并只输出唯一综合推荐；将多频率主系统完成条件推迟到分钟链验收完成 |
| 3.3 | 2026-07-16 | 已取代 | 将策略模板明确为项目基于 Qlib 的冻结合同；新增资格、去重、方向、排名、买点、退出、组合与执行插槽；规定 RD-Agent 只能提交槽位绑定；新增 ETF 资产配置；拆分 T+0 ETF 与股票 T+1 分钟回归；修正 Swing 指标先后关系、Qlib 优化能力边界及嵌套样本外和多重检验要求 |
| 3.2 | 2026-07-16 | 已取代 | 将第一版的均线、突破、ADX、MACD、布林带、RSI、乖离率、因子、配对和ML等内容纳入版本化Qlib研究组件库；固定五类正式策略模板；明确RD-Agent只能在模板扩展点提出候选，不能直接修改或发布生产策略 |
| 3.1 | 2026-07-16 | 已取代 | 明确运行责任和固定日历；区分历史/实时分钟与下一 Bar 执行；新增个人影子账户与投资政策、证券市场规则版本、数据水位、全收益基准与绩效归因、purge/embargo 与模型生命周期、RD-Agent 沙箱安全、动态容量、RPO/RTO、策略前向观察及最小模拟闭环 |
| 3.0 | 2026-07-16 | 已取代；DOCX快照 | 明确 500 万元个人投资授权和除实盘外的完整范围；固定数据单位、复权与公司行动、费用与冲击、反过拟合、策略准入、推荐输出、模拟账本、暂停淘汰、60 日模拟与分阶段验收；权威维护载体迁移为 Markdown |
| 2.0 | 2026-07-16 | 已取代 | 原位重整为 Qlib + RD-Agent 技术基准单主线；固定 Tushare、非实盘/非高频边界；定义双核心、配对卫星、统一模拟交易、核心/卫星分配与验收 |
| 1.x | 历史 | 已取代 | 原稿由 Git 历史及冻结 DOCX 副本保存，不再作为当前方案 |

## 附录 D：最终实施 TODO List

- [ ] 本模块已实现、测试并通过验收

以下清单是 4.4 最终稿的唯一总体实施清单。勾选某项前，必须同时存在代码实现、自动化测试、可复算制品和对应验收证据；只完成页面、接口占位或单元测试不得勾选。

### D.1 产品、投资政策与治理

- [ ] 本模块已实现、测试并通过验收

- [ ] 冻结唯一主线、非实盘边界和单一生产路径，完成退役路径扫描。
- [ ] 建立验收本金、模拟资本和个人影子账户 NAV 三种独立资本口径。
- [ ] 建立个人投资声明：可投资资产、应急资金、期限、用款、人民币损失上限和风险能力。
- [ ] 建立 `household_exposure_policy`，区分量化子账户风险与家庭/个人总资产视图，纳入系统外账户、资产和负债的只读汇总。
- [ ] 建立 `goal_liability_contract_version`、应急/中期/长期资金桶、目标资金缺口、提款顺序风险和目标成功率区间。
- [ ] 建立版本化账户政策基准，并完成策略基准、分配基准和政策基准隔离。
- [ ] 建立 `human_execution_policy`，冻结个人每日证券数、单批操作数、响应窗口、未确认上限和降级规则。
- [ ] 冻结“法规/数据/人民币损失/执行/账户风险/策略约束/Alpha”的投资决策优先级。
- [ ] 冻结仅供本人使用边界；对外个性化建议、收费或多用户场景另行合规审批。
- [ ] 定义并验收 `personal_mvp`，与最终全部功能范围分开声明。
- [ ] 建立模块勾选、审批、版本、生效日期、回滚和不可变审计机制。
- [ ] 建立 `platform_total_cost_contract_version`，分项记录数据、LLM、算力、存储、通知、维护和恢复成本。

### D.2 数据、PIT、复权与市场规则

- [ ] 本模块已实现、测试并通过验收

- [ ] 完成 Tushare 原始不可变快照、血缘、水位线、哈希和权限证据。
- [ ] 完成标准化 provider 契约和 `provider_exit_plan`，演练接口停用、授权终止、长期故障及替代数据源重新准入。
- [ ] 完成阶段0 `data_capability_matrix`：权限、费用、限流、历史深度、PIT恢复率、延迟分位数、分钟候选池、存储和重建预算。
- [ ] 为财务、成分、行业、退市和公司行动数据建立 `pit_quality` 与历史可恢复率报告。
- [ ] 实现原始价、锚定复权价和相邻复权总收益三种明确口径及回归测试。
- [ ] 完成日线成交量/成交额、分钟单位、跨接口恒等式和日线/分钟聚合校验。
- [ ] 建立交易所/监管/中国结算独立控制面，核验日历、规则和关键公司行动。
- [ ] 完成证券品种、板块、T+0/T+1、涨跌停、申报单位和历史规则版本。
- [ ] 完成历史分钟、实时分钟、完整 Bar、候选池、限流、延迟和盘后逐 Bar 对账。
- [ ] 完成独立 `control_reference_provider` 及汇率、通胀、利率、官方基准和控制数据的版本、可用时间与隔离。

### D.3 Qlib、RD-Agent 与研究治理

- [ ] 本模块已实现、测试并通过验收

- [ ] 固定并验证 Qlib/RD-Agent 版本、提交标识、依赖锁和制品兼容。
- [ ] 完成正式策略模板、`engine_type`/`strategy_family`/`recipe_id`、组件插槽、冻结字段、标签和执行契约。
- [ ] 建立 `return_contract_version`、`label_contract_version` 和 `feature_materialization_contract_hash`，通过离线与逐事件重放一致性验收。
- [ ] 建立 `optimizer_contract_version` 和 `discrete_repair_contract`，冻结目标函数、惩罚量纲、求解器/数值环境、容差和整手后约束修复。
- [ ] 完成 RD-Agent 沙箱、断网、密钥隔离、资源预算、依赖白名单和日志脱敏。
- [ ] 记录 LLM 知识截止、检索资料、`idea_available_at` 与历史知识风险；验证 LLM/RD-Agent 不可用不影响正式生产链。
- [ ] 完成原子试验台账、全局研究预算、最终锁箱和不可重置失败历史。
- [ ] 完成 `adaptive_research_inference_contract_version`、探索/确认隔离、锁箱不可见、一次性解封、序贯校正和跨程序统计预算。
- [ ] 建立随 `N_eff`、候选相关性和模型复杂度变化的 `trial_budget_contract`，删除固定候选数乘参数数组合。
- [ ] 完成嵌套 walk-forward、purge/embargo、区块 Bootstrap、FDR、DSR 和 PBO。
- [ ] 预注册市场状态分类和简单基线套件，禁止按结果修改牛熊区间或替换基线。
- [ ] 建立 `metric_contract_version`，冻结 Sharpe、IR、Alpha、绝对/主动回撤、Tracking Error 和风险概率公式。
- [ ] 为指数增强、ETF配置、股票核心、卫星、配对和执行算法建立独立 `admission_contract_version`。
- [ ] 完成 `rank_score`、`expected_return`、`event_probability` 语义隔离、滚动校准、收缩和预测不确定性。
- [ ] 完成标签随机化、信号置换/平移、随机因子和未来字段泄露哨兵。
- [ ] 同时验收统计显著性、经济显著性、成本后收益、容量和简单基线增量。
- [ ] 完成模型注册、漂移、重训候选、审批切换和旧版本回放/回滚。

### D.4 策略与组合

- [ ] 本模块已实现、测试并通过验收

- [ ] 完成 ETF 资产配置核心模板及资产类别、折溢价、跟踪质量和同类去重。
- [ ] 完成跨境 ETF 的本位币/汇率/异步日历合同，以及债券/货币 ETF 的久期、凸性、利差和底层持仓陈旧度风险。
- [ ] 完成 `personal_stock_core` 低复杂度股票核心及 10–30 只、月度低换手、多本金和人工执行硬门。
- [ ] 完成指数增强完整模拟模板及 PIT 基准、行业/风格、跟踪误差和月度调仓；未通过人工执行能力门时不得输出个人正式数量建议。
- [ ] 完成全市场行业受控多因子模板及宽基全收益基准。
- [ ] 完成可选周频、Swing、T+0 ETF 分钟回归和股票 T+1 隔夜回归模板。
- [ ] 完成配对研究、假设性融券、原子账本和非原子人工执行压力测试。
- [ ] 分离 `capital_budget`、`target_volatility`、`risk_budget`、成员上限、协方差、实际风险贡献和现金缩放，删除 `risk_budget × strategy_weight` 生产公式。
- [ ] 冻结核心/卫星比例分母为正常状态风险资产资本；验证风险退出、现金缩放和不可交易状态不会为了满足70%核心比例而加仓。
- [ ] 完成协方差有效样本、特征值、条件数、正半定检查、相关性置信区间、ERC风险预算偏差与失败回退。
- [ ] 区分 ERC 必须为正的风险贡献与一般组合可解释的对冲性负风险贡献。
- [ ] 完成 ETF 与直接股票持仓的底层暴露穿透、陈旧度和重复集中度限制。
- [ ] 完成账户级净额合并、冲突解释、硬风险覆盖和唯一综合推荐。
- [ ] 完成动态 `no_trade_band`，纳入最低佣金、税费、冲击和预测不确定性。

### D.5 回测、成本、容量与风险

- [ ] 本模块已实现、测试并通过验收

- [ ] 完成下一 Bar、PIT 股票池、退市证券、停牌、涨跌停和公司行动回测。
- [ ] 完成公司行动生效前未完成订单的取消/调整、股息进入现金及独立再投资规则。
- [ ] 完成 Qlib 正式回测、执行回放、Recorder 和 `execution_contract_hash` 一致性。
- [ ] 完成全收益基准、现金计息、TWR、XIRR、收益归因和证据类型隔离。
- [ ] 区分 `official_total_return_index`、`synthetic_total_return_proxy` 和 `investable_benchmark_portfolio`，完成合成基准跟踪差异校验。
- [ ] 完成佣金、印花税、过户费、滑点、冲击、参与率和双倍成本压力。
- [ ] 完成 ETF 管理/托管等持续费用、跟踪差异、价差、折溢价和成交冲击的分项成本及防重复扣减。
- [ ] 使用模拟/人工成交偏差分组校准成本模型，并完成样本外验证。
- [ ] 完成建仓、减仓、清仓天数和多日执行容量验证。
- [ ] 完成 `transition_portfolio_contract_version`，每次部分成交后重算过渡组合风险并修复或取消剩余订单。
- [ ] 完成牛熊、急跌、跳空、流动性收缩、相关性上升和尾部依赖压力测试。
- [ ] 完成带明确期限、路径模型和置信区间的回撤超调、CDaR、Expected Shortfall、恢复时间和人民币损失超限概率。
- [ ] 对比固定回撤止损、无止损、波动率缩放、趋势过滤和渐进减仓，验证低位卖出与重新入场成本。
- [ ] 完成50万元、100万元、500万元和个人账户当前 NAV 的整手、最低佣金、现金残余和可执行性验收。
- [ ] 完成 `normal → caution → risk_reduced → risk_off → cooldown → staged_reentry` 状态机。

### D.6 模拟交易、账本、税务与估值

- [ ] 本模块已实现、测试并通过验收

- [ ] 完成目标、计划、释放、接受、部分成交、成交、撤单、拒绝和过期订单生命周期。
- [ ] 完成 1min/5min 执行、T+0/T+1、冻结资金/数量、参与率和跨日订单。
- [ ] 建立不可变复式分录，能够从事件流重放现金、持仓、损益和 NAV。
- [ ] 区分账面、可用、冻结、在途、可取现金，以及总持仓、可卖和待交收数量。
- [ ] 建立 `tax_lot_id`、持有期、剩余数量、批次成本和股息红利税规则。
- [ ] 完成应收股息、应付税费、退市结算和其他公司行动会计处理。
- [ ] 完成估值来源、估值时点、价格陈旧度、异常估值层级和未认证 NAV。
- [ ] 完成幂等、重启恢复、日终对账和会计恒等式阻断。
- [ ] 完成四级执行证据、决策价到人工成交价的 Implementation Shortfall 和未成交机会成本。
- [ ] 建立账户总账与 `strategy_virtual_subledger`，完成内部净额抵消、外部成交、成本和机会成本归因。
- [ ] 完成虚拟市场时钟确定性回放，相同输入产生相同订单、分录和 NAV。
- [ ] 完成数值性质测试：成本单调性、容量单调性、公司行动经济价值守恒和未来数据不改变过去决策。

### D.7 个人影子账户与推荐

- [ ] 本模块已实现、测试并通过验收

- [ ] 完成持仓、现金、成本、可卖数量、人工成交、费用和外部现金流导入。
- [ ] 完成默认关闭的券商只读账户状态适配器、权限证据、独立网络/进程、允许列表和禁止写操作验收。
- [ ] 完成导入快照校验、不可变修正、加密存储和最小权限。
- [ ] 完成账户快照年龄、现金/持仓/可卖数量置信度和未确认成交导致的建议降级。
- [ ] 无当日最新账户与未完成委托状态时，分钟个人开仓建议只能输出目标权重或 `simulation_only`。
- [ ] 同时展示模型目标、模拟计划、个人建议、用户确认和未执行偏差。
- [ ] 建立 `action_semantics_version`，严格区分 `BUY`、`SELL`、`HOLD`、`EXIT`、`NO_ACTION`、等待和未成交。
- [ ] 完成部分成交、未成交、系统外成交和实际税费/成交价差异对账。
- [ ] 完成人工覆盖原因、模型计划、模拟执行和用户实际结果的独立归因。
- [ ] 完成行业、风格、底层暴露、风险、收益和费用偏差报告。
- [ ] 完成家庭/系统外暴露汇总报告；无法覆盖时明确量化子账户报告的适用边界。

### D.8 调度、通知、运维与安全

- [ ] 本模块已实现、测试并通过验收

- [ ] 完成月、周、日、1min/5min 到期调度、交易日历、超时、重试和幂等。
- [ ] 完成计算、决策和通知事件隔离及通知去重。
- [ ] 完成主/备用通知渠道、失败重试、送达、确认、过期和硬风险升级。
- [ ] 实测数据到通知的端到端延迟；分钟策略不满足延迟时 fail closed。
- [ ] 完成时钟同步、时间漂移、网络中断和通知不可用降级演练。
- [ ] 完成数据、策略、账户、回撤、恢复和安全告警。
- [ ] 完成全局 `safe_mode`，验证只停止新增风险而继续数据、账本、退出提醒和审计。
- [ ] 隔离开发、研究验证和正式模拟环境的数据库、密钥、制品和任务队列。
- [ ] 建立数据水位、任务成功率、对账、推荐延迟、通知送达、账本和备份恢复 SLO 及违约降级。
- [ ] 建立推荐质量指标：提醒数量、重复、过期、撤回、确认、实际执行、人工偏差、错过窗口和 Implementation Shortfall。
- [ ] 完成每日行动简报、周度复核、月度绩效归因、季度生命周期和影子账户偏差报告。
- [ ] 完成 PostgreSQL、密钥、个人数据、Qlib 数据集和 Recorder 制品备份。
- [ ] 完成分钟数据与研究制品容量预算、分区、压缩、冷热分层、保留和归档校验。
- [ ] 完成 Tushare 数据权限、用途、对外展示和再分发边界治理。
- [ ] 完成 `fallback_policy_version`，演练旧批准版本、简单基线、保持目标、降低风险和全现金回退，以及 Qlib、RD-Agent/LLM、Tushare、求解器和通知故障域。
- [ ] 完成 `RPO ≤ 15 分钟`、`RTO ≤ 4 小时` 和季度隔离恢复演练。

### D.9 最终验收

- [ ] 本模块已实现、测试并通过验收

- [ ] ETF 核心、股票核心和核心分配分别通过正式样本外准入。
- [ ] `personal_stock_core` 与指数增强使用独立模板、回测和证据；高持仓指数增强未通过人工执行能力门时保持 `simulation_only`。
- [ ] 各策略按自己的准入合同验收；指数增强不再使用统一10%绝对回撤硬门，ETF配置按预注册效用路径验收。
- [ ] 所有拟启用策略通过成本、容量、反过拟合、负对照和压力测试。
- [ ] 所有自动研究候选通过探索/确认隔离、锁箱不可见、序贯统计和跨程序预算验收。
- [ ] 所有拟启用策略通过预测校准、协方差/风险贡献诊断、多本金和人工执行能力门。
- [ ] 所有拟启用策略通过收益/标签/特征一致性、优化器连续/离散约束、数值环境和动作语义验收。
- [ ] 核心成员和组合完成至少连续 60 个交易日前向模拟。
- [ ] 月频核心完成至少 6 个真实前向调仓周期，并继续以 12 个月为优选观察目标。
- [ ] 统一模拟账户完成 120 日回放、会计重放、公司行动、税务和估值演练。
- [ ] 跨境/通胀/固定收益合同、平台总成本、目标负债资金缺口和跨日执行过渡组合完成专项演练。
- [ ] 个人影子账户完成导入、部分成交、外部现金流、公司行动和修正版本演练。
- [ ] 可选只读适配器完成权限扩大、接口异常、断连和自动回退到手工/CSV 的故障演练。
- [ ] 完成账户状态过期、未确认成交和一分钟级建议阻断/降级演练。
- [ ] 完成 10% 回撤退出、冷静期、分阶段恢复和二次触发演练。
- [ ] 完成回撤风险政策与无止损、波动率缩放、趋势过滤及渐进减仓的对照验收。
- [ ] 完成通知发送、送达、确认、备用升级和分钟延迟验收。
- [ ] 完成推荐质量、提醒疲劳、家庭/系统外暴露边界和预注册故障回退验收。
- [ ] 完成执行证据等级、Implementation Shortfall、确定性回放和标准报告验收。
- [ ] 完成备份恢复、密钥恢复、幂等重放和故障注入。
- [ ] 无未解决严重风险、血缘不一致、过期批次、第二生产路径、真实委托/撤单/资金请求或未经授权的券商查询。
- [ ] 第四阶段全部通过后确认“除实盘下单外的完整个人量化投资系统”验收完成。
- [ ] 第五阶段通过后确认配对/统计套利研究与假设性融券模拟范围验收完成。
